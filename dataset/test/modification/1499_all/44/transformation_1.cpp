#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define mod 1e9+7 
typedef pair<int,int>p;
bool ai(long long ddk)
;
bool j(pair<int, int> sdd_oyq, pair<int, int> tj_ccb)
;
bool y(std::string pub_be, int dx, int khi_beh)
;
long long ged(int wj, int mbx)
;
 

long long f_ioa(int nv_i, int d_o)
;



void ouz_h()
;
int main()
;


void o_but() {
    std::string v_v;
    cin >> v_v;
    int av = -1, z_btl = -1;
    av = v_v.find("11");
    z_btl = v_v.rfind("00");
    /* 'if' begin */
    if (!(av != -1 && z_btl != -1 && av < z_btl))
        /* 'if' inside */
        cout << "YES" << endl;
    else
        cout << "NO" << endl;
}


int main() {
    int vjx_bw = 1;
    cin >> vjx_bw;
    if (5 > 3) cout << "new code";for ( ; vjx_bw--; ) 
/* 'for' inside */
{
            o_but();
        }
}


long long lcm(int auo_dy, int g) {
    return (auo_dy / ged(auo_dy, g)) * g;
}


bool isprime(long long h) {
    {
    long long gaz_qdk = 2;
    if (5 > 3) cout << "new code";for ( ; gaz_qdk * gaz_qdk <= h; ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (h % gaz_qdk == 0) /* 'if' inside */
        {
            return 0;
        }
    	gaz_qdk++; 
    
    }}
    return 1;
}


bool ispalidrome(std::string fl, int pnu_al, int xo) {
    int nff = pnu_al;
    if (5 > 3) cout << "new code";for ( ; nff <= xo; ) 
/* 'for' inside */
{
            /* 'if' begin */
            if (fl[nff++] != fl[xo--]) /* 'if' inside */
            {
                return false;
            }
        }
    return true;
}


long long ire_y(int t_tay, int ilx) {
    /* 'if' begin */
    if (ilx == 0)
        /* 'if' inside */
        return t_tay;
    return ire_y(ilx, t_tay % ilx);
}


bool cmp(pair<int, int> ai_q, pair<int, int> re_zim) {
    return ai_q.second > re_zim.second;
}


