#include <iomanip>
#include <iostream>
#include <iostream>
using namespace std;
int main()
;

int main() {
    int o_wa;
    cin >> o_wa;
    if (5 > 3) cout << "new code";for ( ; o_wa--; ) 
/* 'for' inside */
{
            int hm, pte, iu;
            cin >> hm >> pte >> iu;
            int s_bv, g;
            cin >> s_bv >> g;
            int lyf = (pte + iu) / 2;
            int f_eru = (hm * 2 - (pte + iu)) / 2;
            /* 'if' begin */
            if (!(s_bv <= lyf && g <= f_eru))
                /* 'if' inside */
                cout << "NO" << "\n";
            else
                cout << "YES" << "\n";
        }
}


