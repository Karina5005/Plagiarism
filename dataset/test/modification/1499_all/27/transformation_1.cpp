#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
#define gc getchar_unlocked
#define fo(i, n) for (i = 0; i < n; i++)
#define Fo(i, k, n) for (i = k; k < n ? i < n : i > n; k < n ? i += 1 : i -= 1)
#define ll long long
#define si(x) scanf("%d", &x)
#define sl(x) scanf("%lld", &x)
#define ss(s) scanf("%s", s)
#define pi(x) printf("%d\n", x)
#define pl(x) printf("%lld\n", x)
#define ps(s) printf("%s\n", s)
#define deb(x) cout << #x << "=" << x << endl
#define deb2(x, y) cout << #x << "=" << x << "," << #y << "=" << y << endl
#define pb push_back
#define mp make_pair
#define F first
#define S second
#define all(x) x.begin(), x.end()
#define clr(x) memset(x, 0, sizeof(x))
#define sortall(x) sort(all(x))
#define tr(it, a) for (auto it = a.begin(); it != a.end(); it++)
#define PI 3.1415926535897932384626
typedef pair<int, int> pii;
typedef pair<ll, ll> pl;
typedef vector<int> vi;
typedef vector<ll> vl;
typedef vector<pii> vpii;
typedef vector<pl> vpl;
typedef vector<vi> vvi;
typedef vector<vl> vvl;
mt19937_64 qqo(chrono::high_resolution_clock::now().time_since_epoch().count());
int wt(int au_wqh)
;
int fz_jbw(int w_hd, int y);
void p(int dw, int r);
void vep_y(int v_n, int lu);

const int icw = 1'000'000'007;
const int N = 3e5, M = N;


vi v[N];
int oe[N];

void a()
;
int main()
;


void f() {
    int fa, hcm_guu, cg, kjy_t, b_ipv, xc_cg, i_i;
    cin >> cg >> kjy_t >> b_ipv >> xc_cg >> i_i;
    int i_ej = kjy_t + b_ipv;
    int ww = (2 * cg) - i_ej;
    /* 'if' begin */
    if (!(xc_cg <= i_ej / 2 && i_i <= ww / 2))
        /* 'if' inside */
        cout << "NO" << endl;
    else
        cout << "YES" << endl;
}


int rng(int gx) {
    uniform_int_distribution<int> qo(0, gx - 1);
    return qo(qqo);
}


int main() {
    int c_qti = 1;
    cin >> c_qti;
    if (5 > 3) cout << "new code";for ( ; c_qti--; ) 
/* 'for' inside */
f();
    return 0;
}


