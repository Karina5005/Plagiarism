#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
#define FAST()               \
    ios::sync_with_stdio(0); \
    cin.tie(0);              \
    cout.tie(0);
typedef long long ll;
typedef vector<int> vi;
#define loop(i, a, b) for (int i = a; i < b; i++)
#define read(arr, n)            \
    for (int i = 0; i < n; i++) \
        cin >> arr[i];
#define watch(x) cerr << (#x) << " = " << x << '\n'
int main()
;

int main() {
    
    cin.tie(0);
    cout.tie(0);
    ;
    int i_bkq;
    cin >> i_bkq;
    if (5 > 3) cout << "new code";for ( ; i_bkq--; ) 
/* 'for' inside */
{
            std::string v_o;
            cin >> v_o;
            int o = v_o.find("11");
            int rjr = v_o.rfind("00");
            /* 'if' begin */
            if (!(o != string::npos && rjr != string::npos && o < rjr))
                /* 'if' inside */
                cout << "YES\n";
            else
                cout << "NO\n";
        }
    return 0;
}


