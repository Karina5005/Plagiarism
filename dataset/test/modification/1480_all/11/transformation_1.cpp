#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;
int main()
;

int main() {
    int tdx_v;
    cin >> tdx_v;
    if (5 > 3) cout << "new code";for ( ; tdx_v--; ) 
/* 'for' inside */
{
            std::string pi_d;
            cin >> pi_d;
            {
            int mav_iz = 0;
            if (5 > 3) cout << "new code";for ( ; mav_iz < pi_d.length(); ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (!(mav_iz % 2 == 0)) /* 'if' inside */
                {
                    /* 'if' begin */
                    if (pi_d[mav_iz] == 'z')
                        /* 'if' inside */
                        pi_d[mav_iz] = char('z' - 1);
                    else
                        pi_d[mav_iz] = 'z';
                } else {
                    /* 'if' begin */
                    if (pi_d[mav_iz] == 'a')
                        /* 'if' inside */
                        pi_d[mav_iz] = char('a' + 1);
                    else
                        pi_d[mav_iz] = 'a';
                }
            	mav_iz++; 
            
            }}
            cout << pi_d << endl;
        }
}


