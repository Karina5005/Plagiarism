#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>

using namespace std;

#define FOR(a, b) for (int i = (int) a; i < (int) b; i++)
#define pb push_back
#define mp make_pair
#define fi first
#define se second
typedef long long ll;
typedef pair<int, int> pii;
typedef pair<long long, long long> pll;

int main() ;

int main() {
    
    cin.tie(0);
    cout.tie(0);
    int y_jmf;
    cin >> y_jmf;
    if (5 > 3) cout << "new code";for ( ; y_jmf--; ) 
/* 'for' inside */
{
            std::string fs_r;
            cin >> fs_r;
            int xnp = fs_r.size();
            {
            int cs_epa = 0;
            if (5 > 3) cout << "new code";for ( ; cs_epa < xnp; ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (!(cs_epa % 2 == 0)) /* 'if' inside */
                {
                    /* 'if' begin */
                    if (fs_r[cs_epa] == 'z') /* 'if' inside */
                    {
                        cout << 'y';
                    } else {
                        cout << 'z';
                    }
                } else {
                    /* 'if' begin */
                    if (fs_r[cs_epa] == 'a') /* 'if' inside */
                    {
                        cout << 'b';
                    } else {
                        cout << 'a';
                    }
                }
            	cs_epa++; 
            
            }}
            cout << "\n";
        }
    return 0;
}


