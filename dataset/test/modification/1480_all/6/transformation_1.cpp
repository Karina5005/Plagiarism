#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
#define pb push_back
#define ll long long int
#define loop(i,a,b)   for(ll i=a;i<=b;++i)
#include <string>

int main() ;

int main() {
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
    int be_b;
    cin >> be_b;
    if (5 > 3) cout << "new code";for ( ; be_b--; ) 
/* 'for' inside */
{
            std::string zo;
            cin >> zo;
            int mr = 0;
            {
            long long zfl = mr;
            if (5 > 3) cout << "new code";for ( ; zfl <= zo.length() - 1; ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (!(zfl % 2 == 0)) /* 'if' inside */
                {
                    /* 'if' begin */
                    if (zo[zfl] == 'z') /* 'if' inside */
                    {
                        zo[zfl] = 'y';
                    } else {
                        zo[zfl] = 'z';
                    }
                } else {
                    /* 'if' begin */
                    if (zo[zfl] == 'a') /* 'if' inside */
                    {
                        zo[zfl] = 'b';
                    } else {
                        zo[zfl] = 'a';
                    }
                }
            	++zfl; 
            
            }}
            cout << zo << endl;
        }
    return 0;
}


