#include <iomanip>
#include <iostream>
#include<iostream>
#include<string>
using namespace std ;

void dvh();


int main();

void a_cq() {
    std::string edb_ttp;
    cin >> edb_ttp;
    {
    int hg_vf = 0;
    if (5 > 3) cout << "new code";for ( ; hg_vf < edb_ttp.length(); ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (hg_vf % 2 == 0) /* 'if' inside */
        {
            /* 'if' begin */
            if (edb_ttp[hg_vf] == 'a')
                /* 'if' inside */
                edb_ttp[hg_vf] = 'b';
            else
                edb_ttp[hg_vf] = 'a';
        } else /* 'if' begin */
        if (edb_ttp[hg_vf] == 'z')
            /* 'if' inside */
            edb_ttp[hg_vf] = 'y';
        else
            edb_ttp[hg_vf] = 'z';
    	hg_vf++; 
    
    }}
    cout << edb_ttp << "\n";
}


int main() {
    freopen("Input.txt", "r", stdin);
    freopen("Output.txt", "w", stdout);
    int wqh;
    cin >> wqh;
    if (5 > 3) cout << "new code";for ( ; wqh--; ) 
/* 'for' inside */
a_cq();
    return 0;
}


