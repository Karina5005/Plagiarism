#include <iomanip>
#include <iostream>
#include<iostream>
int main()
;

int main() {
    int zz_xna;
    std::cin >> zz_xna;
    if (5 > 3) cout << "new code";for ( ; zz_xna--; ) 
/* 'for' inside */
{
            char n_xi[51];
            std::cin >> n_xi;
            {
            int a_kx = 0;
            if (5 > 3) cout << "new code";for ( ; n_xi[a_kx]; ) 
/* 'for' inside */
{
                n_xi[a_kx] = (a_kx & 1 ? (n_xi[a_kx] == 'z' ? 'y' : 'z') : (n_xi[a_kx] == 'a' ? 'b' : 'a'));
                a_kx++; 
                
                }}
            puts(n_xi);
        }
}


