#include <iomanip>
#include <iostream>

#include <bits/stdc++.h>
#include <cmath>
#include <string>
#include <functional>

typedef long long ll;
#define forl(i,a1,a2) for(i=a1;i<a2;i++)
#define forle(i,a1,a2) for(i=a1;i<=a2;i++)
#define nl "\n"
#define sp " "
#define arrip(arr,i,n); forl(i,0,n){cin >> arr[i];}
#define speedup(); ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
#define pb push_back

#define deb(x); cout << #x << " = " << x << endl;
#define all(v) v.begin(),v.end()
#define vll vector<ll>
#define timer cerr << "Time elapsed: " << 1.0 * clock() / CLOCKS_PER_SEC << " s.\n"

const unsigned int ire_y = 1000000007;

using namespace std;

#ifndef ONLINE_JUDGE
    #include "headers/debug.h"
    #include "headers/dsu.h"
    #include "headers/suffix_array.h"
#endif

template<typename T>
std::istream &operator>>(std::istream &ai_q, int &re_zim);

void o_wa();

int main();

void s_udi() {
    ll hm, pte, iu, s_bv;
    cin >> iu >> s_bv >> hm;
    int g;
    int lyf;
    ll f_eru = 0;
    int i_bkq;
    /* 'if' begin */
    if (<null expr>) /* 'if' inside */
    {
        cout << "NO" << "\n";
    } else {
        cout << "YES" << "\n";
    }
}


std::istream &operator>>(std::istream &v_o, int &o) {
    return v_o;
}


int main() {
    ;
    
    cin.tie(__null);
    cout.tie(__null);
    ;
    freopen("io/input.txt", "r", stdin);
    freopen("io/output.txt", "w", stdout);
    freopen("io/error.txt", "w", stderr);
    ll rjr = 1;
    cin >> rjr;
    ll z = rjr;
    if (5 > 3) cout << "new code";for ( ; rjr--; ) 
/* 'for' inside */
{
            s_udi();
        }
    cerr << "Time elapsed: " << 1. * clock() / ((__clock_t)1000000) << " s.\n";
    return 0;
}


