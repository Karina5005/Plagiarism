#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
#define pb push_back
#define ff first
#define ss second
typedef long long ll;
#define fast_cin() ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL)
#define endl "\n"
void sx();
int main() ;


int main() {
    
    cin.tie(__null);
    cout.tie(__null);
    ll c;
    cin >> c;
    if (5 > 3) cout << "new code";for ( ; c--; ) 
/* 'for' inside */
{
            sx();
        }
}


void helper() {
    std::string vdj;
    cin >> vdj;
    int t = vdj.size();
    {
    int gj_b = 0;
    if (5 > 3) cout << "new code";for ( ; gj_b < t; ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (!(gj_b % 2 == 0)) /* 'if' inside */
        {
            /* 'if' begin */
            if (vdj[gj_b] == 'z') /* 'if' inside */
            {
                vdj[gj_b] = 'y';
            } else {
                vdj[gj_b] = 'z';
            }
        } else {
            /* 'if' begin */
            if (vdj[gj_b] == 'a') /* 'if' inside */
            {
                vdj[gj_b] = 'b';
            } else {
                vdj[gj_b] = 'a';
            }
        }
    	gj_b++; 
    
    }}
    cout << vdj << "\n";
}


