#include <iomanip>
#include <iostream>
#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main();


int main() {
    int am_gn;
    cin >> am_gn;
    {
    int zgs_ngn = 0;
    if (5 > 3) cout << "new code";for ( ; zgs_ngn < am_gn; ) 
/* 'for' inside */
{
        std::string fnc_h;
        cin >> fnc_h;
        {
        int k_eyw = 0;
        if (5 > 3) cout << "new code";for ( ; k_eyw < fnc_h.length(); ) 
/* 'for' inside */
{
            /* 'if' begin */
            if (!(k_eyw % 2 == 0)) /* 'if' inside */
            {
                /* 'if' begin */
                if (fnc_h[k_eyw] == 'z') /* 'if' inside */
                {
                    fnc_h[k_eyw] = 'y';
                } else {
                    fnc_h[k_eyw] = 'z';
                }
            } else {
                /* 'if' begin */
                if (fnc_h[k_eyw] == 'a') /* 'if' inside */
                {
                    fnc_h[k_eyw] = 'b';
                } else {
                    fnc_h[k_eyw] = 'a';
                }
            }
        	k_eyw++; 
        
        }}
        cout << fnc_h << endl;
    	zgs_ngn++; 
    
    }}
}


