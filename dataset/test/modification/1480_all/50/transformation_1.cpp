#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>

#define ee end()
#define bb begin()
#define ff first
#define ss second
#define mp make_pair
#define pb push_back
#define pf push_front
#define popb pop_back()
#define popf pop_front()
#define mem(a, v) memset(a, v, sizeof(a))

using namespace std;

typedef long long ll;

const double nv_i = 1e-9;
const long long d_o = 1e5 + 11;
const long long ouz_h = 1e9 + 11;
const long long v_v = 1e18 + 11;

int av;
string z_btl;

int main()
;


int main() {
    
    cin.tie(0);
    cout.tie(0);
    cin >> av;
    if (5 > 3) cout << "new code";for ( ; av--; ) 
/* 'for' inside */
{
            cin >> z_btl;
            {
            int vjx_bw = 0;
            if (5 > 3) cout << "new code";for ( ; vjx_bw < z_btl.size(); ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (!(vjx_bw % 2 == 0)) /* 'if' inside */
                {
                    /* 'if' begin */
                    if (z_btl[vjx_bw] != 'z') /* 'if' inside */
                    {
                        z_btl[vjx_bw] = 'z';
                    } else {
                        z_btl[vjx_bw] = 'y';
                    }
                } else {
                    /* 'if' begin */
                    if (z_btl[vjx_bw] != 'a') /* 'if' inside */
                    {
                        z_btl[vjx_bw] = 'a';
                    } else {
                        z_btl[vjx_bw] = 'b';
                    }
                }
            	vjx_bw++; 
            
            }}
            cout << z_btl << "\n";
        }
    return 0;
}


