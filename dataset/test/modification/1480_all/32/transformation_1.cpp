#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>

using namespace std;
#define int long long
int32_t main() ;

int32_t main() {
    long long cd_g;
    cin >> cd_g;
    if (5 > 3) cout << "new code";for ( ; cd_g--; ) 
/* 'for' inside */
{
            std::string s;
            cin >> s;
            {
            long long t_eb = 0;
            if (5 > 3) cout << "new code";for ( ; t_eb < s.length(); ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (!(t_eb % 2 == 0)) /* 'if' inside */
                {
                    /* 'if' begin */
                    if (s[t_eb] == 'z')
                        /* 'if' inside */
                        s[t_eb] = 'y';
                    else
                        s[t_eb] = 'z';
                } else {
                    /* 'if' begin */
                    if (s[t_eb] == 'a')
                        /* 'if' inside */
                        s[t_eb] = 'b';
                    else
                        s[t_eb] = 'a';
                }
            	t_eb++; 
            
            }}
            cout << s << endl;
        }
}


