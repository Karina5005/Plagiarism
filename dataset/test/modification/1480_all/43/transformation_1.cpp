#include <iomanip>
#include <iostream>

#include<bits/stdc++.h>
using namespace std;

int main() ;

int main() {
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
    int fij;
    cin >> fij;
    if (5 > 3) cout << "new code";for ( ; fij--; ) 
/* 'for' inside */
{
            std::string y;
            cin >> y;
            {
            int p = 0;
            if (5 > 3) cout << "new code";for ( ; p < int(y.size()); ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (!(p % 2 == 0)) /* 'if' inside */
                {
                    /* 'if' begin */
                    if (y[p] != 'z')
                        /* 'if' inside */
                        y[p] = 'z';
                    else
                        y[p] = 'y';
                } else {
                    /* 'if' begin */
                    if (y[p] != 'a')
                        /* 'if' inside */
                        y[p] = 'a';
                    else
                        y[p] = 'b';
                }
            	p++; 
            
            }}
            cout << y << endl;
        }
    return 0;
}


