#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;
long long b_fcl,os;
int main()
;




int main() {
    std::string m;
    cin >> b_fcl;
    if (5 > 3) cout << "new code";for ( ; b_fcl--; ) 
/* 'for' inside */
{
            cin >> m;
            {
            os = 0;
            if (5 > 3) cout << "new code";for ( ; os < m.size(); ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (!(os % 2 == 0)) /* 'if' inside */
                {
                    /* 'if' begin */
                    if (m[os] != 'z')
                        /* 'if' inside */
                        m[os] = 'z';
                    else
                        m[os] = 'y';
                } else {
                    /* 'if' begin */
                    if (m[os] != 'a')
                        /* 'if' inside */
                        m[os] = 'a';
                    else
                        m[os] = 'b';
                }
            	os++; 
            
            }}
            cout << m << "\n";
        }
}


