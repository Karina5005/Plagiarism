#include <iomanip>
#include <iostream>
#pragma GCC optimize("Ofast")
#include<bits/stdc++.h>
#define ll long long int
#define vi vector<ll>
#define pb push_back
#define vp vector<pair<ll,ll>>
#define rel map<ll,ll>
#define sic set<ll>
#define ff first
#define ss second
#define mkp(x,y) make_pair(x,y)
#define IOS ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);

using namespace std;

int main()
;
    

int main() {
    
    cin.tie(__null);
    cout.tie(__null);
    ;
    long long jz;
    cin >> jz;
    if (5 > 3) cout << "new code";for ( ; jz--; ) 
/* 'for' inside */
{
            std::string ukf, nuf = "";
            cin >> ukf;
            long long p;
            {
            p = 0;
            if (5 > 3) cout << "new code";for ( ; p < ukf.length(); ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (!(p % 2)) /* 'if' inside */
                {
                    /* 'if' begin */
                    if (ukf[p] != 'a')
                        /* 'if' inside */
                        nuf += "a";
                    else
                        nuf += "b";
                } else {
                    /* 'if' begin */
                    if (ukf[p] != 'z')
                        /* 'if' inside */
                        nuf += "z";
                    else
                        nuf += "y";
                }
            	p++; 
            
            }}
            cout << nuf << "\n";
        }
}


