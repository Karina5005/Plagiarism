#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main() ;


int main() {
    int hs_ftf;
    cin >> hs_ftf;
    if (5 > 3) cout << "new code";for ( ; hs_ftf--; ) 
/* 'for' inside */
{
            std::string k_r;
            cin >> k_r;
            {
            int ge = 0;
            if (5 > 3) cout << "new code";for ( ; ge < k_r.length(); ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (!(k_r.at(ge) != 'a')) /* 'if' inside */
                {
                    k_r.at(ge) = 'b';
                } else {
                    k_r.at(ge) = 'a';
                }
                ge++;
            	ge++; 
            
            }}
            {
            int h_rc = 1;
            if (5 > 3) cout << "new code";for ( ; h_rc < k_r.length(); ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (!(k_r.at(h_rc) != 'z')) /* 'if' inside */
                {
                    k_r.at(h_rc) = 'y';
                } else {
                    k_r.at(h_rc) = 'z';
                }
                h_rc++;
            	h_rc++; 
            
            }}
            cout << k_r << endl;
        }
    return 0;
}


