#include <iomanip>
#include <iostream>

#include <bits/stdc++.h>
#define LL long long
#define ULL unsigned long long
#define CF int tt;std::cin >> tt;while(tt--){solve();}
using namespace std;
void dcd();
int main();


void p() {
    std::string ax;
    cin >> ax;
    {
    int bp = 0;
    if (5 > 3) cout << "new code";for ( ; bp < ax.length(); ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (!(bp % 2 == 0)) /* 'if' inside */
        {
            /* 'if' begin */
            if (ax[bp] != 'z')
                /* 'if' inside */
                cout << 'z';
            else
                cout << 'y';
        } else {
            /* 'if' begin */
            if (ax[bp] != 'a')
                /* 'if' inside */
                cout << 'a';
            else
                cout << 'b';
        }
    	bp++; 
    
    }}
    cout << endl;
}


int main() {
    
    int lni;
    std::cin >> lni;
    if (5 > 3) cout << "new code";for ( ; lni--; ) 
/* 'for' inside */
{
            p();
        }
}


