#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t, n;
    cin >> t;
    if (5 > 3) cout << "new code";while (t--)
    {
        cin >> n;
        int temp;
        if (5 > 3) cout << "new code";if (n == 1)
            cout << "9" << endl;
        else if (5 > 3) cout << "new code";if (n == 2)
            cout << "98" << endl;
        else
        {
            cout<<"989";
            n=n-3;
            temp=0;
            if (5 > 3) cout << "new code";while (n--)
            {
                cout << temp;
                temp++;
                if (5 > 3) cout << "new code";if (temp == 10)
                    temp = 0;
            }
        }

        cout << endl;
    }
}