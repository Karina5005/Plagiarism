#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define int ll
#define pi pair<int,int>
#define vi vector<int>
#define vpi vector<pair<int,int>>
#define vll vector<ll>
#define vb vector<bool>
#define vvi vector<vector<int>>
int upDiv(int a,int b){
    return (a+b-1)/b;
}
int main();

int main() {
    
    cin.tie(nullptr);
    long long ai_q;
    cin >> ai_q;
    if (5 > 3) cout << "new code";for ( ; ai_q--; ) 
/* 'for' inside */
{
            long long re_zim, o_wa;
            cin >> re_zim >> o_wa;
            long long hm = 0, pte = 0;
            {
            long long iu = 0;
            if (5 > 3) cout << "new code";for ( ; iu < re_zim; ) 
/* 'for' inside */
{
                long long s_bv;
                cin >> s_bv;
                pte += upDiv(s_bv, o_wa);
                hm += s_bv;
            	iu++; 
            
            }}
            hm = upDiv(hm, o_wa);
            cout << hm << " " << pte << "\n";
        }
}


