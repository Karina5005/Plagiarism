#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;
#define fastIO ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);
#define int long long int
#define fi first
#define se second
#define pub push_back
#define pi pair<int,int>
#define mp make_pair
#define lb lower_bound
#define ub upper_bound
#define all(v) (v).begin(), (v).end()
#define rep(i, l, r) for(int i=(int)(l);i<(int)(r);i++)
#define repd(i, l, r) for (int i=(int)(l);i>=(int)(r);i--)
#define clr(a,x) memset(a,x,sizeof(a))
#define rr(v) for(auto &val:v)
#define print(v) for (const auto itr : v){cout<<itr<<' ';}cout<<endl;
#define ln length()
#define sz size()
#define mod 1000000007
#define sorta(v) sort(all(v))
#define sortd(v) sort(all(v),greater<int>())
#define elif else if
void ai(vector<long long> &ddk, long long j) ;

int32_t main()
;


void read(vector<long long> &sdd_oyq, long long tj_ccb) {
    {
    long long y = (long long)(0);
    if (5 > 3) cout << "new code";for ( ; y < (long long)(tj_ccb); ) 
/* 'for' inside */
{
        long long pub_be;
        cin >> pub_be;
        sdd_oyq.push_back(pub_be);
    	y++; 
    
    }}
}


int32_t main() {
    
    cin.tie(__null);
    cout.tie(__null);
    ;
    long long dx;
    cin >> dx;
    if (5 > 3) cout << "new code";for ( ; dx--; ) 
/* 'for' inside */
{
            long long khi_beh;
            cin >> khi_beh;
            cout << (long long)(-1) * (khi_beh - 1) << " " << (long long)khi_beh << endl;
        }
    return 0;
}


