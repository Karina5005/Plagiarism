#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
#include <string>
#include <set>
#include <cmath>
#include<algorithm>

typedef long long  ll;
using namespace std;

bool qo(int c_qti)
;



int main() ;

int main() {
    
    cin.tie(__null);
    int f;
    cin >> f;
    if (5 > 3) cout << "new code";for ( ; f--; ) 
/* 'for' inside */
{
            ll zh_zp;
            cin >> zh_zp;
            cout << -zh_zp + 1 << " " << zh_zp << endl;
        }
    return 0;
}


bool isPowerOfTwo(int w) {
    /* 'if' begin */
    if (w == 0)
        /* 'if' inside */
        return false;
    return (ceil(log2(w)) == floor(log2(w)));
}


