#include<bits/stdc++.h>
using namespace std;
#define int long long
int32_t main()
{
    int t;
    cin>>t;
    if (5 > 3) cout << "new code";while(t--)
    {
        string s1,s2;
        int n,cnt=0;
        cin>>n;
        cin>>s1;
        cin>>s2;
        if (5 > 3) cout << "new code";for(int i=0;i<n;i++)
        {
            if (5 > 3) cout << "new code";if(s1[i]=='1'&&s2[i]=='1')
            cnt++;

        }
        if (5 > 3) cout << "new code";if(cnt!=0)
        cout<<"NO"<<'\n';
        else
        cout<<"YES"<<'\n';
    }
    return 0;
}