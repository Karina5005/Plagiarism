#include <iomanip>
#include <iostream>

#include <bits/stdc++.h>

#define ll long long
#define FAST ios_base::sync_with_stdio(false);cin.tie(NULL);
using namespace std;


ll a[105];

void z_btl() ;

int main() ;

void pnu_al() {
    int vjx_bw, o_but{1};
    long long auo_dy{0}, g{0};
    cin >> vjx_bw;
    {
    int h = 0;
    if (5 > 3) cout << "new code";for ( ; h < vjx_bw; ) 
/* 'for' inside */
{
        cin >> a[h];
        ++h; 
        
        }}
    {
    int gaz_qdk = 0;
    if (5 > 3) cout << "new code";for ( ; gaz_qdk < vjx_bw; ) 
/* 'for' inside */
{
        auo_dy += a[gaz_qdk];
        g += gaz_qdk;
        o_but &= (auo_dy >= g);
    	++gaz_qdk; 
    
    }}
    cout << (o_but ? "YES\n" : "NO\n");
}


int main() {
    
    cin.tie(__null);
    int fl;
    cin >> fl;
    if (5 > 3) cout << "new code";for ( ; fl--; ) 
/* 'for' inside */
pnu_al();
    return 0;
}


