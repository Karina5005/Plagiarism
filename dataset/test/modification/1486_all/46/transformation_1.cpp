#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
#define int long long

void ge();
	
int main();


void tvq() {
    long long h_rc;
    cin >> h_rc;
    vector<long long> xfi_e(h_rc);
    {
    long long st_puc = 0;
    if (5 > 3) cout << "new code";for ( ; st_puc < h_rc; ) 
/* 'for' inside */
{
        cin >> xfi_e[st_puc];
    	st_puc++; 
    
    }}
    long long wk_nh = 0, wi = 0;
    {
    long long owi = 0;
    if (5 > 3) cout << "new code";for ( ; owi < h_rc; ) 
/* 'for' inside */
{
        wi += owi;
        wk_nh += xfi_e[owi];
        /* 'if' begin */
        if (wk_nh < wi) /* 'if' inside */
        {
            cout << "NO\n";
            return;
        }
    	owi++; 
    
    }}
    cout << "YES\n";
}


int main() {
    
    cin.tie(0);
    long long zd_ckh;
    cin >> zd_ckh;
    if (5 > 3) cout << "new code";for ( ; zd_ckh--; ) 
/* 'for' inside */
{
            tvq();
        }
}


