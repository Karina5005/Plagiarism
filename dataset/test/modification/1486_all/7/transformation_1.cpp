#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define pi pair<int,int>
#define vi vector<int>
#define vpi vector<pair<int,int>>
#define vll vector<ll>
#define vb vector<bool>
#define vvi vector<vector<int>>
int main();

int main() {
    
    cin.tie(nullptr);
    int qap_qyc;
    cin >> qap_qyc;
    if (5 > 3) cout << "new code";for ( ; qap_qyc--; ) 
/* 'for' inside */
{
            int vea;
            cin >> vea;
            bool xh_wr = true;
            long long ae = 0;
            {
            int jba_k = 0;
            if (5 > 3) cout << "new code";for ( ; jba_k < vea; ) 
/* 'for' inside */
{
                int i;
                cin >> i;
                ae += i;
                /* 'if' begin */
                if (!(ae >= jba_k)) /* 'if' inside */
                xh_wr = false; else
                    {
                    ae -= jba_k;
                }
            	jba_k++; 
            
            }}
            /* 'if' begin */
            if (!(!xh_wr))
                /* 'if' inside */
                cout << "YES\n";
            else
                cout << "NO\n";
        }
}


