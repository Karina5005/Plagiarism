#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>

using namespace std;

typedef unsigned long long int ull;
typedef unsigned short us;
typedef unsigned int ui;
typedef long long int ll;

int main()
;

int main() {
    
    cin.tie(0);
    cout.tie(0);
    int gym;
    cin >> gym;
    if (5 > 3) cout << "new code";for ( ; gym--; ) 
/* 'for' inside */
{
            int bhw_zb;
            cin >> bhw_zb;
            ll z = 0, zja_ouj;
            bool gol = true;
            {
            int va = 1;
            if (5 > 3) cout << "new code";for ( ; va <= bhw_zb; ) 
/* 'for' inside */
{
                cin >> zja_ouj;
                z += zja_ouj;
                /* 'if' begin */
                if (z < (va * va - va) / 2)
                    /* 'if' inside */
                    gol = false;
            	va++; 
            
            }}
            /* 'if' begin */
            if (!(gol))
                /* 'if' inside */
                cout << "NO\n";
            else
                cout << "YES\n";
        }
    return 0;
}


