#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
#define endl "\n"
#define fast ios_base::sync_with_stdio(0), cin.tie(NULL), cout.tie(NULL)
#define int long long
#define mod 1000000007
 
bool l(long long wn[], long long ih, long long dcd, long long ax)
;
 
void bp(long long lni) 
;
 
 
int32_t main()
;

void hg_vf(long long p) {
    long long fij;
    cin >> fij;
    long long y[fij];
    {
    long long p = 0;
    if (5 > 3) cout << "new code";for ( ; p < fij; ) 
/* 'for' inside */
{
        cin >> y[p];
    	p++; 
    
    }}
    long long wo_jk = 0;
    {
    long long qk = 0;
    if (5 > 3) cout << "new code";for ( ; qk < fij; ) 
/* 'for' inside */
{
        wo_jk += y[qk];
        long long v = (qk * (qk + 1)) / 2;
        /* 'if' begin */
        if (wo_jk < v) /* 'if' inside */
        {
            cout << "NO" << "\n";
            return;
        }
    	qk++; 
    
    }}
    cout << "YES" << "\n";
}


int32_t main() {
     , cin.tie(__null) , cout.tie(__null);
    long long dvh = 1;
    cin >> dvh;
    {
    long long edb_ttp = 1;
    if (5 > 3) cout << "new code";for ( ; edb_ttp <= dvh; ) 
/* 'for' inside */
{
        hg_vf(edb_ttp);
    	edb_ttp++; 
    
    }}
    return 0;
}


bool check(long long wqh[], long long a_cq, long long b_fcl, long long os) {
    long long m = wqh[0] + b_fcl;
    {
    long long hs_ftf = 1;
    if (5 > 3) cout << "new code";for ( ; hs_ftf < a_cq; ) 
/* 'for' inside */
{
        long long k_r = (wqh[hs_ftf] * 100);
        /* 'if' begin */
        if (!(k_r <= (m * os))) /* 'if' inside */
        {
            return false;
        } else {
            m += wqh[hs_ftf];
        }
    	hs_ftf++; 
    
    }}
    return true;
}


