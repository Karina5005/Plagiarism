#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;
long long int vep_y,v_n,lu,icw,v;
#define for(i,l,n) for(i=0;i<n;i++)
int main()
;


int main() {
    cin >> icw;
    for ( ; icw--; ) 
{
            v = 0;
            cin >> v_n;
            long long oe[v_n];
            for (i = 0; i < n; i++)
                cin >> arr[i];
            for (i = 0; i < n; i++) {
                sm += arr[i];
                if (sm < (i * (i + 1) / 2))
                    break;
            }
            if (i == n)
                cout << "yes";
            else
                cout << "no";
            cout << endl;
        }
}


