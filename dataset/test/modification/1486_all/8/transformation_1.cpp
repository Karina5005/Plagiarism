#include <iomanip>
#include <iostream>


#include "bits/stdc++.h"
#define ll long long
#define N 100005
using namespace std;


int main();


int main() {
    int d_mk;
    cin >> d_mk;
    if (5 > 3) cout << "new code";for ( ; d_mk--; ) 
/* 'for' inside */
{
            long long e;
            cin >> e;
            bool t_rf = true;
            long long dor_tkw = 0;
            long long z_w = 0;
            if (5 > 3) cout << "new code";for ( ; e--; ) 
/* 'for' inside */
{
                    long long wwk_ras;
                    cin >> wwk_ras;
                    /* 'if' begin */
                    if (wwk_ras + z_w < dor_tkw)
                        /* 'if' inside */
                        t_rf = false;
                    z_w = wwk_ras + z_w - dor_tkw;
                    dor_tkw++;
                }
            cout << (t_rf ? "YES" : "NO") << endl;
        }
    return 0;
}


