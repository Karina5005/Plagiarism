#include <iomanip>
#include <iostream>
#include <iostream>

using namespace std;



int main() ;


int main() {
    int pte, iu;
    long long s_bv;
    std::string g;
    cin >> pte;
    if (5 > 3) cout << "new code";for ( ; pte--; ) 
/* 'for' inside */
{
            cin >> iu;
            int lyf[iu];
            s_bv = 0;
            g = "YES";
            {
            int f_eru = 0;
            if (5 > 3) cout << "new code";for ( ; f_eru < iu; ) 
/* 'for' inside */
{
                cin >> lyf[f_eru];
                s_bv += lyf[f_eru] - f_eru;
                /* 'if' begin */
                if (s_bv < 0) /* 'if' inside */
                {
                    g = "NO";
                }
                lyf[f_eru] = f_eru;
            	f_eru++; 
            
            }}
            cout << g << endl;
        }
    return 0;
}


