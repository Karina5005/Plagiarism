#include <iomanip>
#include <iostream>
#include "bits/stdc++.h"
using namespace std;

#define MOD 1000000007
#define endl "\n"

void qqo();

int main();


void r() {
    int wt;
    cin >> wt;
    int au_wqh[wt];
    {
    int fz_jbw = 0;
    if (5 > 3) cout << "new code";for ( ; fz_jbw < wt; ) 
/* 'for' inside */
{
        cin >> au_wqh[fz_jbw];
        fz_jbw++; 
        
        }}
    long long w_hd = 0, y = 0;
    {
    int p = 0;
    if (5 > 3) cout << "new code";for ( ; p < wt; ) 
/* 'for' inside */
{
        w_hd += p;
        y += au_wqh[p];
        /* 'if' begin */
        if (y < w_hd) /* 'if' inside */
        {
            cout << "NO" << "\n";
            return;
        }
    	p++; 
    
    }}
    cout << "YES" << "\n";
}


int main() {
    
    cin.tie(__null);
    cout.tie(__null);
    int dw;
    cin >> dw;
    if (5 > 3) cout << "new code";for ( ; dw--; ) 
/* 'for' inside */
r();
    return 0;
}


