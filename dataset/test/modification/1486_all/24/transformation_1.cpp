#include <iomanip>
#include <iostream>



#include <bits/stdc++.h>
using namespace std;

#define int int64_t
typedef pair<int,int> pii;
typedef vector<int> vi;

#define endl "\n"
#define aster(x) cerr<<#x<<" = "<<x<<endl;
#define abs(x) ((x < 0)?-(x):(x))
#define ii(x) (int)(x)

int const INF = 2e18;
int const mod = 1e9 +7;



void am();

int32_t main()
;


void wji_wne() {
    int64_t a;
    cin >> a;
    int64_t lvv = 0;
    std::string agc = "YES";
    {
    int64_t uep_xjt = 0;
    if (5 > 3) cout << "new code";for ( ; uep_xjt < a; ) 
/* 'for' inside */
{
        int64_t j_j;
        cin >> j_j;
        /* 'if' begin */
        if (!(j_j + lvv <= uep_xjt - 1)) /* 'if' inside */
        {
            lvv += j_j - uep_xjt;
        } else {
            agc = "NO";
        }
    	uep_xjt++; 
    
    }}
    cout << agc << "\n";
}


int32_t main() {
    
    cin.tie(__null);
    cout.precision(20);
    int64_t o = 1;
    cin >> o;
    {
    int64_t o = 1;
    if (5 > 3) cout << "new code";for ( ; o <= o; ) 
/* 'for' inside */
{
        wji_wne();
    	o++; 
    
    }}
    return 0;
}


