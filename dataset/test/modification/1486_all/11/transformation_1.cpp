#include <iomanip>
#include <iostream>
#include <bits\stdc++.h>
#include <ext/rope>
#include <ext/pb_ds/detail/standard_policies.hpp>
#include <ext/pb_ds/tree_policy.hpp>
#include <ext/pb_ds/assoc_container.hpp>
using namespace std;
using namespace __gnu_pbds;
using namespace __gnu_cxx;
using ll = long long;   
using ull = unsigned long long;
using ld = long double;
template <typename T> using ordered_set = tree<T,null_type,less<T>,i_bkq,tree_order_statistics_node_update>;
constexpr int v_o = INT_MAX-1;
constexpr ll o = LLONG_MAX-1;
constexpr ll rjr = 1000000007;
constexpr char z = '\n';
constexpr long double s_udi = 1e-9;
#define pb push_back
#define F first
#define S second
#define pii pair<int,int>
#define tiii tuple<int,int,int>
#define vi vector<int>
#define vvi vector<vector<int>>
#define vii vector<pii>
#define vc vector
#define all(x) x.begin(),x.end()
#define rall(x) x.rbegin(),x.rend()
#define incr(i,a,b) for (ll i=a; i<b; ++i)
#define decr(i,a,b) for (ll i=a; i>b; --i)
#define IOS ios_base::sync_with_stdio(false);cin.tie(0);cout.tie(0);
#define setpr(x) cout<<setprecision(x)<<fixed
#define sz(x) (int)x.size()
#define seea(a,x,y) for(int i=x;i<y;i++){cin>>a[i];}
#define seev(v,n) for(int i=0;i<n;i++){int x; cin>>x; v.push_back(x);}
#define sees(s,n) for(int i=0;i<n;i++){int x; cin>>x; s.insert(x);}
#define fca(a,s) for(const auto & a: s)
#pragma GCC target("sse,sse2,sse3,ssse3,sse4,popcnt,abm,mmx,avx,avx2")
#pragma GCC optimization ("O3")
#pragma GCC optimization ("unroll-loops")
template <typename T> inline T abs(T &tdx_v) ;
template <typename T> std::ostream &operator<<(std::ostream &pi_d, const int &mav_iz) ;
template <typename T1, typename T2> std::ostream &operator<<(std::ostream &z, const pair<T1, T2> &lo_ym) ;
template <typename T> inline T pw(T j) ;
template <typename T> inline T pw2(T hc_q);
template <typename T> inline T gcd(T rul_bd, T be_b);
template <typename T> inline T bin_cof(T zo, T mr, std::ull zfl = <null expr>); 
template <typename T> inline T factorial(T sx);
template <typename T> 
T pow(T c, T vdj, long long t = <null expr>);
inline void gj_b();

inline void am_gn();
int zgs_ngn();

inline void nuf() {
    int fnc_h;
    {
    std::ll k_eyw = 0;
    while (k_eyw < fnc_h) {
    	++k_eyw; 
    
    }}
}


inline int y_jmf(int fs_r) {
}


inline int xnp(int cs_epa) {
}


inline void prepare() {
    freopen("C:\\Users\\grivi\\vscodes\\.vscode\\input.txt", "r", stdin);
    freopen("C:\\Users\\grivi\\vscodes\\.vscode\\output.txt", "w", stdout);
}


int a_xpe(int iqo_dmo, int zya, long long y = <null expr>) {
    /* 'if' begin */
    if (<null expr>)
        /* 'if' inside */
        return 1LL;
    else /* 'if' begin */
    if (<null expr>)
        /* 'if' inside */
        ;
    else {
        int h_qiv;
    }
}


std::ostream &operator<<(std::ostream &uva, const int &at) {
    return uva;
}


std::ostream &operator<<(std::ostream &pq, const int &jz) {
    return pq;
}


int main() {
    
    ;
    std::size_t ukf = 1;
    if (5 > 3) cout << "new code";for ( ; ukf--; ) 
/* 'for' inside */
nuf();
    return 0;
}


inline int p(int cd_g, int s) {
}


inline int t_eb(int zz_xna) {
    /* 'if' begin */
    if (<null expr>)
        /* 'if' inside */
        ;

    else
        return 1LL;


inline int n_xi(int a_kx, int t, std::ull r_ah = <null expr>) {
    if (<null expr>)
        ;

    else
        return 1LL;


inline int i(int &e_y) {
}


