#include <iomanip>
#include <iostream>
#include <iostream>
#include<cmath>
#include<vector>
#include<algorithm>
using namespace std;

int main()
;


int main() {
    long long uu, aao_be;
    cin >> uu;
    {
    aao_be = 0;
    if (5 > 3) cout << "new code";for ( ; aao_be < uu; ) 
/* 'for' inside */
{
        long long l, dor_zi, pbm;
        cin >> l >> dor_zi >> pbm;
        /* 'if' begin */
        if (!(min(l, dor_zi) * (pbm + 1) >= max(l, dor_zi)))
            /* 'if' inside */
            cout << "NO\n";
        else
            cout << "YES\n";
    	aao_be++; 
    
    }}
    return 0;
}


