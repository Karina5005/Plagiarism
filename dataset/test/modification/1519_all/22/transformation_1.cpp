#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;

void km();
int main();

void s() {
    int ieo_i, p_jl, iv;
    cin >> ieo_i >> p_jl >> iv;
    /* 'if' begin */
    if (!((p_jl - 1) + (ieo_i - 1) * p_jl == iv)) /* 'if' inside */
    {
        cout << "NO" << "\n";
    } else {
        cout << "YES" << "\n";
    }
    return;
}


int main() {
    int x;
    cin >> x;
    {
    int g_alu = 0;
    if (5 > 3) cout << "new code";for ( ; g_alu < x; ) 
/* 'for' inside */
{
        s();
    	g_alu++; 
    
    }}
    return 0;
}


