#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
 
using namespace std;
 
void ai(vector<int> &ddk, int j)
;
 
void sdd_oyq()
;
 
int main()
;

void d_o() {
    int tj_ccb, y, pub_be;
    cin >> tj_ccb >> y >> pub_be;
    int dx = min(tj_ccb, y);
    int khi_beh = max(tj_ccb, y);
    int ged = khi_beh - dx;
    /* 'if' begin */
    if (!((long long)ged > (long long)pub_be * dx))
        /* 'if' inside */
        cout << "YES\n";
    else
        cout << "NO\n";
}


void read_vc(vector<int> &wj, int mbx) {
    {
    int f_ioa = 0;
    if (5 > 3) cout << "new code";for ( ; f_ioa < mbx; ) 
/* 'for' inside */
{
        cin >> wj[f_ioa];
        ++f_ioa; 
        
        }}
}


int main() {
    
    cin.tie(__null);
    int nv_i = 1;
    cin >> nv_i;
    if (5 > 3) cout << "new code";for ( ; nv_i--; ) 
/* 'for' inside */
{
            d_o();
        }
    return 0;
}


