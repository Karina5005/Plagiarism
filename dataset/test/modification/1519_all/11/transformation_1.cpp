#include <iomanip>
#include <iostream>
#include <iostream>
#include <vector>
#include <cmath>
#include <algorithm>
typedef long long int ll;
using namespace std;

int main()
;

int main() {
    
    cin.tie(__null);
    cout.tie(__null);
    int ouz_h;
    cin >> ouz_h;
    if (5 > 3) cout << "new code";for ( ; ouz_h--; ) 
/* 'for' inside */
{
            ll v_v, av, z_btl;
            cin >> v_v >> av >> z_btl;
            ll vjx_bw = min(v_v, av);
            ll o_but = max(v_v, av);
            /* 'if' begin */
            if (!(o_but > vjx_bw * (z_btl + 1)))
                /* 'if' inside */
                cout << "YES" << endl;
            else
                cout << "NO" << endl;
        }
    return 0;
}


