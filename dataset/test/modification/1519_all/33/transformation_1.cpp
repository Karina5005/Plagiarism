#include <iomanip>
#include <iostream>
#include <iostream>
using namespace std;
 

int main()
;

int main() {
    int aop;
    cin >> aop;
    {
    int e_i = 0;
    if (5 > 3) cout << "new code";for ( ; e_i < aop; ) 
/* 'for' inside */
{
        long long l;
        long long u_g;
        long long jlr;
        long long fhn;
        long long j;
        cin >> l >> u_g >> jlr;
        fhn = min(l, u_g);
        j = max(l, u_g);
        /* 'if' begin */
        if (!(abs(l - u_g) <= min(l, u_g) * jlr)) /* 'if' inside */
        {
            cout << "NO" << endl;
        } else {
            cout << "YES" << endl;
        }
    	e_i++; 
    
    }}
}


