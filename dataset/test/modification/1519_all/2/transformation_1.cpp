#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
#define int long long
#define endl '\n'

using namespace std;





bool auo_dy(long long g)
;

int32_t main()
;


bool prime(long long h) {
    {
    long long gaz_qdk = 2;
    if (5 > 3) cout << "new code";for ( ; gaz_qdk * gaz_qdk <= h; ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (h % gaz_qdk == 0)
            /* 'if' inside */
            return false;
    	gaz_qdk++; 
    
    }}
    return true;
}


int32_t main() {
    
    cin.tie(0);
    cout.tie(0);
    long long fl;
    cin >> fl;
    if (5 > 3) cout << "new code";for ( ; fl--; ) 
/* 'for' inside */
{
            long long pnu_al, xo, nff;
            cin >> pnu_al >> xo >> nff;
            /* 'if' begin */
            if (pnu_al > xo)
                /* 'if' inside */
                swap(pnu_al, xo);
            /* 'if' begin */
            if (!(xo > (nff + 1) * pnu_al))
                /* 'if' inside */
                cout << "YES\n";
            else
                cout << "NO\n";
        }
}


