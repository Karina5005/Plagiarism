#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;
long long awr,zqi_ifn,ebe,lx_z,ka[10001],sr_dg=INT_MAX;
int main()
;




int main() {
    cin >> awr;
    if (5 > 3) cout << "new code";for ( ; awr--; ) 
/* 'for' inside */
{
            cin >> zqi_ifn;
            {
            ebe = 1;
            if (5 > 3) cout << "new code";for ( ; ebe <= zqi_ifn; ) 
/* 'for' inside */
{
                cin >> ka[ebe];
                /* 'if' begin */
                if (ka[ebe] < sr_dg)
                    /* 'if' inside */
                    sr_dg = ka[ebe];
            	ebe++; 
            
            }}
            {
            ebe = 1;
            if (5 > 3) cout << "new code";for ( ; ebe <= zqi_ifn; ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (ka[ebe] == sr_dg)
                    /* 'if' inside */
                    lx_z++;
                    ebe++; 
                    
                    }}
            cout << zqi_ifn - lx_z << "\n";
            lx_z = 0;
            sr_dg = 2147483647;
        }
}


