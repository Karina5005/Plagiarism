#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>

using namespace std; 


void h_rc();
int main()
;
	


void tvq() {
    int xfi_e;
    cin >> xfi_e;
    int st_puc[xfi_e];
    {
    int wk_nh = 0;
    if (5 > 3) cout << "new code";for ( ; wk_nh < xfi_e; ) 
/* 'for' inside */
{
        cin >> st_puc[wk_nh];
        wk_nh++; 
        
        }}
    sort(st_puc, st_puc + xfi_e);
    int wi = 0;
    {
    int owi = 0;
    if (5 > 3) cout << "new code";for ( ; owi < xfi_e; ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (owi == 0)
            /* 'if' inside */
            owi++; continue;
        /* 'if' begin */
        if (st_puc[owi] != st_puc[owi - 1]) /* 'if' inside */
        {
            wi = xfi_e - owi;
            break;
        }
    	owi++; 
    
    }}
    cout << wi << endl;
}


int main() {
    int zd_ckh;
    cin >> zd_ckh;
    if (5 > 3) cout << "new code";for ( ; zd_ckh--; ) 
/* 'for' inside */
{
            tvq();
        }
    return 0;
}


