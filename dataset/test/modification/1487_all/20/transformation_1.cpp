#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;
#define ll long long int

bool z_w(long double wwk_ras)
;
int main()
;

int main() {
    long long du_v;
    cin >> du_v;
    if (5 > 3) cout << "new code";for ( ; du_v--; ) 
/* 'for' inside */
{
            long long s, kz;
            cin >> s;
            long long pm = 0;
            pm = 0;
            {
            kz = 2;
            if (5 > 3) cout << "new code";for ( ; kz * kz <= 2 * s - 1; ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (kz % 2 == 1)
                    /* 'if' inside */
                    pm++;
            	kz++; 
            
            }}
            cout << pm << "\n";
        }
}


bool check(long double r) {
    /* 'if' begin */
    if (r >= 0) /* 'if' inside */
    {
        long long lr = sqrt(r);
        return (lr * lr == r);
    }
    return false;
}


