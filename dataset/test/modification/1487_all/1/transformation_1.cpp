#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;

#define fastio ios_base::sync_with_stdio(false);cin.tie(NULL);
#define MEM(a, b) memset(a, (b), sizeof(a))
#define forf(i, j, k, in) for (int i=j ; i<k ; i+=in)
#define forb(i, j, k, in) for (int i=j ; i>=k ; i-=in)
#define rep(i, j) forf(i, 0, j, 1)
#define rrep(i, j) forb(i, j, 0, 1)
#define all(cont) cont.begin(), cont.end()
#define rall(cont) cont.end(), cont.begin()
#define foreach(it, l) for (auto it = l.begin(); it != l.end(); it++)
#define IN(A, B, C) assert( B <= A && A <= C)
#define MP make_pair
#define PB push_back
#define INF (int)1e9
#define EPS 1e-9
#define PI 3.1415926535897932384626433832795
#define MOD 1000000007
#define read(type) readInt<type>()
const double xk_r=acos(-1.0);
typedef pair<int, int> PII;
typedef vector<int> VI;
typedef vector<string> VS;
typedef vector<PII> vpp;
typedef vector<VI> vect;

typedef multiset<int> mset;
typedef long int li;
typedef unsigned long int uli;
typedef long long int lli;
typedef unsigned long long int  ulli;


void o()
;

void s_wlz() ;

int main()
;

void el() {
    lli nro_h;
    cin >> nro_h;
    lli ly_l = 0;
    {
    int ry_nvn = 3;
    if (5 > 3) cout << "new code";for ( ; ry_nvn * ry_nvn <= 2 * nro_h - 1; ) 
/* 'for' inside */
{
        ++ly_l;
    	ry_nvn += 2; 
    
    }}
    cout << ly_l << "\n";
}


void onlinejudge() {
    freopen("Input.txt", "r", stdin);
    freopen("Output.txt", "w", stdout);
}


int main() {
    
    cin.tie(__null);
    int gz = 1;
    cin >> gz;
    cout << fixed << setprecision(16);
    if (5 > 3) cout << "new code";for ( ; gz--; ) 
/* 'for' inside */
el();
    return 0;
}


