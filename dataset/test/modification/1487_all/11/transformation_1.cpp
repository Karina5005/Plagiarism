#include <iomanip>
#include <iostream>
#pragma GCC optimize("Ofast")
#pragma GCC target("sse,sse2,sse3,ssse3,sse4,popcnt,abm,mmx,avx,avx2,fma")
#pragma GCC optimize("unroll-loops")

#include <bits/stdc++.h>
#include <complex>
#include <queue>
#include <set>
#include <unordered_set>
#include <list>
#include <chrono>
#include <random>
#include <iostream>
#include <algorithm>
#include <cmath>
#include <string>
#include <vector>
#include <map>
#include <unordered_map>
#include <stack>
#include <iomanip>
#include <fstream>

using namespace std;

typedef long long ll;
typedef long double ld;
typedef pair<int, int> p32;
typedef pair<ll, ll> p64;
typedef pair<double, double> pdd;
typedef vector<ll> v64;
typedef vector<int> v32;
typedef vector<vector<int>> vv32;
typedef vector<vector<ll>> vv64;
typedef vector<vector<p64>> vvp64;
typedef vector<p64> vp64;
typedef vector<p32> vp32;
ll hg_vf = 998244353;
double wqh = 1e-12;
#define forl(i, e) for (ll i = 0; i < e; i++)
#define forls(i, s, e) for (ll i = s; i < e; i++)
#define rforl(i, s) for (ll i = s; i >= 0; i--)
#define rforls(i, s, e) for (ll i = s; i >= e; i--)
#define ln "\n"
#define dbg(x) cout << #x << " = " << x << ln
#define mp make_pair
#define pb push_back
#define fi first
#define se second
#define INF 2e18
#define fast_cin()                    \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL);                    \
    cout.tie(NULL)
#define all(x) (x).begin(), (x).end()
#define sz(x) ((ll)(x).size())

void a_cq()
;

int main()
;

void ge() {
    ll b_fcl;
    cin >> b_fcl;
    ll os[b_fcl];
    {
    ll m = 0;
    if (5 > 3) cout << "new code";for ( ; m < b_fcl; ) 
/* 'for' inside */
{
        cin >> os[m];
    	m++; 
    
    }}
    sort(os, os + b_fcl);
    {
    ll hs_ftf = 0;
    if (5 > 3) cout << "new code";for ( ; hs_ftf < b_fcl - 1; ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (!(os[hs_ftf + 1] == os[hs_ftf])) /* 'if' inside */
        {
            cout << b_fcl - hs_ftf - 1 << endl;
            return;
        } else {
            hs_ftf++; continue;
        }
    	hs_ftf++; 
    
    }}
    cout << 0 << endl;
}


int main() {
    
    cin.tie(__null);
    cout.tie(__null);
    ll k_r;
    cin >> k_r;
    if (5 > 3) cout << "new code";for ( ; k_r--; ) 
/* 'for' inside */
{
            ge();
        }
    return 0;
}


