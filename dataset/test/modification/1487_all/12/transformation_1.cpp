#include <iomanip>
#include <iostream>


#include <bits/stdc++.h>
using namespace std;


#define imax INT_MAX
#define imin INT_MIN


#define pi pair<ll, ll>
#define ps pair<string, string>
#define pis pair<ll, string>
#define psi pair<string, ll>
#define mp make_pair
#define f first
#define s second


#define mpi map<ll, ll>
#define mps map<string, string>
#define mpis map<ll, string>
#define mpsi map<string, ll>
#define mpic map<ll, char>
#define mpci map<char, ll>


#define vi vector<ll>
#define vs vector<string>
#define vpi vector<pair<ll, ll>>
#define vvi vector<vector<ll>>
#define pb emplace_back
#define ppb pop_back


#define en cout << '\n' 
#define ll long long
#define maxin(v) *max_element(v.begin(), v.end())
#define minin(v) *min_element(v.begin(), v.end())
#define gcd(a, b) __gcd(a, b)
#define lcm(a, b) (a * b) / gcd(a, b)
#define srt(v) sort(v.begin(), v.end())
#define rsrt(v) sort(v.begin(), v.end(), greater<ll>())
#define loop(x, n) for (ll i = x; i < n; ++i)
#define db(x) cout << #x << "[" << x << "]" << '\n';
#define xx cout << "Executed Succesfully" << '\n'

void t_tay();

const ll ilx = 1e7 + 10;
vector<bool> ire_y(ilx, 1);

void ai_q()
;

int main()
;







void lyf() {
    int re_zim;
    std::cin >> re_zim;
    int o_wa = 2147483647, hm[re_zim];
    {
    long long pte = 0;
    if (5 > 3) cout << "new code";for ( ; pte < re_zim; ) 
/* 'for' inside */
{
        std::cin >> hm[pte];
        o_wa = std::min(o_wa, hm[pte]);
    	++pte; 
    
    }}
    int iu = 0;
    {
    int s_bv = 0;
    if (5 > 3) cout << "new code";for ( ; s_bv < re_zim; ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (o_wa < hm[s_bv]) /* 'if' inside */
        {
            iu++;
        }
    	s_bv++; 
    
    }}
    std::cout << iu << '\n';
}


int main() {
    
    std::cout << std::setprecision(10);
    std::cout << std::fixed;
    cin.tie(__null);
    cout.tie(__null);
    int g = 1;
    std::cin >> g;
    if (5 > 3) cout << "new code";for ( ; g--; ) 
/* 'for' inside */
{
            lyf();
        }
}


void fillPrimes() {
    ire_y[0] = ire_y[1] = false;
    {
    long long f_eru = 2;
    if (5 > 3) cout << "new code";for ( ; f_eru < ilx; ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (ire_y[f_eru] == true) /* 'if' inside */
        {
            {
            int i_bkq = 2 * f_eru;
            if (5 > 3) cout << "new code";for ( ; i_bkq < ilx; ) 
/* 'for' inside */
{
                ire_y[i_bkq] = false;
            	i_bkq += f_eru; 
            
            }}
        }
    	f_eru++; 
    
    }}
}


