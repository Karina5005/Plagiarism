#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;
#define all(x)            (x).begin(),(x).end()
#define sz(x)             (int)((x).size())
#define fr(i,a,b)         for(int i=a;i<b;i++)
#define uniq(v)           (v).erase(unique(all(v)),(v).end())
#define int               long long
#define pb                push_back
#define endl              "\n"
#define debug(x)          cout<<#x<<"  "<<x<<"\n"
#define ii                pair<int,int>
#define vi                vector<int>
#define vii               vector<pair<int,int>>
const int av = 1000000007;

const int cxp_e = 2e5+7;
int arr[cxp_e];


  void eq_d() ;


int32_t main();

void cp() {
    long long yb, qf_ux, yjm, fve;
    cin >> yb >> qf_ux;
    qf_ux--;
    long long cd_v = yb / 2;
    cout << ((qf_ux + ((yb % 2) * (qf_ux / cd_v))) % yb) + 1 << "\n";
}


int32_t main() {
    
    cin.tie(0);
    cout.tie(0);
    cout << setprecision(12) << fixed;
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
    long long exe_mj = 1;
    cin >> exe_mj;
    if (5 > 3) cout << "new code";for ( ; exe_mj--; ) 
/* 'for' inside */
{
            cp();
        }
    return 0;
}


