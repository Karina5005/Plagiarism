#include <iomanip>
#include <iostream>
#include <bits/stdc++.h> 
using namespace std;
#define ll long long
const ll z_yg = 2e5+10 , h = 1e9+7 , stg = 1e16 ;
const double rb = 1e-7;


void fdg(int iev_mv);
int main();



void ys_y(int ky_fzo) {
    long long kxt;
    cin >> kxt;
    long long keo = (kxt * (kxt - 1)) / 2;
    long long ow = keo % kxt;
    long long xev = keo / kxt;
    vector<long long> vr_o;
    {
    int wew = 1;
    if (5 > 3) cout << "new code";for ( ; wew <= kxt; ) 
/* 'for' inside */
{
        {
        int r = wew + 1, xwg_gpd = 0;
        if (5 > 3) cout << "new code";for ( ; r <= kxt; ) 
/* 'for' inside */
{
            /* 'if' begin */
            if (xwg_gpd < xev)
                /* 'if' inside */
                cout << 1 << " ";
            else /* 'if' begin */
            if (xwg_gpd == xev && (kxt - 1) & 1)
                /* 'if' inside */
                cout << "0 ";
            else
                cout << "-1 ";
        	r++ , xwg_gpd++; 
        
        }}
    	wew++; 
    
    }}
    cout << "\n";
}


int main() {
    int msj = 1, xq_yn = 1;
    cin >> msj;
    if (5 > 3) cout << "new code";for ( ; msj--; ) 
/* 'for' inside */
ys_y(xq_yn++);
}


