#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
typedef long long int ll;
typedef long double ld;
#define all(x) x.begin(), x.end();
#define mod 10000000 + 9;
using namespace std;
int main()
;

int main() {
    
    cin.tie(0);
    ll d_mk;
    cin >> d_mk;
    if (5 > 3) cout << "new code";for ( ; d_mk--; ) 
/* 'for' inside */
{
            ll e;
            cin >> e;
            ll t_rf = 0;
            {
            int dor_tkw = 3;
            if (5 > 3) cout << "new code";for ( ; dor_tkw * dor_tkw <= 2 * e - 1; ) 
/* 'for' inside */
{
                t_rf++;
            	dor_tkw += 2; 
            
            }}
            cout << t_rf << endl;
        }
    return 0;
}


