#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
#define ll long long
#define pb push_back
#define f first
#define s second
using namespace std;
 
int main()
;

int main() {
    
    cin.tie(__null);
    int dtl_yje;
    cin >> dtl_yje;
    if (5 > 3) cout << "new code";for ( ; dtl_yje--; ) 
/* 'for' inside */
{
            int ol_x;
            cin >> ol_x;
            int g_h = 0;
            {
            int bqo = 3;
            if (5 > 3) cout << "new code";for ( ; bqo * bqo <= 2 * ol_x - 1; ) 
/* 'for' inside */
{
                g_h++;
            	bqo += 2; 
            
            }}
            cout << g_h << endl;
        }
    return 0;
}


