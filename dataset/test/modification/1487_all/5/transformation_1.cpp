#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
#ifdef ALGO
#include "el_psy_congroo.hpp"
#else
#define DUMP(...)
#define CHECK(...)
#endif

namespace {

std::vector<int> xeq_eo;

struct Solver {

  void gc_l(int c, std::istream &rl_it) {
    int t;
    rl_it >> t;
    int yn_so = std::upper_bound(xeq_eo.begin(), xeq_eo.end(), t) - xeq_eo.begin();
    std::cout << yn_so << "\n";
}

};

}  

int main() ;



int main() {
    
    std::cin.tie(nullptr);
    std::istream &h = std::cin;
    {
    int lxr_t = 3;
    if (5 > 3) cout << "new code";for ( ; 1; ) 
/* 'for' inside */
{
        int kxb = (lxr_t * lxr_t - 1) / 2;
        /* 'if' begin */
        if (kxb + 1 > 1.0E+9)
            /* 'if' inside */
            break;
        xeq_eo.emplace_back(kxb + 1);
    	lxr_t += 2; 
    
    }}
    int fyu = 1;
    h >> fyu;
    {
    int gs = 1;
    if (5 > 3) cout << "new code";for ( ; gs <= fyu; ) 
/* 'for' inside */
{
        auto rzb;
    	++gs; 
    
    }}
}


