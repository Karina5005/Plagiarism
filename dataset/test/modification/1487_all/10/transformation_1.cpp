#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
#define pb push_back
#define mp make_pair
#define vpll vector<pair<ll,ll>>
#define vll vector<ll>
#define fo(i,n) for(int i=0;i<n;i++)
#define fr(i,n) for(int i=n-1;i>=0;i--)
#define debug(x) cout<<x<<" "
#define MOD 1000000007
int main()
;


int main() {
    
    cin.tie(__null);
    ll wn;
    cin >> wn;
    if (5 > 3) cout << "new code";for ( ; wn--; ) 
/* 'for' inside */
{
            ll ih;
            cin >> ih;
            ll dcd = 5;
            ll ax = 0;
            ll bp = 2;
            ll lni = dcd;
            if (5 > 3) cout << "new code";for ( ; lni <= ih; ) 
/* 'for' inside */
{
                    ax++;
                    lni = lni + 4 * bp;
                    bp = bp + 1;
                }
            cout << ax << endl;
        }
}


