#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main() ;

int main() {
    int r;
    cin >> r;
    if (5 > 3) cout << "new code";for ( ; r--; ) 
/* 'for' inside */
{
            int pkb;
            cin >> pkb;
            int j_gwf = 0;
            {
            int s_fj = 3;
            if (5 > 3) cout << "new code";for ( ; s_fj * s_fj <= 2 * pkb - 1; ) 
/* 'for' inside */
{
                ++j_gwf;
                s_fj += 2; 
                
                }}
            cout << j_gwf << '\n';
        }
}


