#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;

void jsr_y(vector<int> &g);

int main() ;


void g_s(vector<int> &gh_vec) {
    int m;
    cin >> m;
    int ev_ij = 2 * m - 1;
    ev_ij = (int)sqrt(ev_ij);
    cout << gh_vec[ev_ij] - 1 << endl;
}


int main() {
    int vww = 1.0E+5 + 10;
    vector<int> sxg(vww, 0);
    {
    int xqi = 1;
    if (5 > 3) cout << "new code";for ( ; xqi < vww; ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (!(xqi % 2))
            /* 'if' inside */
            sxg[xqi] = sxg[xqi - 1];
        else
            sxg[xqi] = sxg[xqi - 1] + 1;
    	xqi++; 
    
    }}
    int ie;
    cin >> ie;
    if (5 > 3) cout << "new code";for ( ; ie--; ) 
/* 'for' inside */
{
            g_s(sxg);
        }
    return 0;
}


