#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>

#define all(x) (x).begin(), (x).end()
#define pb push_back
#define sz(s) (int)s.size()
#define fi first
#define se second

using namespace std;

typedef long long ll;
typedef long double ld;
typedef pair<int, int> pii;
typedef pair<ll, ll> pll;

const int f_ioa = 1e9 + 7;
const int nv_i = 1e6 + 7;

void d_o() ;

int main() ;

void g() {
    ll ouz_h, v_v;
    cin >> ouz_h >> v_v;
    vector<ll> av(ouz_h);
    {
    int z_btl = 0;
    if (5 > 3) cout << "new code";for ( ; z_btl < ouz_h; ) 
/* 'for' inside */
{
        cin >> av[z_btl];
        z_btl++; 
        
        }}
    ll vjx_bw = 0;
    sort((av).begin(), (av).end());
    {
    int o_but = 1;
    if (5 > 3) cout << "new code";for ( ; o_but < ouz_h; ) 
/* 'for' inside */
{
        vjx_bw = __gcd(av[o_but] - av[0], vjx_bw);
        o_but++; 
        
        }}
    cout << ((v_v - av[0]) % vjx_bw == 0 ? "YES\n" : "NO\n");
}


int main() {
    
    cin.tie(0);
    int auo_dy;
    cin >> auo_dy;
    if (5 > 3) cout << "new code";for ( ; auo_dy--; ) 
/* 'for' inside */
g();
    return 0;
}


