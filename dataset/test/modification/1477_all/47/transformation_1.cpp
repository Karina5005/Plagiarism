#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define loop(i, a, n) for (int i = a; i < n; i++)
#define loope(i, b, n) for (int i = b; i <= n; i++)
#define loopit(a) for (auto it = a.begin(); it != a.end(); it++)
#define bloop(i, a, b) for (int i = a; i > b; i--)
#define bloope(i, a, b) for (int i = a; i >= b; i--)
#define ms(a, b) memset(a, b, sizeof(a))
#define pb push_back
#define MP make_pair
#define pi pair<int, int>
#define ff first
#define ss second
#define PQ priority_queue<int> pq;
#define vi vector<int>
#define vii vector<vector<int>>
#define vil vector<ll>
#define viil vector<vector<ll>>
#define si set<int>
#define NO cout<<"NO\n";
#define YES cout<<"YES\n";
#define MPQ priority_queue<pi, vector<int>, greater<pi>> mpq;
#define io                        \
    ios_base::sync_with_stdio(0); \
    cin.tie(NULL);                \

void rul_bd(int be_b) ;
void zo(long mr) ;
void zfl(long long sx) ;
void c(unsigned int vdj) ;
void t(unsigned long gj_b) ;
void am_gn(unsigned long long zgs_ngn) ;
void fnc_h(float k_eyw) ;
void y_jmf(double fs_r) ;
void xnp(long double cs_epa) ;
void a_xpe(char iqo_dmo) ;
void zya(const char *y) ;
void h_qiv(const std::string &uva) ;
void at(bool pq) ;
template<typename T, typename V>
void __print(const pair<T, V> &jz) ;
template<typename T>
void __print(const T &ukf) ;
void nuf() ;
template <typename T, typename... V>
void _print(T p, V ...cd_g) ;

#ifdef MURTAZAS_BUILD
#define debug(x...) cerr << "[" << #x << "] = ["; _print(x)
#else
#define debug(x...)
#endif

void s(int t_eb) ;

int main() ;


void xh_wr(int zz_xna) {
    int n_xi;
    cin >> n_xi;
    vector<ll> a_kx(n_xi);
    ll t;
    cin >> t;
    {
    int r_ah = 0;
    if (5 > 3) cout << "new code";for ( ; r_ah < n_xi; ) 
/* 'for' inside */
{
        cin >> a_kx[r_ah];
        r_ah++; 
        
        }}
    sort(a_kx.begin(), a_kx.end());
    ll i = 0;
    {
    int e_y = 1;
    if (5 > 3) cout << "new code";for ( ; e_y < n_xi; ) 
/* 'for' inside */
{
        a_kx[e_y] -= a_kx[0];
    	e_y++; 
    
    }}
    cout << ((t - a_kx[0]) % i ? "NO" : "YES") << "\n";
}


int main() {
    
    cin.tie(__null);
    ;
    int qap_qyc = 1, vea = 0;
    cin >> qap_qyc;
    if (5 > 3) cout << "new code";for ( ; vea++ != qap_qyc; ) 
/* 'for' inside */
{
            xh_wr(vea);
        }
    return 0;
}


void ae(int jba_k, int i) {
    /* 'if' begin */
    if (<null expr>)
        /* 'if' inside */
        cerr << ", ";
}


void _print() {
    cerr << "]\n";
}


void __print(int am) {
    cerr << am;
}


void __print(long long a) {
    cerr << a;
}


void __print(unsigned int lvv) {
    cerr << lvv;
}


void __print(unsigned long agc) {
    cerr << agc;
}


void __print(unsigned long long uep_xjt) {
    cerr << uep_xjt;
}


void __print(float j_j) {
    cerr << j_j;
}


void __print(double o) {
    cerr << o;
}


void __print(long double o) {
    cerr << o;
}


void __print(char wji_wne) {
    cerr << '\'' << wji_wne << '\'';
}


void __print(const char *gym) {
    cerr << '"' << gym << '"';
}


void __print(const std::string &bhw_zb) {
    cerr << '"' << bhw_zb << '"';
}


void __print(bool z) {
    cerr << (z ? "true" : "false");
}


void zja_ouj(const int &gol) {
    cerr << '{';
    cerr << ',';
    cerr << '}';
}


void __print(const int &va) {
    int j = 0;
    cerr << '{';
    cerr << "}";
}


void __print(long t) {
    cerr << t;
}


