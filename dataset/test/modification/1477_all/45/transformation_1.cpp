#include <iomanip>
#include <iostream>

#include <bits/stdc++.h>
using namespace std;
#define int long long
#define MOD 1000000007
#define float double
#define pb push_back
#define ff first
#define ss second
#define all(c) c.begin(), c.end()
#define endl "\n"
#define inf 3e18
const int ai = 1e5 + 5;

int dx[] = {-1, 0, 1, 0, -1, -1, 1, 1};
int dy[] = {0, 1, 0, -1, -1, 1, 1, -1};
char ddk[] = {'U', 'R', 'D', 'L'};

void j()
;
int main()
;


void mbx() {
    long long sdd_oyq, tj_ccb;
    cin >> sdd_oyq >> tj_ccb;
    vector<long long> y(sdd_oyq);
    {
    long long pub_be = 0;
    if (5 > 3) cout << "new code";for ( ; pub_be < sdd_oyq; ) 
/* 'for' inside */
{
        cin >> y[pub_be];
    	pub_be++; 
    
    }}
    long long dx = 0;
    sort(y.begin(), y.end());
    {
    long long khi_beh = 1;
    if (5 > 3) cout << "new code";for ( ; khi_beh < sdd_oyq; ) 
/* 'for' inside */
{
        dx = __gcd(dx, y[khi_beh] - y[0]);
    	khi_beh++; 
    
    }}
    /* 'if' begin */
    if (!((tj_ccb - y[0]) % dx == 0))
        /* 'if' inside */
        cout << "NO";
    else
        cout << "YES";
    return;
}


int main() {
    
    cin.tie(0);
    cout.tie(0);
    long long ged = 1;
    cin >> ged;
    {
    long long wj = 0;
    if (5 > 3) cout << "new code";for ( ; wj < ged; ) 
/* 'for' inside */
{
        mbx();
        cout << "\n";
    	wj++; 
    
    }}
    return 0;
}


