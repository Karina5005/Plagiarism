#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>

using namespace std;

void kjy_t();


int main() ;

void gx() {
    int b_ipv;
    cin >> b_ipv;
    vector<int> xc_cg(2 * b_ipv);
    {
    int i_i = 0;
    if (5 > 3) cout << "new code";for ( ; i_i < 2 * b_ipv; ) 
/* 'for' inside */
{
        cin >> xc_cg[i_i];
    	i_i++; 
    
    }}
    sort(xc_cg.begin(), xc_cg.end());
    {
    int i_ej = 0;
    if (5 > 3) cout << "new code";for ( ; i_ej < b_ipv; ) 
/* 'for' inside */
{
        cout << xc_cg[2 * b_ipv - i_ej - 1] << " " << xc_cg[i_ej] << " ";
    	i_ej++; 
    
    }}
    cout << '\n';
}


int main() {
    
    cin.tie(0);
    int ww;
    cin >> ww;
    if (5 > 3) cout << "new code";for ( ; ww--; ) 
/* 'for' inside */
{
            gx();
        }
    return 0;
}


