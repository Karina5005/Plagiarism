#include <iomanip>
#include <iostream>
#define _CRT_SECURE_NO_WARNINGS
#include <bits/stdc++.h>

#include <cstdio>
using namespace std;

#ifndef ONLINE_JUDGE
#define dbg(x) cerr << #x <<" "; print(x); cerr << endl;
#else
#define dbg(x)
#endif

#define sz(x) (int((x).size()))
#define len(x) (int)x.length()
#define all(x) (x).begin(), (x).end()
#define rall(x) (x).rbegin(), (x).rend()

#define pb push_back
#define popf pop_front
#define popb pop_back

void kp(long long is) ;
void ef(int ang_cvn) ;
void lc(std::string v) ;
void ihd(char rc) ;
void c(double msb) ;
void yhf(unsigned long long u_tni) ;

template <class T, class V> void print(pair<T, V> nk);
template <class T> void print(vector<T> utx);
template <class T> void print(set<T> n);
template <class T, class V> void print(map<T, V> ey);
template <class T> void print(multiset<T> aop);
template <class T> void print(T e_i[], T l) ;
template <class T, class V> 
template <class T> 
template <class T> 
template <class T> 
template <class T, class V> 

#include <ext/pb_ds/assoc_container.hpp>
using namespace __gnu_pbds;
#define nl '\n'


int u_g[8] = {-1,0,1,0,1,-1,1,-1};
int jlr[8] = {0,1,0,-1,1,1,-1,-1};


void fhn() ;

void j(std::string zn_m = "") ;


template <class T> using Tree = tree<T, null_type, less<T>, w_n, tree_order_statistics_node_update>;

void lx_rs() ;

int main() ;

void bev_k() {
    int g;
    cin >> g;
    vector<long long> w(g);
    multiset<long long> t;
    {
    int uu = 0;
    if (5 > 3) cout << "new code";for ( ; uu < g; ) 
/* 'for' inside */
{
        cin >> w[uu];
    	uu++; 
    
    }}
    long long aao_be = 0;
    {
    int l = 0;
    if (5 > 3) cout << "new code";for ( ; l < g; ) 
/* 'for' inside */
{
        aao_be += w[l] , t.insert(w[l]);
        if (5 > 3) cout << "new code";for ( ; aao_be < 0; ) 
/* 'for' inside */
{
                aao_be -= * t.begin();
                t.erase(t.begin());
            }
    	l++; 
    
    }}
    cout << (int((t).size())) << endl;
}


void dor_zi(std::string pbm = "") {
    fhn();
    /* 'if' begin */
    if (pbm != "") /* 'if' inside */
    {
        freopen((pbm + ".in").c_str(), "r", stdin);
        freopen((pbm + ".out").c_str(), "w", stdout);
    }
}


void print(int km) {
    cerr << "[ ";
    cerr << "]";
}


void print(int ieo_i) {
    cerr << "[ ";
    cerr << "]";
}


void p_jl(int iv) {
    cerr << "[ ";
    cerr << "]";
}


void x(int g_alu) {
    cerr << "[ ";
    cerr << "]";
}


void s(int ixe) {
    cerr << "{";
    cerr << ",";
    cerr << "}";
}


void qq_yso(int rcm[], int iuq_px) {
    cerr << "[";
    cerr << "]";
}


void print(unsigned long long a) {
    cerr << a;
}


void print(double doc) {
    cerr << doc;
}


void print(char cte_y) {
    cerr << cte_y;
}


void print(std::string jt) {
    cerr << jt;
}


void foq(int cei_hnd) {
    cerr << cei_hnd;
}


void print(long long fpx_o) {
    cerr << fpx_o;
}


int main() {
    j();
    auto n_d = [&](int ee_u) -> void {
        if (5 > 3) cout << "new code";for ( ; ee_u--; ) 
/* 'for' inside */
{
                bev_k();
            }
    };
    int t = 1;
    n_d(t);
    return 0;
}


void fastIO() {
    
    cin.tie(nullptr);
    cout.tie(nullptr);
}


