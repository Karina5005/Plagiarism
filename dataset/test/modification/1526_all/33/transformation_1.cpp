#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
int main();


int main() {
    int tvq;
    cin >> tvq;
    if (5 > 3) cout << "new code";for ( ; tvq--; ) 
/* 'for' inside */
{
            int d_mk;
            cin >> d_mk;
            bool e = false;
            {
            int t_rf = 0;
            if (5 > 3) cout << "new code";for ( ; t_rf < 11; ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (d_mk >= 111 * t_rf && (d_mk - 111 * t_rf) % 11 == 0) /* 'if' inside */
                {
                    e = true;
                    break;
                }
            	t_rf++; 
            
            }}
            cout << (e ? "YES" : "NO") << "\n";
        }
}


