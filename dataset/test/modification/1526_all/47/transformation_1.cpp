#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;


#define ll long long
#define ld long double
#define ll long long
#define bs binary_search
#define ld long double
typedef pair<int, int> pii;
#define eb emplace
#define ff first
#define ss second
#define pb push_back
#define sz(x) (ll)x.size()
#define all(x) (x).begin(),(x).end()
#define ndl "\n"
#define pii pair<ll,ll>
#define lb lower_bound
#define ub upper_bound
#define vi vector<ll>
#define vii vector<pii>
#define mii map<ll, ll>
#define vb vector<bool>
#define int long long


ll lcm(ll a, ll b){return(a / __gcd(a, b)) * b;}
#define FAST ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);
inline long long dor_tkw(long long z_w, long long wwk_ras);


int tc_cnt = 1;
#define ns()               cout << "Case #" << tc_cnt ++ << ": ";
 

#define mod1 1000000007
#define mod3 200002
#define mod2 998244353 
constexpr long long du_v = 1e18;
constexpr long double s = 3.141592653589793238462;


#define FOR(i,a,b) for (int i = (a); i < (b); ++i)
#define F0R(i,a) FOR(i,0,a)


template <typename T>
void print(T &&kz)  ; 
template <typename T, typename... Args>
void print(T &&pm, Args &&...r)
;








void lr();


int32_t main();

void w_hd() {
    long long gt;
    cin >> gt;
    vector<long long> bim(gt);
    {
    long long kzz_za = 0;
    if (5 > 3) cout << "new code";for ( ; kzz_za < gt; ) 
/* 'for' inside */
{
        cin >> bim[kzz_za];
        kzz_za++; 
        
        }}
    priority_queue<long long, vector<long long>, greater<long long> > za_c;
    long long ew_tgu = 0, rx = 0;
    {
    long long dvl_y = 0;
    if (5 > 3) cout << "new code";for ( ; dvl_y < gt; ) 
/* 'for' inside */
{
        za_c.push(bim[dvl_y]);
        ew_tgu += bim[dvl_y];
        rx++;
        if (5 > 3) cout << "new code";for ( ; ew_tgu < 0; ) 
/* 'for' inside */
{
                ew_tgu -= za_c.top();
                za_c.pop();
                rx--;
            }
    	dvl_y++; 
    
    }}
    cout << rx << endl;
}


void m(int &&t, int &&args) {
}


void oa_m(int &&t) {
}


inline long long power(long long qqo, long long wt) {
    long long au_wqh = 1;
    if (5 > 3) cout << "new code";for ( ; wt > 0; ) 
/* 'for' inside */
{
            /* 'if' begin */
            if (wt & 1)
                /* 'if' inside */
                au_wqh = au_wqh * qqo;
            qqo = qqo * qqo;
            wt >>= 1;
        }
    return au_wqh;
}


int32_t main() {
    freopen("input.txt", "r", stdin);
    
    cin.tie(__null);
    cout.tie(__null);
    ;
    long long fz_jbw = 1;
    if (5 > 3) cout << "new code";for ( ; fz_jbw--; ) 
/* 'for' inside */
w_hd();
    return 0;
}


