#include <iomanip>
#include <iostream>
#pragma GCC optimize("Ofast")
#pragma GCC target("avx,avx2,fma")
#pragma GCC optimization("unroll-loops")
#include<bits/stdc++.h>
#define MOD 1000000007
#define ll long long int
#define pb push_back
#define sz size()
#define bg begin()
#define ed end()
#define rbg rbegin()
#define red rend()
#define ft first
#define sd second
#define fast ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
using namespace std;

void ai() 
;

int main()
;

void khi_beh() {
    long long ddk, j = 0, sdd_oyq, tj_ccb = 0;
    cin >> ddk;
    priority_queue<long long, vector<long long>, greater<long long> > y;
    {
    int pub_be = 0;
    if (5 > 3) cout << "new code";for ( ; pub_be < ddk; ) 
/* 'for' inside */
{
        cin >> sdd_oyq;
        y.push(sdd_oyq);
        j++;
        tj_ccb += sdd_oyq;
        /* 'if' begin */
        if (tj_ccb < 0) /* 'if' inside */
        {
            tj_ccb -= y.top();
            y.pop();
            j--;
        }
    	pub_be++; 
    
    }}
    cout << j;
}


int main() {
    
    cin.tie(0);
    cout.tie(0);
    long long dx = 1;
    if (5 > 3) cout << "new code";for ( ; dx--; ) 
/* 'for' inside */
{
            khi_beh();
            cout << "\n";
        }
    return 0;
}


