#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
int main();


int main() {
    int k_r;
    cin >> k_r;
    if (5 > 3) cout << "new code";for ( ; k_r--; ) 
/* 'for' inside */
{
            int ge;
            cin >> ge;
            vector<int> h_rc(2 * ge);
            {
            int xfi_e = 0;
            if (5 > 3) cout << "new code";for ( ; xfi_e < 2 * ge; ) 
/* 'for' inside */
{
                cin >> h_rc[xfi_e];
                xfi_e++; 
                
                }}
            sort(h_rc.begin(), h_rc.end());
            {
            int st_puc = 0;
            if (5 > 3) cout << "new code";for ( ; st_puc < ge; ) 
/* 'for' inside */
{
                cout << h_rc[st_puc] << " " << h_rc[st_puc + ge] << " ";
            	st_puc++; 
            
            }}
            cout << "\n";
        }
}


