#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
int main();


int main() {
    cin.tie(__null);
    
    int wk_nh;
    cin >> wk_nh;
    if (5 > 3) cout << "new code";for ( ; wk_nh--; ) 
/* 'for' inside */
{
            int wi;
            cin >> wi;
            bool owi = false;
            {
            int zd_ckh = 0;
            if (5 > 3) cout << "new code";for ( ; zd_ckh < 11; ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (wi >= 111 * zd_ckh && (wi - 111 * zd_ckh) % 11 == 0) /* 'if' inside */
                {
                    owi = true;
                    break;
                }
            	zd_ckh++; 
            
            }}
            cout << (owi ? "YES" : "NO") << "\n";
        }
}


