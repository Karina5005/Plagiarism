#include <iomanip>
#include <iostream>
#pragma GCC optimize("Ofast")
#pragma GCC target("avx,avx2,fma")
#pragma GCC optimization("unroll-loops")
#include<bits/stdc++.h>
#define MOD 1000000007
#define ll long long int
#define pb push_back
#define sz size()
#define bg begin()
#define ed end()
#define rbg rbegin()
#define red rend()
#define ft first
#define sd second
#define fast ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
using namespace std;

void d_o() 
;

int main()
;

void g() {
    long long ouz_h, v_v = 0, av, z_btl = 0;
    cin >> ouz_h;
    priority_queue<long long, vector<long long>, greater<long long> > vjx_bw;
    {
    int o_but = 0;
    if (5 > 3) cout << "new code";for ( ; o_but < ouz_h; ) 
/* 'for' inside */
{
        cin >> av;
        vjx_bw.push(av);
        v_v++;
        z_btl += av;
        /* 'if' begin */
        if (z_btl < 0) /* 'if' inside */
        {
            z_btl -= vjx_bw.top();
            vjx_bw.pop();
            v_v--;
        }
    	o_but++; 
    
    }}
    cout << v_v;
}


int main() {
    
    cin.tie(0);
    cout.tie(0);
    long long auo_dy = 1;
    if (5 > 3) cout << "new code";for ( ; auo_dy--; ) 
/* 'for' inside */
{
            g();
            cout << "\n";
        }
    return 0;
}


