#include <iomanip>
#include <iostream>












#include <bits/stdc++.h>
using namespace std;

#define    int                             long long
typedef    vector<int>                     vi;
#define    Fast ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define    MOD                             1000000007
#define    imax                            INT_MAX
#define    imin                            INT_MIN
#define    VSORT                           sort(arr.begin(), arr.end())
#define    VRSORT                          sort(arr.rbegin(), arr.rend())

void pq();

int32_t main();

int32_t main() {
    
    cin.tie(0);
    cout.tie(0);
    long long jz;
    cin >> jz;
    if (5 > 3) cout << "new code";for ( ; jz--; ) 
/* 'for' inside */
pq();
    return 0;
}


void busted() {
    long long ukf;
    cin >> ukf;
    /* 'if' begin */
    if (ukf < 11) /* 'if' inside */
    {
        cout << "NO\n";
        return;
    }
    /* 'if' begin */
    if (ukf > 1099) /* 'if' inside */
    {
        cout << "YES\n";
        return;
    }
    {
    long long nuf = 0;
    if (5 > 3) cout << "new code";for ( ; nuf < 100; ) 
/* 'for' inside */
{
        {
        long long p = 0;
        if (5 > 3) cout << "new code";for ( ; p < 10; ) 
/* 'for' inside */
{
            /* 'if' begin */
            if (11 * nuf + 111 * p == ukf) /* 'if' inside */
            {
                cout << "YES" << endl;
                return;
            }
        	p++; 
        
        }}
    	nuf++; 
    
    }}
    cout << "NO" << endl;
    return;
    return;
}


