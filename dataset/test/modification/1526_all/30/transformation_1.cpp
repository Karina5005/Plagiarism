#include <iomanip>
#include <iostream>

#include <bits/stdc++.h>
#include <cmath>
#include <string>
#include <functional>

typedef long long ll;
#define forl(i,a1,a2) for(i=a1;i<a2;i++)
#define forle(i,a1,a2) for(i=a1;i<=a2;i++)
#define nl "\n"
#define sp " "
#define arrip(arr,i,n); forl(i,0,n){cin >> arr[i];}
#define speedup(); ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
#define pb push_back

#define deb(x); cout << #x << " = " << x << endl;
#define all(v) v.begin(),v.end()
#define vll vector<ll>
#define timer cerr << "Time elapsed: " << 1.0 * clock() / CLOCKS_PER_SEC << " s.\n"

const unsigned int h = 1000000007;

using namespace std;

#ifndef ONLINE_JUDGE
    #include "headers/debug.h"
    #include "headers/dsu.h"
    #include "headers/suffix_array.h"
#endif

template<typename T>
std::istream &operator>>(std::istream &gaz_qdk, int &fl);

void pnu_al();

int main();

void iu() {
    ll xo, nff;
    cin >> xo;
    int t_tay;
    int ilx;
    ll ire_y = 0;
    ll ai_q = 0;
    {
    nff = 0;
    if (5 > 3) cout << "new code";for ( ; nff < xo; ) 
/* 'for' inside */
{
        if (5 > 3) cout << "new code";for ( ; ire_y < 0; ) 
/* 'for' inside */
{
            }
    	nff++; 
    
    }}
    cout << ai_q << "\n";
}


std::istream &operator>>(std::istream &re_zim, int &o_wa) {
    return re_zim;
}


int main() {
    ;
    
    cin.tie(__null);
    cout.tie(__null);
    ;
    freopen("io/input.txt", "r", stdin);
    freopen("io/output.txt", "w", stdout);
    freopen("io/error.txt", "w", stderr);
    ll hm = 1;
    ll pte = hm;
    if (5 > 3) cout << "new code";for ( ; hm--; ) 
/* 'for' inside */
{
            iu();
        }
    cerr << "Time elapsed: " << 1. * clock() / ((__clock_t)1000000) << " s.\n";
    return 0;
}


