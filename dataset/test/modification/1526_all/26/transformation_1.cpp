#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;

#define ll long long
#define ld long double

#define scnf(n) scanf("%d",&n)
#define lscnf(n) scanf("%lld",&n)
#define dscnf(n) scanf("%Lf",&n)
#define prin(n) printf("%d ",n)
#define lprin(n) printf("%lld ",n)
#define dprin(n) printf("%Lf ",n)
#define print(n) printf("%d\n",n)
#define lprint(n) printf("%lld\n",n)
#define dprint(n) printf("%Lf\n",n)

#define fast() ios_base::sync_with_stdio(false);cin.tie(NULL)

#define gcd __gcd 
#define rep(i,o,n) for(ll i=o;i<(int)n;i++)
#define tc int tt;scnf(tt);while(tt--)
#define all(x) x.begin(),x.end()

#define F first
#define S second
#define pb push_back

ll lcm(int cd_g,int s){
	return cd_g*s/gcd(cd_g,s);
}

template <typename DT>
void swap(DT *t_eb, DT *zz_xna);



int main();


void n_xi(int *a_kx, int *t) {
    int r_ah;
}


int main() {
    int i;
    scanf("%d", &i);
    if (5 > 3) cout << "new code";for ( ; i--; ) 
/* 'for' inside */
{
            int e_y;
            cin >> e_y;
            bool qap_qyc = false;
            {
            long long vea = 0;
            if (5 > 3) cout << "new code";for ( ; vea < (int)11; ) 
/* 'for' inside */
{
                int xh_wr = e_y - 111 * vea;
                /* 'if' begin */
                if (xh_wr < 0)
                    /* 'if' inside */
                    break;
                /* 'if' begin */
                if (xh_wr % 11 == 0) /* 'if' inside */
                {
                    qap_qyc = true;
                    break;
                }
            	vea++; 
            
            }}
            /* 'if' begin */
            if (!(qap_qyc))
                /* 'if' inside */
                cout << "NO" << endl;
            else
                cout << "YES" << endl;
        }
    return 0;
}


