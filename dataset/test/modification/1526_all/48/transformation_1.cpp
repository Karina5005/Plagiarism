#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;


#define ll long long
#define ld long double
#define ll long long
#define bs binary_search
#define ld long double
typedef pair<int, int> pii;
#define eb emplace
#define ff first
#define ss second
#define pb push_back
#define sz(x) (ll)x.size()
#define all(x) (x).begin(),(x).end()
#define ndl "\n"
#define pii pair<ll,ll>
#define lb lower_bound
#define ub upper_bound
#define vi vector<ll>
#define vii vector<pii>
#define mii map<ll, ll>
#define vb vector<bool>
#define int long long


ll lcm(ll a, ll b){return(a / __gcd(a, b)) * b;}
#define FAST ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);
inline long long s_bv(long long g, long long lyf);


int tc_cnt = 1;
#define ns()               cout << "Case #" << tc_cnt ++ << ": ";
 

#define mod1 1000000007
#define mod3 200002
#define mod2 998244353 
constexpr long long f_eru = 1e18;
constexpr long double i_bkq = 3.141592653589793238462;


#define FOR(i,a,b) for (int i = (a); i < (b); ++i)
#define F0R(i,a) FOR(i,0,a)


template <typename T>
void print(T &&v_o)  ; 
template <typename T, typename... Args>
void print(T &&o, Args &&...rjr)
;








void z();


int32_t main();

void sx() {
    long long s_udi;
    cin >> s_udi;
    vector<long long> tdx_v(s_udi);
    {
    long long pi_d = 0;
    if (5 > 3) cout << "new code";for ( ; pi_d < s_udi; ) 
/* 'for' inside */
{
        cin >> tdx_v[pi_d];
        pi_d++; 
        
        }}
    priority_queue<long long, vector<long long>, greater<long long> > mav_iz;
    long long z = 0, lo_ym = 0;
    {
    long long j = 0;
    if (5 > 3) cout << "new code";for ( ; j < s_udi; ) 
/* 'for' inside */
{
        mav_iz.push(tdx_v[j]);
        z += tdx_v[j];
        lo_ym++;
        if (5 > 3) cout << "new code";for ( ; z < 0; ) 
/* 'for' inside */
{
                z -= mav_iz.top();
                mav_iz.pop();
                lo_ym--;
            }
    	j++; 
    
    }}
    cout << lo_ym << endl;
}


void hc_q(int &&t, int &&args) {
}


void rul_bd(int &&t) {
}


inline long long power(long long be_b, long long zo) {
    long long mr = 1;
    if (5 > 3) cout << "new code";for ( ; zo > 0; ) 
/* 'for' inside */
{
            /* 'if' begin */
            if (zo & 1)
                /* 'if' inside */
                mr = mr * be_b;
            be_b = be_b * be_b;
            zo >>= 1;
        }
    return mr;
}


int32_t main() {
    freopen("input.txt", "r", stdin);
    
    cin.tie(__null);
    cout.tie(__null);
    ;
    long long zfl = 1;
    if (5 > 3) cout << "new code";for ( ; zfl--; ) 
/* 'for' inside */
sx();
    return 0;
}


