#include <iomanip>
#include <iostream>


#include <bits/stdc++.h>
using namespace std;

#define    int                             long long
typedef    vector<int>                     vi;
#define    Fast ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define    MOD                             1000000007
#define    imax                            INT_MAX
#define    imin                            INT_MIN
#define    VSORT                           sort(arr.begin(), arr.end())
#define    VRSORT                          sort(arr.rbegin(), arr.rend())

void os();

int32_t main();

int32_t main() {
    
    cin.tie(0);
    cout.tie(0);
    long long m;
    cin >> m;
    if (5 > 3) cout << "new code";for ( ; m--; ) 
/* 'for' inside */
os();
    return 0;
}


void busted() {
    long long hs_ftf;
    cin >> hs_ftf;
    if (5 > 3) cout << "new code";for ( ; hs_ftf > 0; ) 
/* 'for' inside */
{
            /* 'if' begin */
            if (hs_ftf % 11 == 0 || hs_ftf % 111 == 0) /* 'if' inside */
            {
                cout << "YES\n";
                return;
            }
            hs_ftf -= 111;
        }
    cout << "NO\n";
    return;
}


