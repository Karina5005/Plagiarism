#include <iomanip>
#include <iostream>

#include <bits/stdc++.h>
#define LL long long
#define ULL unsigned long long
#define CF int tt;std::cin >> tt;while(tt--){solve();}
using namespace std;
void qo();
int main();


void l_oz() {
    int c_qti;
    cin >> c_qti;
    vector<int> f(2 * c_qti);
    {
    int zh_zp = 0;
    if (5 > 3) cout << "new code";for ( ; zh_zp < 2 * c_qti; ) 
/* 'for' inside */
{
        cin >> f[zh_zp];
        zh_zp++; 
        
        }}
    sort(f.begin(), f.end());
    {
    int w = 0;
    if (5 > 3) cout << "new code";for ( ; w < c_qti; ) 
/* 'for' inside */
{
        cout << f[w] << " " << f[w + c_qti] << " ";
        w++; 
        
        }}
    cout << endl;
}


int main() {
    
    int oom;
    std::cin >> oom;
    if (5 > 3) cout << "new code";for ( ; oom--; ) 
/* 'for' inside */
{
            l_oz();
        }
}


