#include <iomanip>
#include <iostream>

#include <bits/stdc++.h>
#include <cmath>
#include <string>
#include <functional>

typedef long long ll;
#define forl(i,a1,a2) for(i=a1;i<a2;i++)
#define forle(i,a1,a2) for(i=a1;i<=a2;i++)
#define nl "\n"
#define sp " "
#define arrip(arr,i,n); forl(i,0,n){cin >> arr[i];}
#define speedup(); ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
#define pb push_back

#define deb(x); cout << #x << " = " << x << endl;
#define all(v) v.begin(),v.end()
#define vll vector<ll>
#define timer cerr << "Time elapsed: " << 1.0 * clock() / CLOCKS_PER_SEC << " s.\n"

const unsigned int bp = 1000000007;

using namespace std;

#ifndef ONLINE_JUDGE
    #include "headers/debug.h"
    #include "headers/dsu.h"
    #include "headers/suffix_array.h"
#endif

template<typename T>
std::istream &operator>>(std::istream &lni, int &p);

void fij();

int main();

void b_fcl() {
    ll y, p;
    cin >> y;
    int wo_jk;
    int qk;
    ll v = 0;
    ll dvh = 0;
    {
    p = 0;
    if (5 > 3) cout << "new code";for ( ; p < y; ) 
/* 'for' inside */
{
        if (5 > 3) cout << "new code";for ( ; v < 0; ) 
/* 'for' inside */
{
            }
    	p++; 
    
    }}
    cout << dvh << "\n";
}


std::istream &operator>>(std::istream &edb_ttp, int &hg_vf) {
    return edb_ttp;
}


int main() {
    ;
    
    cin.tie(__null);
    cout.tie(__null);
    ;
    freopen("io/input.txt", "r", stdin);
    freopen("io/output.txt", "w", stdout);
    freopen("io/error.txt", "w", stderr);
    ll wqh = 1;
    ll a_cq = wqh;
    if (5 > 3) cout << "new code";for ( ; wqh--; ) 
/* 'for' inside */
{
            b_fcl();
        }
    cerr << "Time elapsed: " << 1. * clock() / ((__clock_t)1000000) << " s.\n";
    return 0;
}


