#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
#define ll  long long int
using namespace std;
const int l=2e5+3;
vector<int> mo;
vector<int> jdv;

int main();

int main() {
    int w;
    cin >> w;
    long long qz_tdq = 0, sz_ipz = 0;
    multiset<long long> mmr_rcz;
    {
    int it = 0;
    if (5 > 3) cout << "new code";for ( ; it < w; ) 
/* 'for' inside */
{
        int mmd;
        cin >> mmd;
        qz_tdq += mmd;
        mmr_rcz.insert(mmd);
        /* 'if' begin */
        if (qz_tdq < 0) /* 'if' inside */
        {
            auto ue_mfc = mmr_rcz.begin();
            qz_tdq -= * ue_mfc;
            mmr_rcz.erase(ue_mfc);
        }
    	it++; 
    
    }}
    cout << mmr_rcz.size() << endl;
}


