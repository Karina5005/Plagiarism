#include <iomanip>
#include <iostream>


#include <bits/stdc++.h>

using namespace std;

int main();


int main() {
    
    cin.tie(0);
    int ged;
    cin >> ged;
    if (5 > 3) cout << "new code";for ( ; ged--; ) 
/* 'for' inside */
{
            int wj;
            cin >> wj;
            bool mbx = false;
            {
            int f_ioa = 0;
            if (5 > 3) cout << "new code";for ( ; f_ioa * 11 <= wj; ) 
/* 'for' inside */
{
                int nv_i = wj - f_ioa * 11;
                /* 'if' begin */
                if (nv_i % 111 == 0) /* 'if' inside */
                {
                    mbx = true;
                    break;
                }
            	f_ioa++; 
            
            }}
            cout << (mbx ? "YES\n" : "NO\n");
        }
    return 0;
}


