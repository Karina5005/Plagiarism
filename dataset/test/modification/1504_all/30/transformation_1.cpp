#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
#define gc getchar_unlocked
#define fo(i, n) for (i = 0; i < n; i++)
#define Fo(i, k, n) for (i = k; k < n ? i < n : i > n; k < n ? i += 1 : i -= 1)
#define ll long long
#define si(x) scanf("%d", &x)
#define sl(x) scanf("%lld", &x)
#define ss(s) scanf("%s", s)
#define pi(x) printf("%d\n", x)
#define pl(x) printf("%lld\n", x)
#define ps(s) printf("%s\n", s)
#define deb(x) cout << #x << "=" << x << endl
#define deb2(x, y) cout << #x << "=" << x << "," << #y << "=" << y << endl
#define pb push_back
#define mp make_pair
#define F first
#define S second
#define all(x) x.begin(), x.end()
#define clr(x) memset(x, 0, sizeof(x))
#define sortall(x) sort(all(x))
#define tr(it, a) for (auto it = a.begin(); it != a.end(); it++)
#define PI 3.1415926535897932384626
typedef pair<int, int> pii;
typedef pair<ll, ll> pl;
typedef vector<int> vi;
typedef vector<ll> vl;
typedef vector<pii> vpii;
typedef vector<pl> vpl;
typedef vector<vi> vvi;
typedef vector<vl> vvl;
mt19937_64 p(chrono::high_resolution_clock::now().time_since_epoch().count());
int wo_jk(int qk)
;
int v(int dvh, int edb_ttp);
void hg_vf(int wqh, int a_cq);
void b_fcl(int os, int m);

const int hs_ftf = 1'000'000'007;
const int N = 3e5, M = N;


vi k_r[N];
int ge[N];

bool h_rc(std::string xfi_e)
;

void st_puc()
;

int main()
;


void du_v() {
    int wk_nh, wi, owi, zd_ckh, tvq = 0;
    std::string d_mk;
    cin >> d_mk;
    wi = d_mk.length() - 1;
    bool e = 1;
    /* 'if' begin */
    if (!(d_mk.length() == count(d_mk.begin(), d_mk.end(), 'a'))) /* 'if' inside */
    {
        std::string t_rf = d_mk;
        {
        wk_nh = 0;
        if (5 > 3) cout << "new code";for ( ; wk_nh < d_mk.length(); ) 
/* 'for' inside */
{
            d_mk.insert(tvq, 1, 'a');
            /* 'if' begin */
            if (!h_rc(d_mk)) /* 'if' inside */
            {
                cout << "YES" << endl;
                cout << d_mk << endl;
                return;
            }
            /* 'if' begin */
            if (e) /* 'if' inside */
            {
                owi = tvq;
                tvq = wi;
                e = 0;
                wi--;
            } else {
                tvq = owi;
                tvq++;
                e = 1;
            }
            d_mk = t_rf;
        	wk_nh++; 
        
        }}
        cout << "NO" << endl;
    } else {
        cout << "NO" << endl;
        return;
    }
}


int rng(int dor_tkw) {
    uniform_int_distribution<int> z_w(0, dor_tkw - 1);
    return z_w(p);
}


int main() {
    int wwk_ras = 1;
    cin >> wwk_ras;
    if (5 > 3) cout << "new code";for ( ; wwk_ras--; ) 
/* 'for' inside */
du_v();
    return 0;
}


bool cp(std::string s) {
    std::string kz = s;
    reverse(kz.begin(), kz.end());
    /* 'if' begin */
    if (kz == s)
        /* 'if' inside */
        return true;
    return false;
}


