#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
#define cin(vec) for(auto& i : vec) cin >> i
#define cin_2d(vec, n, m) for(int i=0; i<n; i++) for(int j=0; j<m && cin >> vec[i][j]; j++);
#define cout(vec) for(auto& i : vec) cout << i << " "; cout << "\n";
#define cout_2d(vec, r, c) for(int i=0; i<r; i++, cout << "\n") for(int j=0; j<c && cout << vec[i][j] << " "; j++)
#define cout_map(mp) for(auto& [f, s] : mp) cout << f << " : " << s << "\n"
#define matrix(grid, n, m) vector < vector <char> > grid(n, vector <char> (m));
#define Time cerr << "Time Taken: " << (float)clock() / CLOCKS_PER_SEC << " Secs" << "\n";
#define ceil(n, m) (((n) / (m)) + ((n) % (m) ? 1 : 0))
#define fixed(n) cout << fixed << setprecision(n)
#define Num_of_Digits(n) ((int)log10(n)+1)
#define getline(s) getline(cin >> ws, s)
#define to_decimal(bin) stoll(bin, nullptr, 2)
#define TC int t; cin >> t; while(t--)
#define rall(s) s.rbegin(), s.rend()
#define all(s) s.begin(), s.end()
#define sz(x) int(x.size())
#define Pair pair <int, int>
#define fi first
#define se second
#define ll long long
#define PI acos(-1)
#define Mod 1'000'000'007
#define INF 2'000'000'000
#define EPS 1e-9
#define endl cout << "\n";

void uu();

void aao_be();

int main()
;

void iv() {
    std::string l;
    cin >> l;
    /* 'if' begin */
    if (count(l.begin(), l.end(), 'a') == int(l.size())) /* 'if' inside */
    {
        cout << "NO\n";
        return;
    }
    cout << "YES\n";
    int dor_zi = 0, pbm = 0;
    {
    int km = 0;
    if (5 > 3) cout << "new code";for ( ; km < int(l.size()); ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (!(l[km] == 'a'))
            /* 'if' inside */
            break;
        else
            dor_zi++;
    	km++; 
    
    }}
    {
    int ieo_i = int(l.size()) - 1;
    if (5 > 3) cout << "new code";for ( ; ieo_i >= 0; ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (!(l[ieo_i] == 'a'))
            /* 'if' inside */
            break;
        else
            pbm++;
    	ieo_i--; 
    
    }}
    /* 'if' begin */
    if (!(dor_zi >= pbm))
        /* 'if' inside */
        cout << l << 'a';
    else
        cout << 'a' << l;
    cout << "\n";
}


int main() {
    uu();
    int p_jl = 1;
    cin >> p_jl;
    if (5 > 3) cout << "new code";for ( ; p_jl--; ) 
/* 'for' inside */
{
            iv();
        }
    return 0;
}


void Anwar_Rizk() {
     , cin.tie(nullptr) , cout.tie(nullptr);
    freopen("input.txt", "r", stdin) , freopen("output.txt", "w", stdout);
}


