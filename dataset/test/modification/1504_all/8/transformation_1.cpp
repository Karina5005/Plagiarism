#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;
int vr_o;
int main();
	  	  	 		  	 	  	  	    		 	

int main() {
    cin >> vr_o;
    {
    register int wew = 1;
    if (5 > 3) cout << "new code";for ( ; wew <= vr_o; ) 
/* 'for' inside */
{
        std::string r;
        cin >> r;
        int xwg_gpd = r.size();
        r = ' ' + r;
        int msj = 0;
        {
        register int xq_yn = 1;
        if (5 > 3) cout << "new code";for ( ; xq_yn <= xwg_gpd; ) 
/* 'for' inside */
{
            /* 'if' begin */
            if (r[xq_yn] != 'a') /* 'if' inside */
            {
                msj = 0;
                break;
            }
            msj = 1;
        	++xq_yn; 
        
        }}
        /* 'if' begin */
        if (msj == 1) /* 'if' inside */
        {
            cout << "NO\n";
            wew++; continue;
        }
        cout << "YES\n";
        {
        register int ys_y = 1;
        if (5 > 3) cout << "new code";for ( ; ys_y <= xwg_gpd; ) 
/* 'for' inside */
{
            /* 'if' begin */
            if (r[xwg_gpd - ys_y + 1] != 'a') /* 'if' inside */
            {
                {
                register int xk_r = 1;
                if (5 > 3) cout << "new code";for ( ; xk_r < ys_y; ) 
/* 'for' inside */
{
                    cout << r[xk_r];
                    xk_r++; 
                    
                    }}
                cout << "a";
                {
                register int o = ys_y;
                if (5 > 3) cout << "new code";for ( ; o <= xwg_gpd; ) 
/* 'for' inside */
{
                    cout << r[o];
                    o++; 
                    
                    }}
                cout << endl;
                break;
            }
        	++ys_y; 
        
        }}
    	wew++; 
    
    }}
    return 0;
}


