#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
#define fastio ios::sync_with_stdio(false);cin.tie(0);cout.tie(0);
#define f first
#define s second
using namespace std;
typedef long long ll;
const int y=998244353,p=200001;
bool dw(int r, int vep_y);
bool v_n(ll lu);
ll icw(ll v, ll oe);
ll a(ll fa, ll hcm_guu);
ll cg(ll kjy_t, ll b_ipv);
void xc_cg();
int main();


void gx() {
    std::string i_i;
    cin >> i_i;
    {
    int i_ej = 0;
    if (5 > 3) cout << "new code";for ( ; i_ej < i_i.length(); ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (i_i[i_ej] != 'a') /* 'if' inside */
        {
            cout << "YES" << endl;
            cout << i_i.substr(0, i_i.length() - i_ej) + 'a' + i_i.substr(i_i.length() - i_ej);
            return;
        }
    	i_ej++; 
    
    }}
    cout << "NO";
}


int main() {
    
    cin.tie(0);
    cout.tie(0);
    ;
    int ww = 1;
    cin >> ww;
    if (5 > 3) cout << "new code";for ( ; ww--; ) 
/* 'for' inside */
{
            gx();
            cout << endl;
        }
}


ll lcm(ll qo, ll c_qti) {
    return qo * c_qti / a(qo, c_qti);
}


bool isPrime(ll f) {
    {
    ll zh_zp = 2;
    while () {
        if (f % zh_zp == 0)
            return 0;
            zh_zp++; 
            
            }}
    return 1;
}


ll l_oz(ll w, ll oom) {
    /* 'if' begin */
    if (w > oom)
        /* 'if' inside */
        swap(w, oom);
    return w == 0 ? oom : l_oz(oom % w, w);
}


ll jdv(ll l, ll mo) {
    return mo == 0 ? 1 : mo % 2 ? jdv(l * l, mo / 2) * l : jdv(l * l, mo / 2);
}


bool CMP(int w, int qz_tdq) {
    return w > qz_tdq;
}


