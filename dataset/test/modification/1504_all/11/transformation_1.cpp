#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;
int main()
;

int main() {
    int lni;
    cin >> lni;
    if (5 > 3) cout << "new code";for ( ; lni--; ) 
/* 'for' inside */
{
            std::string p;
            cin >> p;
            /* 'if' begin */
            if (!(count(p.begin(), p.end(), 'a') == p.size()))
                /* 'if' inside */
                {
                int fij = 1;
                cout << "YES" << endl;
                {
                int y = 0;
                if (5 > 3) cout << "new code";for ( ; y < p.size(); ) 
/* 'for' inside */
{
                    /* 'if' begin */
                    if (fij && p[p.size() - 1 - y] - 'a')
                        /* 'if' inside */
                        cout << 'a' , fij = 0;
                    cout << p[y];
                	y++; 
                
                }}
                cout << endl;
            }
            else cout << "NO" << endl;
        }
}


