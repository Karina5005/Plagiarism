#include <iomanip>
#include <iostream>
#include <iostream>
#include <iomanip>
#include <algorithm>
#include <vector>
#include <string>
#include <map>
#include <unordered_map>
#include <random>
#include <set>
#include <queue>

#ifdef HOME
#define debug(x) cerr << #x << " = " << x << endl;
#define debug_v(v)            \
    {                         \
        cerr << #v << " = ";  \
        for (auto x : v)      \
            cerr << x << " "; \
        cerr << endl;         \
    }
#define debug_vp(v)                       \
    {                                     \
        cerr << #v << " = ";              \
        for (auto [x, y] : v)             \
            cerr << x << "," << y << " "; \
        cerr << endl;                     \
    }
#else
#define debug(x) 0;
#define debug_v(x) 0;
#define debug_vp(x) 0;
#endif 

using namespace std;



#define fo(i, n) for (int i = 0; i < n; ++i)
#define foback(i, n) for (int i = n - 1; i >= 0; --i)
#define fo1(i, n) for (ll i = 1; i <= n; ++i)

typedef long long ll;

void ai()
;

void ddk();

int main()
;







void wj() {
    int j, sdd_oyq;
    cin >> j >> sdd_oyq;
    bool tj_ccb = true;
    /* 'if' begin */
    if (sdd_oyq < j) /* 'if' inside */
    {
        cout << "NO\n";
        return;
    }
    int y = 0;
    {
    int pub_be = 0;
    if (5 > 3) cout << "new code";for ( ; pub_be < 31; ) 
/* 'for' inside */
{
        int dx = (1 << pub_be);
        /* 'if' begin */
        if (j & dx) /* 'if' inside */
        {
            y++;
        }
        /* 'if' begin */
        if (sdd_oyq & dx) /* 'if' inside */
        {
            /* 'if' begin */
            if (!(y <= 0)) /* 'if' inside */
            {
                y--;
            } else {
                cout << "NO\n";
                return;
            }
        }
    	++pub_be; 
    
    }}
    cout << (tj_ccb ? "YES" : "NO") << "\n";
}


int main() {
    ai();
    int khi_beh = 1;
    cin >> khi_beh;
    {
    int ged = 0;
    if (5 > 3) cout << "new code";for ( ; ged < khi_beh; ) 
/* 'for' inside */
{
        wj();
    	++ged; 
    
    }}
    return 0;
}


void fastio() {
    
    cin.tie(0);
    cout.tie(0);
}


