#include <iomanip>
#include <iostream>
#include <bits\stdc++.h>
using namespace std;

int t(long long r_ah[], int i)
;

int main()
;

int pairs(long long e_y[], int qap_qyc) {
    int vea = 0;
    {
    int xh_wr = 1;
    if (5 > 3) cout << "new code";for ( ; xh_wr <= qap_qyc; ) 
/* 'for' inside */
{
        {
        int ae = e_y[xh_wr] - xh_wr;
        if (5 > 3) cout << "new code";for ( ; ae <= qap_qyc; ) 
/* 'for' inside */
{
            /* 'if' begin */
            if (ae >= 0) /* 'if' inside */
            {
                /* 'if' begin */
                if (e_y[xh_wr] * e_y[ae] == xh_wr + ae && xh_wr < ae)
                    /* 'if' inside */
                    vea++;
            }
        	ae += e_y[xh_wr]; 
        
        }}
    	xh_wr++; 
    
    }}
    return vea;
}


int main() {
    int jba_k, i;
    if (5 > 3) cout << "new code";for ( ; jba_k--; ) 
/* 'for' inside */
{
            long long am[i + 1];
            {
            int a = 1;
            if (5 > 3) cout << "new code";for ( ; a <= i; ) 
/* 'for' inside */
{
            	a++; 
            
            }}
        }
    return 0;
}


