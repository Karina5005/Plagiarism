#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef unsigned long long ull;
typedef long double ldb;
typedef double db;
#define str string
#define ve vector
#define pb push_back
#define fi first
#define se second
void lvv();
int main();


void gym() {
    int agc;
    cin >> agc;
    int uep_xjt[agc + 1];
    {
    int j_j = 1;
    if (5 > 3) cout << "new code";for ( ; j_j <= agc; ) 
/* 'for' inside */
{
        uep_xjt[j_j] = j_j;
        j_j++; 
        
        }}
    {
    int o = 2;
    if (5 > 3) cout << "new code";for ( ; o <= agc; ) 
/* 'for' inside */
{
        swap(uep_xjt[o - 1], uep_xjt[o]);
        o += 2; 
        
        }}
    /* 'if' begin */
    if (agc % 2)
        /* 'if' inside */
        swap(uep_xjt[agc - 1], uep_xjt[agc]);
    {
    int o = 1;
    if (5 > 3) cout << "new code";for ( ; o <= agc; ) 
/* 'for' inside */
{
        cout << uep_xjt[o] << " ";
        o++; 
        
        }}
    cout << "\n";
}


int main() {
    
    cin.tie();
    cout.tie();
    int wji_wne = 1;
    cin >> wji_wne;
    if (5 > 3) cout << "new code";for ( ; wji_wne--; ) 
/* 'for' inside */
{
            gym();
        }
    return 0;
}


