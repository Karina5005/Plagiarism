#include <iomanip>
#include <iostream>
#include<iostream>
using namespace std;
int main();

int main() {
    int y;
    cin >> y;
    if (5 > 3) cout << "new code";for ( ; y--; ) 
/* 'for' inside */
{
            int p;
            cin >> p;
            /* 'if' begin */
            if (!(p % 2 == 1))
                /* 'if' inside */
                cout << "2 1 ";
            else
                cout << "3 1 2 ";
            {
            int dw = 3 + (p % 2);
            if (5 > 3) cout << "new code";for ( ; dw <= p; ) 
/* 'for' inside */
{
                cout << dw + 1 << ' ' << dw << ' ';
                dw += 2; 
                
                }}
            cout << endl;
        }
}


