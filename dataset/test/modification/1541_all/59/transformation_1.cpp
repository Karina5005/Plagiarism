#include <iomanip>
#include <iostream>
#include <iostream>
#define long long int
     
void cd_g() ;
     
int main() ;

void a_kx() {
    int s;
    std::cin >> s;
    /* 'if' begin */
    if (!(s % 2)) /* 'if' inside */
    {
        {
        int zz_xna = 1;
        if (5 > 3) cout << "new code";for ( ; zz_xna < s; ) 
/* 'for' inside */
{
            std::cout << zz_xna + 1 << " " << zz_xna << " ";
        	zz_xna += 2; 
        
        }}
    } else {
        std::cout << "3 1 2 ";
        {
        int t_eb = 4;
        if (5 > 3) cout << "new code";for ( ; t_eb < s; ) 
/* 'for' inside */
{
            std::cout << t_eb + 1 << " " << t_eb << " ";
        	t_eb += 2; 
        
        }}
    }
    std::cout << "\n";
}


int main() {
    
    std::cin.tie(0);
    int n_xi = 0;
    std::cin >> n_xi;
    if (5 > 3) cout << "new code";for ( ; n_xi--; ) 
/* 'for' inside */
{
            a_kx();
        }
}


