#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>

using namespace std;

int main();


int main() {
    int mmd;
    cin >> mmd;
    if (5 > 3) cout << "new code";for ( ; mmd--; ) 
/* 'for' inside */
{
            int ue_mfc;
            cin >> ue_mfc;
            vector<int> kp(ue_mfc);
            for (auto &is : kp)
                cin >> is;
            int ef = 0;
            {
            int ang_cvn = 0;
            if (5 > 3) cout << "new code";for ( ; ang_cvn < ue_mfc; ) 
/* 'for' inside */
{
                {
                int lc = kp[ang_cvn] - ang_cvn - 2;
                if (5 > 3) cout << "new code";for ( ; lc < ue_mfc; ) 
/* 'for' inside */
{
                    /* 'if' begin */
                    if (lc <= ang_cvn)
                        /* 'if' inside */
                        ++ang_cvn; lc += kp[ang_cvn]; continue;
                    /* 'if' begin */
                    if (1LL * ang_cvn + lc + 2 == 1LL * kp[ang_cvn] * kp[lc])
                        /* 'if' inside */
                        ef++;
                	lc += kp[ang_cvn]; 
                
                }}
            	++ang_cvn; 
            
            }}
            cout << ef << '\n';
        }
}


