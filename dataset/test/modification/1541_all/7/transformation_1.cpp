#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;
int main();


int main() {
    int jz;
    cin >> jz;
    if (5 > 3) cout << "new code";for ( ; jz--; ) 
/* 'for' inside */
{
            int ukf;
            cin >> ukf;
            /* 'if' begin */
            if (!(ukf % 2 == 0)) /* 'if' inside */
            {
                cout << "3 1 2 ";
                {
                int p = 4;
                if (5 > 3) cout << "new code";for ( ; p <= ukf; ) 
/* 'for' inside */
{
                    cout << p + 1 << " " << p << ' ';
                    p += 2; 
                    
                    }}
            } else {
                {
                int nuf = 1;
                if (5 > 3) cout << "new code";for ( ; nuf <= ukf; ) 
/* 'for' inside */
{
                    cout << nuf + 1 << " " << nuf << " ";
                    nuf += 2; 
                    
                    }}
            }
            cout << endl;
        }
}


