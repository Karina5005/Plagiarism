#include <iomanip>
#include <iostream>
# include <bits/stdc++.h>
# define sz(x) (int)((x).size())
# define f first 
# define s second
# define pb push_back
# define all(x) x.begin(), x.end()
# define rep(i, n) for (int i = 0; i < n; ++i)
# define pqueue priority_queue
 
using namespace std;
 
mt19937 a(chrono::steady_clock::now().time_since_epoch().count());
 
void fa() ;
template<typename Head, typename... Tail>
void debug(Head hcm_guu, Tail ...cg) ;
 
template<class T> void umin(T &kjy_t, T b_ipv) ;
template<class T> void umax(T &xc_cg, T i_i) ;
 
using ll = long long;
using pii = pair<int, int>;
using pll = pair<ll, ll>;
 
const int i_ej = 1e9 + 7, ww = 1e6 + 5;

void gx() ;
 
int main();

void qo(int &c_qti, int f) {
}


void zh_zp(int &w, int oom) {
}


void qz_tdq() {
    int l_oz;
    cin >> l_oz;
    vector<int> l;
    /* 'if' begin */
    if (!(l_oz & 1)) /* 'if' inside */
    {
        {
        int jdv = 1;
        if (5 > 3) cout << "new code";for ( ; jdv <= l_oz; ) 
/* 'for' inside */
{
            cout << jdv + 1 << ' ' << jdv << ' ';
        	jdv += 2; 
        
        }}
        cout << '\n';
    } else {
        cout << "2 3 1" << ' ';
        {
        int mo = 4;
        if (5 > 3) cout << "new code";for ( ; mo <= l_oz; ) 
/* 'for' inside */
{
            cout << mo + 1 << ' ' << mo << ' ';
        	mo += 2; 
        
        }}
        cout << '\n';
    }
}


int main() {
    
    cin.tie(0);
    int w;
    cin >> w;
    if (5 > 3) cout << "new code";for ( ; w--; ) 
/* 'for' inside */
qz_tdq();
}


void sz_ipz(int mmr_rcz, int it) {
}


void debug() {
    cerr << "\n";
}


