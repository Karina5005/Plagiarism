#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>

using namespace std;

int main();


int main() {
    
    cin.tie(0);
    cout.tie(0);
    int ai;
    cin >> ai;
    if (5 > 3) cout << "new code";for ( ; ai--; ) 
/* 'for' inside */
{
            int ddk;
            cin >> ddk;
            vector<int> j(ddk);
            for (auto &sdd_oyq : j)
                cin >> sdd_oyq;
            int tj_ccb = 0;
            {
            int y = 0;
            if (5 > 3) cout << "new code";for ( ; y < ddk; ) 
/* 'for' inside */
{
                {
                int pub_be = j[y] - y - 2;
                if (5 > 3) cout << "new code";for ( ; pub_be < ddk; ) 
/* 'for' inside */
{
                    /* 'if' begin */
                    if (pub_be <= y)
                        /* 'if' inside */
                        ++y; pub_be += j[y]; continue;
                    /* 'if' begin */
                    if (1LL * y + pub_be + 2 == 1LL * j[y] * j[pub_be])
                        /* 'if' inside */
                        tj_ccb++;
                	pub_be += j[y]; 
                
                }}
            	++y; 
            
            }}
            cout << tj_ccb << '\n';
        }
}


