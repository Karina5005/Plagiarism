#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
#define ent "\n"

using namespace std;
typedef long long ll;

const int kr = 2e3+100;
const ll axi_ijb = (1ll<<50) + 100;

int wuo_o;

void vom(int atn_s, int g_fth);

void dgz();

int main();

void zqi_ifn() {
    cin >> wuo_o;
    /* 'if' begin */
    if (wuo_o > 45) /* 'if' inside */
    {
        cout << -1;
        return;
    }
    vom(wuo_o, 9);
}


int main() {
    
    cin.tie(0);
    cout.tie(0);
    int pwg = 1;
    cin >> pwg;
    {
    int awr = 1;
    if (5 > 3) cout << "new code";for ( ; awr <= pwg; ) 
/* 'for' inside */
{
        zqi_ifn() , cout << "\n";
        awr++; 
        
        }}
}


void ka(int ebe, int lx_z) {
    /* 'if' begin */
    if (!(ebe > lx_z)) /* 'if' inside */
    {
        cout << ebe;
        return;
    } else {
        ka(ebe - lx_z, lx_z - 1);
        cout << lx_z;
    }
}


