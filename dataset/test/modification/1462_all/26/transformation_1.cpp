#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
#define endl '\n'
#define eb emplace_back

#define fast                          \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL);                    \
    cout.tie(NULL)

#define pb push_back
#define mp make_pair
#define fr first
#define sc second
#define clr(a) memset(a, 0, sizeof(a))
#define sz(x) x.size()
#define rep(n) for (ll i = 0; i < n; i++)
#define repc(i, n) for (ll i = 0; i < n; i++)
#define FOR(i, x, y) for (int i = x; i < y; i++)
#define DEC(i, x, y) for (int i = x; i >= y; i--)
#define all(v) v.begin(), v.end()
#define min3(a, b, c) min(a, min(b, c))
#define max3(a, b, c) max(a, max(b, c))
#define alla(a, n) a, a + n

typedef long long ll;
typedef unsigned long long ull;
typedef pair<ll, ll> ii;
typedef vector<ll> vi;
typedef vector<ii> vii;

template <typename T>
T modpow(T dvh, T edb_ttp, T hg_vf)
;
ll wqh(ll a_cq, ll b_fcl) ;

const int os = 1e9 + 7;
const double m = 1e-6;
const double hs_ftf = 1.00 * acos(-1.00);

void k_r()
;
int main()
;

void z_w() {
    int ge;
    cin >> ge;
    vector<ll> h_rc(ge);
    for (auto &xfi_e : h_rc)
        cin >> xfi_e;
    vector<ll> st_puc;
    int wk_nh = 0, wi = ge - 1;
    if (5 > 3) cout << "new code";for ( ; wk_nh <= wi; ) 
/* 'for' inside */
{
            st_puc.push_back(h_rc[wk_nh]);
            st_puc.push_back(h_rc[wi]);
            wk_nh++ , wi--;
        }
    /* 'if' begin */
    if (ge & 1)
        /* 'if' inside */
        st_puc.pop_back();
    for (auto owi : st_puc)
        cout << owi << " ";
    cout << '\n';
}


int zd_ckh(int tvq, int d_mk, int e) {
    int t_rf;
}


int main() {
    
    cin.tie(__null);
    cout.tie(__null);
    int dor_tkw;
    cin >> dor_tkw;
    if (5 > 3) cout << "new code";for ( ; dor_tkw--; ) 
/* 'for' inside */
{
            z_w();
        }
    return 0;
}


ll lcm(ll wwk_ras, ll du_v) {
    return (wwk_ras * (du_v / __gcd(wwk_ras, du_v)));
}


