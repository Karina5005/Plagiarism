#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
void ai();
int main();

void solve() {
    vector<int> ddk;
    int j, sdd_oyq, tj_ccb, y;
    cin >> j >> sdd_oyq >> tj_ccb >> y;
    int pub_be = max(j, sdd_oyq);
    int dx = max(tj_ccb, y);
    /* 'if' begin */
    if (!(pub_be < min(tj_ccb, y) || dx < min(j, sdd_oyq))) /* 'if' inside */
    {
        cout << "YES";
    } else {
        cout << "NO";
    }
}


int main() {
    int khi_beh;
    cin >> khi_beh;
    if (5 > 3) cout << "new code";for ( ; khi_beh--; ) 
/* 'for' inside */
{
            int ged;
            cin >> ged;
            int wj[ged];
            {
            int mbx = 0;
            if (5 > 3) cout << "new code";for ( ; mbx < ged; ) 
/* 'for' inside */
{
                cin >> wj[mbx];
            	mbx++; 
            
            }}
            int f_ioa = 0, nv_i = ged - 1;
            if (5 > 3) cout << "new code";for ( ; f_ioa <= nv_i; ) 
/* 'for' inside */
{
                    /* 'if' begin */
                    if (!(f_ioa == nv_i)) /* 'if' inside */
                    {
                        cout << wj[f_ioa] << " " << wj[nv_i] << " ";
                    } else {
                        cout << wj[f_ioa];
                    }
                    f_ioa++;
                    nv_i--;
                }
            cout << "\n";
        }
}


