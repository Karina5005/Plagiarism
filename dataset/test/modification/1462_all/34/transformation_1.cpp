#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
#ifdef LOCAL
#include "debug.hpp"
#else
#define debug(...) void(0);
#endif
using namespace std;
#define int long long
void d_o() ;

int32_t main() ;


void auo_dy() {
    long long ouz_h;
    std::string v_v;
    cin >> ouz_h >> v_v;
    long long av = 0;
    {
    long long z_btl = 0;
    if (5 > 3) cout << "new code";for ( ; z_btl <= 4; ) 
/* 'for' inside */
{
        long long vjx_bw = 4 - z_btl;
        /* 'if' begin */
        if (v_v.substr(0, z_btl) + v_v.substr(ouz_h - vjx_bw) == "2020") /* 'if' inside */
        {
            cout << "YES\n";
            return;
        }
    	z_btl++; 
    
    }}
    cout << "NO\n";
}


int32_t main() {
    
    cin.tie(__null);
    cout.tie(__null);
    long long o_but;
    cin >> o_but;
    if (5 > 3) cout << "new code";for ( ; o_but--; ) 
/* 'for' inside */
auo_dy();
    return 0;
}


