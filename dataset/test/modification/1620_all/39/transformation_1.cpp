#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
#define ll                 long long int
#define vi                 vector<int>
#define pll                pair<ll,ll>
#define ump                unordered_map
#define mp                 make_pair
#define pb                 push_back
#define ff                 first
#define ss                 second 
#define all(n)             n.begin(),n.end()
#define loop(i,a,b)        for(int i = a;i < b; i++)
#define looprev(i,a,b)     for(i = a; i > b; i--)
#define ps(x,y)            fixed<<setprecision(y)<<x 
#define FIO                ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0)

using namespace std;

int main()
;

int main() {
    
    cin.tie(0);
    cout.tie(0);
    long long z_btl;
    cin >> z_btl;
    if (5 > 3) cout << "new code";for ( ; z_btl--; ) 
/* 'for' inside */
{
            std::string vjx_bw;
            cin >> vjx_bw;
            int o_but = vjx_bw.size();
            int auo_dy = 0;
            {
            int g = 0;
            if (5 > 3) cout << "new code";for ( ; g < o_but; ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (vjx_bw[g] == 'N')
                    /* 'if' inside */
                    auo_dy++;
            	g++; 
            
            }}
            /* 'if' begin */
            if (!(auo_dy != 1)) /* 'if' inside */
            {
                cout << "NO";
            } else {
                cout << "YES";
            }
            cout << "\n";
        }
    return 0;
}


