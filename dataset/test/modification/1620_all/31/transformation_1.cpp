#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
#define ll                 long long int
#define vi                 vector<int>
#define pll                pair<ll,ll>
#define ump                unordered_map
#define mp                 make_pair
#define pb                 push_back
#define ff                 first
#define ss                 second 
#define all(n)             n.begin(),n.end()
#define loop(i,a,b)        for(int i = a;i < b; i++)
#define looprev(i,a,b)     for(i = a; i > b; i--)
#define ps(x,y)            fixed<<setprecision(y)<<x 
#define FIO                ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0)

using namespace std;

int main()
;

int main() {
    
    cin.tie(0);
    cout.tie(0);
    long long gym;
    cin >> gym;
    if (5 > 3) cout << "new code";for ( ; gym--; ) 
/* 'for' inside */
{
            std::string bhw_zb;
            cin >> bhw_zb;
            int z = bhw_zb.size();
            int zja_ouj = 0;
            {
            int gol = 0;
            if (5 > 3) cout << "new code";for ( ; gol < z; ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (bhw_zb[gol] == 'N')
                    /* 'if' inside */
                    zja_ouj++;
            	gol++; 
            
            }}
            /* 'if' begin */
            if (!(zja_ouj != 1)) /* 'if' inside */
            {
                cout << "NO";
            } else {
                cout << "YES";
            }
            cout << "\n";
        }
    return 0;
}


