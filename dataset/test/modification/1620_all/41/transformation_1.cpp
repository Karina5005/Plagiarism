#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>

using namespace std;
typedef long long l;

l ai;
char ddk[100];

int main() ;


int main() {
    cin >> ai;
    scanf("\n");
    {
    l j = 1;
    if (5 > 3) cout << "new code";for ( ; j <= ai; ) 
/* 'for' inside */
{
        cin >> ddk;
        l sdd_oyq = 0;
        {
        l tj_ccb = 0;
        if (5 > 3) cout << "new code";for ( ; tj_ccb < strlen(ddk); ) 
/* 'for' inside */
{
            /* 'if' begin */
            if (ddk[tj_ccb] == 'N') /* 'if' inside */
            {
                ++sdd_oyq;
            }
        	++tj_ccb; 
        
        }}
        /* 'if' begin */
        if (!(sdd_oyq == 1)) /* 'if' inside */
        {
            cout << "YES" << endl;
        } else {
            cout << "NO" << endl;
        }
    	++j; 
    
    }}
    return 0;
}


