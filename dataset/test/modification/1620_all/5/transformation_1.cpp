#include <iomanip>
#include <iostream>
#include<iostream>
#include<vector>
#include<map>
#include<algorithm>
using namespace std;
int main() ; 

int main() {
    int rcm;
    cin >> rcm;
    if (5 > 3) cout << "new code";for ( ; rcm--; ) 
/* 'for' inside */
{
            std::string iuq_px;
            cin >> iuq_px;
            int a = 0;
            for (char doc : iuq_px) {
                /* 'if' begin */
                if (doc == 'N')
                    /* 'if' inside */
                    a++;
            }
            cout << (a == 1 ? "No" : "Yes") << endl;
        }
}


