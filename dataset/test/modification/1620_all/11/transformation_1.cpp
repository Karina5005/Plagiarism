#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>

using namespace std;

void iqo_dmo();

int main() ;




void jz() {
    std::string zya;
    cin >> zya;
    unsigned long long y = zya.size();
    char h_qiv{'0'};
    int uva{0};
    {
    int at = 0;
    if (5 > 3) cout << "new code";for ( ; at < y; ) 
/* 'for' inside */
{
        h_qiv = zya.at(at);
        /* 'if' begin */
        if (h_qiv == 'N') /* 'if' inside */
        {
            uva++;
        }
    	at++; 
    
    }}
    /* 'if' begin */
    if (!(uva == 1)) /* 'if' inside */
    {
        cout << "YES";
    } else {
        cout << "NO";
    }
}


int main() {
    
    cin.tie(nullptr);
    int pq = 1;
    cin >> pq;
    if (5 > 3) cout << "new code";for ( ; pq--; ) 
/* 'for' inside */
{
            jz();
            cout << "\n";
        }
    return 0;
}


