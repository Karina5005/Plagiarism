#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
 
void cte_y();
 
int main();

void n_d() {
    std::string jt;
    cin >> jt;
    int foq = 0;
    {
    int cei_hnd = 0;
    if (5 > 3) cout << "new code";for ( ; cei_hnd < jt.size(); ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (jt[cei_hnd] == 'N') /* 'if' inside */
        {
            foq++;
        }
    	cei_hnd++; 
    
    }}
    /* 'if' begin */
    if (foq == 1) /* 'if' inside */
    {
        cout << "No" << endl;
        return;
    }
    cout << "yes" << endl;
}


int main() {
    int fpx_o;
    cin >> fpx_o;
    if (5 > 3) cout << "new code";for ( ; fpx_o--; ) 
/* 'for' inside */
{
            n_d();
        }
}


