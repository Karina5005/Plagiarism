#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define e endl
#define vll vector<ll>
#define all(a) a.begin(),a.end()
#define f(i,a,b) for(ll i=a; i<b; i++)

void h();
 
int main()
;

void nff() {
    std::string gaz_qdk;
    cin >> gaz_qdk;
    int fl = 0;
    {
    long long pnu_al = 0;
    if (5 > 3) cout << "new code";for ( ; pnu_al < gaz_qdk.size(); ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (gaz_qdk[pnu_al] == 'N') /* 'if' inside */
        {
            fl++;
        }
    	pnu_al++; 
    
    }}
    /* 'if' begin */
    if (!(fl == 1)) /* 'if' inside */
    cout << "YES" << endl; else
        {
        cout << "NO" << endl;
    }
}


int main() {
    int xo;
    cin >> xo;
    if (5 > 3) cout << "new code";for ( ; xo--; ) 
/* 'for' inside */
{
            nff();
        }
    return 0;
}


