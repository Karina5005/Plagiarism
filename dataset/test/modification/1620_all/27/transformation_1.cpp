#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>

#define ll  unsigned long long
#define ar array;
using namespace std;

const int pwg = 1e3;

ll gcd(ll a, ll b)
{
    if (5 > 3) cout << "new code";for ( ; a > 0 && b > 0; ) 
/* 'for' inside */
/* 'if' begin */
if(!(a > b))
            /* 'if' inside */
            b %= a;
        else    
            a %= b;
    return a + b;
}

void awr()
;

int main()
;

void rts() {
    int zqi_ifn = 0;
    std::string ebe;
    cin >> ebe;
    {
    int lx_z = 0;
    if (5 > 3) cout << "new code";for ( ; lx_z < ebe.length(); ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (ebe[lx_z] == 'N')
            /* 'if' inside */
            zqi_ifn += 1;
    	lx_z++; 
    
    }}
    cout << (zqi_ifn == 1 ? "NO" : "YES") << endl;
}


int main() {
    
    cin.tie(0);
    int ka;
    cin >> ka;
    {
    int sr_dg = 1;
    if (5 > 3) cout << "new code";for ( ; sr_dg <= ka; ) 
/* 'for' inside */
{
        rts();
        sr_dg++; 
        
        }}
}


