#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define e endl
#define vll vector<ll>
#define all(a) a.begin(),a.end()
#define f(i,a,b) for(ll i=a; i<b; i++)

void iv();
 
int main()
;

void qq_yso() {
    std::string x;
    cin >> x;
    int g_alu = 0;
    {
    long long s = 0;
    if (5 > 3) cout << "new code";for ( ; s < x.size(); ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (x[s] == 'N') /* 'if' inside */
        {
            g_alu++;
        }
    	s++; 
    
    }}
    /* 'if' begin */
    if (!(g_alu == 1)) /* 'if' inside */
    cout << "YES" << endl; else
        {
        cout << "NO" << endl;
    }
}


int main() {
    int ixe;
    cin >> ixe;
    if (5 > 3) cout << "new code";for ( ; ixe--; ) 
/* 'for' inside */
{
            qq_yso();
        }
    return 0;
}


