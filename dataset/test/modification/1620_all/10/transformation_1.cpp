#include <iomanip>
#include <iostream>

#include <iostream>
#include <algorithm>
#include <cmath>
#include <string>
#include <array>
#include <vector>
#include <stack>
#include <queue>
#include <set>
#include <unordered_set>
#include <map>
#include <unordered_map>
#include <iomanip>
#include <functional>

using namespace std;

typedef long long           ll;
typedef unsigned long long ull;
typedef pair<int, int>      pi;

const int  y = 1e9 + 10;
const ll   pub_be = 1e18 + 10;
const int  dx = 1e9 + 7;

ll khi_beh(ll ged, ll wj) ;

int main()
;

int main() {
    
    int mbx;
    cin >> mbx;
    if (5 > 3) cout << "new code";for ( ; mbx--; ) 
/* 'for' inside */
{
            std::string f_ioa;
            cin >> f_ioa;
            int nv_i = 0;
            for (auto &d_o : f_ioa) {
                nv_i += d_o == 'N';
            }
            cout << (nv_i == 1 ? "NO\n" : "YES\n");
        }
}


ll av(ll ouz_h, ll v_v) {
    /* 'if' begin */
    if (v_v == 0) /* 'if' inside */
    {
        return ouz_h;
    }
    return av(v_v, ouz_h % v_v);
}


