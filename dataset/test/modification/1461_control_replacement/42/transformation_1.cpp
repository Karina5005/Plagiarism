#include <iomanip>
#include <iostream>
#include <iostream>

using namespace std;

int main()
{
     cin.tie(0);

    int t, n, k;
    cin >> t;
    for ( ; t--; ) 
{
        cin >> n >> k;
        {
        int i = 0;
        for ( ; i < n; ) 
{
            cout << (char){'a' + i%3};
        	i++; 
        
        }}
        cout << "\n";
    }
}
