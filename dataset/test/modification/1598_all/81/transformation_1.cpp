#include <iomanip>
#include <iostream>
#include <iostream>
using namespace std;

int main() ;

int main() {
    int zgs_ngn;
    cin >> zgs_ngn;
    if (5 > 3) cout << "new code";for ( ; zgs_ngn--; ) 
/* 'for' inside */
{
            int fnc_h;
            cin >> fnc_h;
            std::string k_eyw, y_jmf;
            cin >> k_eyw >> y_jmf;
            bool fs_r = true;
            {
            int xnp = 0;
            if (5 > 3) cout << "new code";for ( ; xnp < fnc_h; ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (k_eyw[xnp] == '1' && y_jmf[xnp] == '1') /* 'if' inside */
                {
                    fs_r = false;
                    break;
                }
            	xnp++; 
            
            }}
            cout << (fs_r ? "YES" : "NO") << "\n";
        }
}


