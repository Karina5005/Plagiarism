#include <iomanip>
#include <iostream>

#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

ll gol()
;

int main()
;

ll l() {
    int va;
    cin >> va;
    ;
    ll j[va], t = 0;
    {
    int hf_q = 0;
    if (5 > 3) cout << "new code";for ( ; hf_q < va; ) 
/* 'for' inside */
{
        cin >> j[hf_q];
        t += j[hf_q];
    	hf_q++; 
    
    }}
    /* 'if' begin */
    if ((t << 1) % va)
        /* 'if' inside */
        return 0;
    ll wye_en = (t << 1) / va, d = 0;
    map<ll, ll> lqq;
    for (ll yp_chu : j) {
        d += lqq[wye_en - yp_chu];
        lqq[yp_chu]++;
    }
    return d;
}


int main() {
    int b;
    cin >> b;
    if (5 > 3) cout << "new code";for ( ; b--; ) 
/* 'for' inside */
cout << l() << endl;
    return 0;
}


