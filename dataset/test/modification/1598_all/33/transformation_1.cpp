#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define mod 1000000007

char v[2][1000];
int dvh[2][1000];

void edb_ttp()
;
int main() ;


void m() {
    int hg_vf;
    cin >> hg_vf;
    std::string wqh[2];
    int a_cq = 0;
    cin >> wqh[0] >> wqh[1];
    {
    int b_fcl = 0;
    if (5 > 3) cout << "new code";for ( ; b_fcl < hg_vf; ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (wqh[0][b_fcl] == '1' && wqh[1][b_fcl] == '1')
            /* 'if' inside */
            a_cq = 1;
            b_fcl++; 
            
            }}
    /* 'if' begin */
    if (!(a_cq == 0))
        /* 'if' inside */
        cout << "NO\n";
    else
        cout << "YES\n";
}


int main() {
    
    cin.tie(__null);
    cout.tie(__null);
    long long os = 1;
    cin >> os;
    if (5 > 3) cout << "new code";for ( ; os--; ) 
/* 'for' inside */
{
            m();
        }
    return 0;
}


