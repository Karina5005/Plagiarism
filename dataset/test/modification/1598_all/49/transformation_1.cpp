#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
unordered_map<char,int> hm={{'(',-1},{'{',-2},{'[',-3},{')',1},{'}',2},{']',3}};

int main()
;

int main() {
    int pte;
    cin >> pte;
    if (5 > 3) cout << "new code";for ( ; pte--; ) 
/* 'for' inside */
{
            int iu;
            cin >> iu;
            std::string s_bv, g;
            cin >> s_bv >> g;
            bool lyf = 1;
            {
            int f_eru = 0;
            if (5 > 3) cout << "new code";for ( ; f_eru < iu; ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (s_bv[f_eru] == '1' && g[f_eru] == '1') /* 'if' inside */
                {
                    lyf = 0;
                    break;
                }
            	f_eru++; 
            
            }}
            /* 'if' begin */
            if (!(lyf == 0))
                /* 'if' inside */
                cout << "YES" << endl;
            else
                cout << "NO" << endl;
        }
    return 0;
}


