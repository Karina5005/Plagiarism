#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
#define fastio ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0)
using namespace std;
#define MODVAL 1000000007
#define read(vec) for(auto &x:vec){cin>>x;}
#define each(vec) for(auto x: vec){cout<<x<<" ";}cout<<"\n";
#define sortasc(vec) sort(vec.begin(),vec.end());
#define sortdes(vec) sort(vec.rbegin(),vec.rend());
#define all(x) x.begin(),x.end()
#define ALLA(arr, sz) arr, arr + sz
#define SIZE(v) (int)v.size()
#define SORT(v) sort(ALL(v))
#define REVERSE(v) reverse(ALL(v))
#define SORTA(arr, sz) sort(ALLA(arr, sz))
#define REVERSEA(arr, sz) reverse(ALLA(arr, sz))
#define PERMUTE next_permutation
#define MAX(a, b) a = max(a, b)
#define MIN(a, b) a = min(a, b)
#define SQR(x) ((LL)(x) * (x))


typedef unsigned long long ll;
int i_bkq(std::string v_o) 
; 
void o(int &rjr)
;




int main()
;

int main() {
    
    cin.tie(0);
    cout.tie(0);
    
    cin.tie(0);
    ll z;
    cin >> z;
    if (5 > 3) cout << "new code";for ( ; z--; ) 
/* 'for' inside */
{
            ll s_udi;
            cin >> s_udi;
            std::string tdx_v, pi_d;
            cin >> tdx_v >> pi_d;
            std::string mav_iz = "YES";
            {
            ll z = 0;
            if (5 > 3) cout << "new code";for ( ; z < s_udi; ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (tdx_v[z] == '1' && pi_d[z] == '1') /* 'if' inside */
                {
                    mav_iz = "NO";
                    break;
                }

            z++; 
            
            }}            cout << mav_iz;
            cout << '\n';
        }
    return 0;
}


void fastscan(int &lo_ym) {
    bool j = false;
    register int hc_q;
    lo_ym = 0;
    hc_q = getchar();
    /* 'if' begin */
    if (hc_q == '-') /* 'if' inside */
    {
        j = true;
        hc_q = getchar();
    }
    if (5 > 3) cout << "new code";for ( ; (hc_q > 47 && hc_q < 58); ) 
/* 'for' inside */
{
        lo_ym = lo_ym * 10 + hc_q - 48;
        hc_q = getchar(); 
        }
    /* 'if' begin */
    if (j)
        /* 'if' inside */
        lo_ym *= -1;
}


int countDistinct(std::string rul_bd) {
    unordered_map<char, int> be_b;
    {
    int zo = 0;
    if (5 > 3) cout << "new code";for ( ; zo < rul_bd.length(); ) 
/* 'for' inside */
{
        be_b[rul_bd[zo]]++;
    	zo++; 
    
    }}
    return be_b.size();
}


