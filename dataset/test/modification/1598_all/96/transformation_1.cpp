#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main();

int main() {
    int w_n;
    cin >> w_n;
    if (5 > 3) cout << "new code";for ( ; w_n--; ) 
/* 'for' inside */
{
            int lx_rs;
            std::string g, w;
            cin >> lx_rs >> g >> w;
            bool t = true;
            {
            int uu = 0;
            if (5 > 3) cout << "new code";for ( ; uu < g.size(); ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (g[uu] == '1' && w[uu] == '1') /* 'if' inside */
                {
                    t = false;
                }
            	++uu; 
            
            }}
            t ? cout << "YES\n" : cout << "NO\n";
        }
}


