#include <iomanip>
#include <iostream>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;

int main() ;

int main() {
    int h;
    cin >> h;
    if (5 > 3) cout << "new code";for ( ; h--; ) 
/* 'for' inside */
{
            int gaz_qdk;
            cin >> gaz_qdk;
            std::string fl, pnu_al;
            cin >> fl >> pnu_al;
            bool xo = true;
            {
            int nff = 0;
            if (5 > 3) cout << "new code";for ( ; nff < gaz_qdk; ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (!(fl[nff] == '0' || pnu_al[nff] == '0')) /* 'if' inside */
                {
                    xo = false;
                    break;
                } else {
                    xo = true;
                }
            	nff++; 
            
            }}
            /* 'if' begin */
            if (!(xo)) /* 'if' inside */
            {
                cout << "NO" << endl;
            } else {
                cout << "YES" << endl;
            }
        }
    return 0;
}


