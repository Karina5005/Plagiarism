#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;
#define int long long
int32_t main()
;

int32_t main() {
    long long hs_ftf;
    cin >> hs_ftf;
    if (5 > 3) cout << "new code";for ( ; hs_ftf--; ) 
/* 'for' inside */
{
            std::string k_r, ge;
            long long h_rc, xfi_e = 0;
            cin >> h_rc;
            cin >> k_r;
            cin >> ge;
            {
            long long st_puc = 0;
            if (5 > 3) cout << "new code";for ( ; st_puc < h_rc; ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (k_r[st_puc] == '1' && ge[st_puc] == '1')
                    /* 'if' inside */
                    xfi_e++;
            	st_puc++; 
            
            }}
            /* 'if' begin */
            if (!(xfi_e != 0))
                /* 'if' inside */
                cout << "YES" << '\n';
            else
                cout << "NO" << '\n';
        }
    return 0;
}


