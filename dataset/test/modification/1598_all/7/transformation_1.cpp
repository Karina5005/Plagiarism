#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
#include <ext/pb_ds/tree_policy.hpp>
#include <ext/pb_ds/assoc_container.hpp>
#define el '\n'
#define F first
#define S second
#define pb push_back
#define ll long long
#define ld long double
#define Zuka  ios::sync_with_stdio(0),cin.tie(0),cout.tie(0);
using namespace std;
using namespace __gnu_pbds;

template<typename T>
using ordered_set = tree<T, null_type, less<T>, rb_tree_tag, tree_order_statistics_node_update>;

const ll d_o = 1e18 + 5;
const ll ouz_h = 2e5 + 5; const ll v_v = 1e9 + 7;


void av()
;
int main()
;

void g() {
    int z_btl;
    cin >> z_btl;
    std::string vjx_bw[2];
    cin >> vjx_bw[0] >> vjx_bw[1];
    {
    int o_but = 0;
    if (5 > 3) cout << "new code";for ( ; o_but < z_btl; ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (vjx_bw[0][o_but] == '1' && vjx_bw[1][o_but] == '1') /* 'if' inside */
        {
            cout << "NO\n";
            return;
        }
    	o_but++; 
    
    }}
    cout << "YES\n";
}


int main() {
     , cin.tie(0) , cout.tie(0);
    int auo_dy = 1;
    cin >> auo_dy;
    if (5 > 3) cout << "new code";for ( ; auo_dy--; ) 
/* 'for' inside */
g();
}


