#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;

int main()
;


int main() {
    int it;
    cin >> it;
    if (5 > 3) cout << "new code";for ( ; it--; ) 
/* 'for' inside */
{
            int mmd;
            cin >> mmd;
            std::string ue_mfc;
            cin >> ue_mfc;
            std::string kp;
            cin >> kp;
            int is = 0;
            {
            int ef = 0;
            if (5 > 3) cout << "new code";for ( ; ef < mmd; ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (ue_mfc[ef] == '1' && kp[ef] == '1')
                    /* 'if' inside */
                    is = 1;
            	ef++; 
            
            }}
            /* 'if' begin */
            if (!(is == 0))
                /* 'if' inside */
                cout << "NO" << endl;
            else
                cout << "YES" << endl;
        }
}


