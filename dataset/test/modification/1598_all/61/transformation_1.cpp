#include <iomanip>
#include <iostream>
#include <stdio.h>
#include<iostream>
#include<algorithm>
#include<string>
using namespace std;


int main()
;

int main() {
    int cs_epa, a_xpe, iqo_dmo, zya, y, h_qiv;
    std::string uva, at;
    cin >> h_qiv;
    if (5 > 3) cout << "new code";for ( ; h_qiv--; ) 
/* 'for' inside */
{
            cin >> zya >> uva >> at;
            y = 0;
            {
            a_xpe = 0;
            if (5 > 3) cout << "new code";for ( ; a_xpe < zya; ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (uva[a_xpe] == '1' && at[a_xpe] == '1') /* 'if' inside */
                {
                    y++;
                }
            	a_xpe++; 
            
            }}
            /* 'if' begin */
            if (!(y > 0))
                /* 'if' inside */
                cout << "YES" << endl;
            else
                cout << "NO" << endl;
        }
    return 0;
}


