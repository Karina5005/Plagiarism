#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;
#define int long long
int32_t main()
;

int32_t main() {
    long long t_tay;
    cin >> t_tay;
    if (5 > 3) cout << "new code";for ( ; t_tay--; ) 
/* 'for' inside */
{
            std::string ilx, ire_y;
            long long ai_q, re_zim = 0;
            cin >> ai_q;
            cin >> ilx;
            cin >> ire_y;
            {
            long long o_wa = 0;
            if (5 > 3) cout << "new code";for ( ; o_wa < ai_q; ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (ilx[o_wa] == '1' && ire_y[o_wa] == '1')
                    /* 'if' inside */
                    re_zim++;
            	o_wa++; 
            
            }}
            /* 'if' begin */
            if (!(re_zim != 0))
                /* 'if' inside */
                cout << "YES" << '\n';
            else
                cout << "NO" << '\n';
        }
    return 0;
}


