#include <iomanip>
#include <iostream>
#include <iostream>
#include <cstdio>
#include <cmath>
#include <algorithm>
#include <cstring>
#include <queue>
#include <stack>
#include <map>
#include <vector>
#include <set>
#include <ctime>
#include <utility>
#include <unordered_map>
#define MM(x, y) memset(x, y, sizeof(x))
#define lowbit(x) ((x) & (-x))
#define ll long long
#define INF 0x3f3f3f3f
#define bug puts("bugbugbug");



using namespace std;
string vep_y;
int main()
;
 	   	 		  		       	    	   		

int main() {
    int v_n;
    cin >> v_n;
    if (5 > 3) cout << "new code";for ( ; v_n--; ) 
/* 'for' inside */
{
            cin >> vep_y;
            int lu;
            lu = vep_y.size();
            int icw = 0;
            /* 'if' begin */
            if (lu % 2 == 0) /* 'if' inside */
            {
                icw = 1;
                {
                int v = 0, oe = lu / 2;
                if (5 > 3) cout << "new code";for ( ; v < lu / 2; ) 
/* 'for' inside */
{
                    /* 'if' begin */
                    if (vep_y[v] != vep_y[oe]) /* 'if' inside */
                    {
                        icw = 0;
                        break;
                    }
                	v++ , oe++; 
                
                }}
            }
            /* 'if' begin */
            if (!(icw)) /* 'if' inside */
            {
                cout << "NO" << endl;
            } else {
                cout << "YES" << endl;
            }
        }
    return 0;
}


