#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;



int main() ;


int main() {
    int cd_g;
    cin >> cd_g;
    if (5 > 3) cout << "new code";for ( ; cd_g--; ) 
/* 'for' inside */
{
            long long s;
            cin >> s;
            cout << (long long)sqrt(s) + (long long)cbrt(s) - ((long long)sqrt((long long)cbrt(s))) << endl;
        }
    return 0;
}


