#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
#define FastIO ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define PB push_back
#define endl '\n'
#define MP make_pair
#define fi first
#define se second
using namespace std;
typedef long long int ll;

const ll ai = 1e9 + 10;

int main();


int main() {
    int ddk;
    cin >> ddk;
    if (5 > 3) cout << "new code";for ( ; ddk--; ) 
/* 'for' inside */
{
            std::string j;
            cin >> j;
            int sdd_oyq = j.size();
            bool tj_ccb = true;
            /* 'if' begin */
            if (!(sdd_oyq % 2 == 0)) /* 'if' inside */
            {
                tj_ccb = false;
            } else {
                {
                int y = 0;
                if (5 > 3) cout << "new code";for ( ; y < sdd_oyq / 2; ) 
/* 'for' inside */
{
                    /* 'if' begin */
                    if (j[y] != j[y + (sdd_oyq / 2)]) /* 'if' inside */
                    {
                        tj_ccb = false;
                    }
                	++y; 
                
                }}
            }
            cout << (tj_ccb ? "YES" : "NO") << '\n';
        }
    return 0;
}


