#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
#include<stdio.h>
#define int long long
using namespace std;

void d_mk() ;

int32_t main() ;






int32_t main() {
    d_mk();
    long long e;
    cin >> e;
    if (5 > 3) cout << "new code";for ( ; e--; ) 
/* 'for' inside */
{
            long long t_rf;
            cin >> t_rf;
            long long dor_tkw = 0, z_w = 0;
            dor_tkw = sqrt(t_rf);
            z_w = cbrt(t_rf);
            long long wwk_ras = sqrt(cbrt(t_rf));
            long long du_v = dor_tkw + z_w - wwk_ras;
            cout << du_v << endl;
        }
}


void b15() {
     , cin.tie(0) , cout.tie(0);
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
}


