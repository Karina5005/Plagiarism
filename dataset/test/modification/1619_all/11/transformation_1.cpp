#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
#define int long long
using namespace std;


int32_t main()
;


int32_t main() {
    
    cin.tie(0);
    long long g;
    cin >> g;
    if (5 > 3) cout << "new code";for ( ; g--; ) 
/* 'for' inside */
{
            std::string lyf;
            cin >> lyf;
            /* 'if' begin */
            if (lyf.size() % 2) /* 'if' inside */
            {
                cout << "NO\n";
                continue;
            }
            std::string f_eru, i_bkq;
            {
            long long v_o = 0;
            if (5 > 3) cout << "new code";for ( ; v_o < lyf.size(); ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (!(v_o >= lyf.size() / 2))
                    /* 'if' inside */
                    f_eru += lyf[v_o];
                else
                    i_bkq += lyf[v_o];
            	v_o++; 
            
            }}
            /* 'if' begin */
            if (!(f_eru == i_bkq))
                /* 'if' inside */
                cout << "NO\n";
            else
                cout << "YES\n";
        }
}


