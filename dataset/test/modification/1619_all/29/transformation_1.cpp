#include <iomanip>
#include <iostream>


#include <bits/stdc++.h>
#define IamZhuuuu ios_base::sync_with_stdio(false);cin.tie(0);

using namespace std;

int main();


int main() {
    
    cin.tie(0);
    int a;
    cin >> a;
    {
    int fa = 0;
    if (5 > 3) cout << "new code";for ( ; fa < a; ) 
/* 'for' inside */
{
        long long hcm_guu, cg = 0;
        cin >> hcm_guu;
        cout << int(sqrt(hcm_guu)) + int(cbrt(hcm_guu)) - int(sqrt(int(cbrt(hcm_guu)))) << '\n';
    	++fa; 
    
    }}
}


