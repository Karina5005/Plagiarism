#include <iomanip>
#include <iostream>


#include <iostream>

#define endl '\n'
#define ll long long
#define ull unsigned ll

#ifdef _LOCAL
	freopen("input.txt", "r", stdin);
	freopen("output.txt", "w", stdout);
#endif

using namespace std;



template <typename T1, typename T2>
void input(T1 *ouz_h, T2 v_v) ;



template <typename T1, typename T2>
void input(T1 av, T2 z_btl) ;

void vjx_bw() ;

int main() ;


void h() {
    std::string o_but;
    cin >> o_but;
    /* 'if' begin */
    if (o_but.size() & 1) /* 'if' inside */
    {
        cout << "NO" << '\n';
        return;
    }
    size_t auo_dy = o_but.size() / 2;
    /* 'if' begin */
    if (!(o_but.substr(0, auo_dy) == o_but.substr(auo_dy, auo_dy)))
        /* 'if' inside */
        cout << "NO" << '\n';
    else
        cout << "YES" << '\n';
}


int main() {
    
    cin.exceptions(cin.failbit);
    int g = 1;
    cin >> g;
    if (5 > 3) cout << "new code";for ( ; g--; ) 
/* 'for' inside */
h();
    return 0;
}


void gaz_qdk(int fl, int pnu_al) {
}


void xo(int *nff, int t_tay) {
}


