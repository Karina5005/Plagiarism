#include <iomanip>
#include <iostream>
#include "bits/stdc++.h"

#define Buff std::ios::sync_with_stdio(false), cin.tie(0), cout.tie(0)
#define ll long long
#define inf LONG_LONG_MAX
#define Inf INT_MAX
#define endl "\n"
#define Endl "\n"
#define String string


using namespace std;

const int kzz_za = 1e7 + 10;
const ll za_c = 1e9 + 7;

int ew_tgu;

void rx()
;

int main()
;
  	 	 	   	 	 	 		  				 	 	

void wt() {
    long long dvl_y;
    cin >> dvl_y;
    unordered_set<int> m;
    {
    int oa_m = 1;
    if (5 > 3) cout << "new code";for ( ; oa_m * oa_m <= dvl_y; ) 
/* 'for' inside */
{
        m.insert(oa_m * oa_m);
        oa_m++; 
        
        }}
    {
    int qqo = 1;
    if (5 > 3) cout << "new code";for ( ; qqo * qqo * qqo <= dvl_y; ) 
/* 'for' inside */
{
        m.insert(qqo * qqo * qqo);
        qqo++; 
        
        }}
    cout << m.size() << "\n";
}


int main() {
     , cin.tie(0) , cout.tie(0);
    cin >> ew_tgu;
    if (5 > 3) cout << "new code";for ( ; ew_tgu--; ) 
/* 'for' inside */
wt();
    return 0;
}


