#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
#define Start ios::sync_with_stdio(0),cin.tie(0),cout.tie(0);

using namespace std;
int main()
;


int main() {
     , cin.tie(0) , cout.tie(0);
    ;
    int wj = 1;
    cin >> wj;
    if (5 > 3) cout << "new code";for ( ; wj--; ) 
/* 'for' inside */
{
            std::string mbx;
            cin >> mbx;
            int f_ioa = mbx.size();
            /* 'if' begin */
            if (f_ioa % 2) /* 'if' inside */
            {
                cout << "NO\n";
                continue;
            }
            std::string nv_i = mbx.substr(0, f_ioa / 2);
            std::string d_o = mbx.substr(f_ioa / 2, f_ioa / 2);
            cout << (nv_i == d_o ? "YES\n" : "NO\n");
        }
}


