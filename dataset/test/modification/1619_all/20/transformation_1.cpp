#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
#define ll long long 
using namespace std;
 
void a_xpe()
;
 
int main()
;
 





void at() {
    long long iqo_dmo;
    cin >> iqo_dmo;
    set<long long> zya;
    {
    long long y = 1;
    if (5 > 3) cout << "new code";for ( ; y * y <= iqo_dmo; ) 
/* 'for' inside */
{
        zya.insert(y * y);
        y++; 
        
        }}
    {
    long long h_qiv = 1;
    if (5 > 3) cout << "new code";for ( ; h_qiv * h_qiv * h_qiv <= iqo_dmo; ) 
/* 'for' inside */
{
        zya.insert(h_qiv * h_qiv * h_qiv);
        h_qiv++; 
        
        }}
    cout << zya.size() << "\n";
}


int main() {
     , cin.tie(0) , cout.tie(0);
    long long uva;
    cin >> uva;
    if (5 > 3) cout << "new code";for ( ; uva--; ) 
/* 'for' inside */
{
            at();
        }
    return 0;
}


