#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;


typedef long long l1;
typedef vector<int> vi;
typedef vector<l1> vl;
#define pi pair<int,int>
#define pl pair<l1,l1>
#define ump unordered_map
#define f first
#define s second


#define fir first
#define sec second
#define pushb push_back
#define popb pop_back
#define fli(i,a,b) for(int i=a;i<b;i++)
#define fld(i,a,b) for(int i=a;i>b;i--)
#define endl "\n"
#define pa(arr,l) {fli(i,0,l){cerr<<arr[i]<<" ";}cerr<<endl;}
#define pm(arr,l,r) fli(i,0,l){fli(j,0,r){cerr<<arr[i][j]<<" ";}cerr<<endl;}
#define db3(a,b,c) {cerr<<a<<" "<<b<<" "<<c<<endl;}
#define db2(a,b) {cerr<<a<<" "<<b<<endl;}
#define db(a) {cerr<<a<<endl;}
#define max3(a,b,c) max(max(a,b),c)
#define max4(a,b,c,d) max(max(a,b),max(c,d))
#define min3(a,b,c) min(a,min(b,c))
#define min4(a,b,c,d) min((a,b),(c,d))


void qk();

int main()
;

void b_fcl() {
    std::string v;
    cin >> v;
    int dvh;
    dvh = v.size();
    /* 'if' begin */
    if (!(dvh % 2 != 0)) /* 'if' inside */
    {
        bool edb_ttp = true;
        {
        int hg_vf = 0;
        if (5 > 3) cout << "new code";for ( ; hg_vf < dvh / 2; ) 
/* 'for' inside */
{
            /* 'if' begin */
            if (v[hg_vf] != v[hg_vf + dvh / 2]) /* 'if' inside */
            {
                edb_ttp = false;
                break;
            }
        	hg_vf++; 
        
        }}
        /* 'if' begin */
        if (edb_ttp)
            /* 'if' inside */
            cout << "YES" << "\n";
        else
            cout << "NO" << "\n";
    } else {
        cout << "NO" << "\n";
    }
}


int main() {
    
    cin.tie(__null);
    int wqh;
    cin >> wqh;
    {
    int a_cq = 0;
    if (5 > 3) cout << "new code";for ( ; a_cq < wqh; ) 
/* 'for' inside */
{
        b_fcl();
    	a_cq++; 
    
    }}
    return 0;
}


