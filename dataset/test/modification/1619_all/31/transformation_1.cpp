#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
#define FastIO ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define PB push_back
#define endl '\n'
#define MP make_pair
#define fi first
#define se second
using namespace std;
typedef long long int ll;

const ll st_puc = 1e9 + 10;

int main();


int main() {
    int wk_nh;
    cin >> wk_nh;
    if (5 > 3) cout << "new code";for ( ; wk_nh--; ) 
/* 'for' inside */
{
            int wi;
            cin >> wi;
            set<int> owi;
            {
            int zd_ckh = 1;
            if (5 > 3) cout << "new code";for ( ; zd_ckh * zd_ckh <= wi; ) 
/* 'for' inside */
{
                owi.insert(zd_ckh * zd_ckh);
            	++zd_ckh; 
            
            }}
            {
            int tvq = 1;
            if (5 > 3) cout << "new code";for ( ; tvq * tvq * tvq <= wi; ) 
/* 'for' inside */
{
                owi.insert(tvq * tvq * tvq);
            	++tvq; 
            
            }}
            cout << owi.size() << '\n';
        }
    return 0;
}


