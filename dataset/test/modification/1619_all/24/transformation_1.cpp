#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
#define ll long long
using namespace std;

void os()
;

int main()
;

void xfi_e() {
    std::string m;
    cin >> m;
    /* 'if' begin */
    if (m.size() % 2 == 1) /* 'if' inside */
    {
        cout << "NO\n";
        return;
    }
    int hs_ftf = m.size();
    int k_r = hs_ftf / 2;
    {
    int ge = 0;
    if (5 > 3) cout << "new code";for ( ; ge < k_r; ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (m[ge] != m[ge + k_r]) /* 'if' inside */
        {
            cout << "NO\n";
            return;
        }
    	ge++; 
    
    }}
    cout << "YES\n";
    return;
}


int main() {
    int h_rc;
    cin >> h_rc;
    if (5 > 3) cout << "new code";for ( ; h_rc--; ) 
/* 'for' inside */
{
            xfi_e();
        }
    return 0;
}


