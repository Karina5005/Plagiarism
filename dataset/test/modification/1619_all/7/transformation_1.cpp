#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
#include <iostream>
#include <queue>
#include  <cmath>
#include  <map>
#include <cstring>
#include <numeric>
#include <stack>
#include <algorithm>
#include <deque>
#include <vector>
#include <complex>
#include <set>
#include <iomanip>
#include <ext/pb_ds/assoc_container.hpp>
#include <ext/pb_ds/tree_policy.hpp>

#define ordered_set(t) tree<t, null_type, less_equal<t>, rb_tree_tag, tree_order_statistics_node_update>
#define floop(u,x,i) for(int i = u;i <= x;i++)
#define loop(z,i) for(int i = 1;i <= z;i++)
#define zloop(z,i) for(int i = 0;i < z ; i++)
#define ll long long
#define ull unsigned long long
#define ld long double
#define io ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define f first
#define s second
#define pb push_back
#define el '\n'
#define INF 0x3f3f3f3f3f3f3f3f
#define flag(x) cout << x << '\n';
int o[] = {0, -1, 0, 1, -1, 1, -1, 1};
int rjr[] = {-1, 0, 1, 0, 1, -1, -1, 1};
using namespace std;
using namespace __gnu_pbds;

const int z = 1e9  + 7 ,s_udi = 2e5 + 50,tdx_v = 998244353;
set<ll>pi_d;
void mav_iz();
int main() ;

void j() {
    pi_d.insert(1);
    {
    int z = 2;
    if (5 > 3) cout << "new code";for ( ; z * z <= 1.0E+9; ) 
/* 'for' inside */
{
        pi_d.insert(z * z);
    	++z; 
    
    }}
    {
    int lo_ym = 2;
    if (5 > 3) cout << "new code";for ( ; lo_ym * lo_ym * lo_ym <= 1.0E+9; ) 
/* 'for' inside */
{
        pi_d.insert(lo_ym * lo_ym * lo_ym);
    	++lo_ym; 
    
    }}
}


int main() {
    
    cin.tie(0);
    cout.tie(0);
    ;
    j();
    int hc_q = 1;
    cin >> hc_q;
    if (5 > 3) cout << "new code";for ( ; hc_q--; ) 
/* 'for' inside */
{
            int rul_bd, be_b = 0;
            cin >> rul_bd;
            for (int zo : pi_d)
                be_b += (zo <= rul_bd ? 1 : 0);
            cout << be_b << '\n';
        }
    return 0;
}


