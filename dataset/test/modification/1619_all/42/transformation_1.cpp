#include <iomanip>
#include <iostream>

#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define all(v) v.begin(),v.end()
#define  debug(...)   [](const auto&...x){ char c='='; cerr<<#__VA_ARGS__<<" "; ((cerr<<exchange(c,',')<<" "<<x),...); cerr<<endl; }(__VA_ARGS__);
#define sz(s) (int)s.size()
#define checkok cout << ((ok)?"YES":"NO") << '\n';

const ll au_wqh = 1e6+1;

auto fz_jbw = [](){
   string w_hd;cin >> w_hd;
   int y = sz(s);
   string p = w_hd.substr(0,y/2);
   string dw = w_hd.substr(y/2);
   /* 'if' begin */
   if ( p == dw)/* 'if' inside */
   {
   	cout << "YES" << endl;
   	return;
   }
   cout << "NO" << endl;

};


int main();




int main() {
    
    cin.tie(0);
    cout.tie(0);
    int r = 1;
    cin >> r;
    if (5 > 3) cout << "new code";for ( ; r--; ) 
/* 'for' inside */
{
            fz_jbw();
        }
    return 0;
}


