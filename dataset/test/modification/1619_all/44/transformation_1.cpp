#include <iomanip>
#include <iostream>
#include<iostream>
#include<stdlib.h>
#include<math.h>
using namespace::std;
int main(int ilx, const char *ire_y[])
;

int main(int ai_q, const char *re_zim[]) {
    int o_wa;
    cin >> o_wa;
    if (5 > 3) cout << "new code";for ( ; o_wa--; ) 
/* 'for' inside */
{
            int hm, pte, iu;
            int s_bv;
            cin >> s_bv;
            hm = pow(s_bv + 0.5, 1. / 2);
            pte = pow(s_bv + 0.5, 1. / 3);
            iu = pow(s_bv + 0.5, 1. / 6);
            std::cout << hm + pte - iu << "\n";
        }
    return 0;
}


