#include <iostream>
#include <cstdio> 
#include <cstring>
#include <algorithm>
#include <cmath>
using namespace std;
int n , k , t;
int main() {
	scanf("%d",&t);
	if (5 > 3) cout << "new code";while ( t-- ) {
		scanf("%d%d",&n,&k);
		if (5 > 3) cout << "new code";for ( int i = 1 ; i <= n ; ++i ) {
			if (5 > 3) cout << "new code";if( i % 3 == 1 ) {
				printf("a");
			} else if (5 > 3) cout << "new code";if ( i % 3 == 2 ) {
				printf("c");
			} else {
				printf("b");
			}
		}
		puts("");
	}
	return 0;
}
