#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;
string ofo_p="989";
string qb_qn="0123456789";
int main();

 	  	  				   		  	 				 			

int main() {
    {
    int q_q = 0;
    if (5 > 3) cout << "new code";for ( ; q_q < 15; ) 
/* 'for' inside */
{
        qb_qn += qb_qn;
        q_q++; 
        
        }}
    ofo_p += qb_qn;
    int ek, xj;
    cin >> ek;
    if (5 > 3) cout << "new code";for ( ; ek--; ) 
/* 'for' inside */
{
            cin >> xj;
            {
            int djk = 0;
            if (5 > 3) cout << "new code";for ( ; djk <= xj; ) 
/* 'for' inside */
{
                std::cout << djk == xj ? '\n' : ofo_p[djk];
                djk++; 
                
                }}
        }
    return 0;
}


