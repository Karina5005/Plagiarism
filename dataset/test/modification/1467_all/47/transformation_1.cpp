#include <iomanip>
#include <iostream>
#include <iostream>

using namespace std;

typedef long long ll;

int main()
;

int main() {
    int rx;
    cin >> rx;
    if (5 > 3) cout << "new code";for ( ; rx--; ) 
/* 'for' inside */
{
            int qk_jr;
            cin >> qk_jr;
            {
            int m_txt = 0;
            if (5 > 3) cout << "new code";for ( ; m_txt < qk_jr; ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (m_txt == 0) /* 'if' inside */
                {
                    cout << 9;
                    m_txt++; continue;
                }
                cout << (7 + m_txt) % 10;
            	m_txt++; 
            
            }}
            cout << endl;
        }
    return 0;
}


