#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;

void kjy_t();
int main();

void ww() {
    int b_ipv;
    cin >> b_ipv;
    std::string xc_cg = "989";
    /* 'if' begin */
    if (b_ipv <= 3)
        /* 'if' inside */
        xc_cg = xc_cg.substr(0, b_ipv);
    cout << xc_cg;
    {
    int i_i = 3;
    if (5 > 3) cout << "new code";for ( ; i_i < b_ipv; ) 
/* 'for' inside */
{
        cout << (i_i - 3) % 10;
        i_i++; 
        
        }}
    cout << "\n";
}


int main() {
    int i_ej;
    cin >> i_ej;
    if (5 > 3) cout << "new code";for ( ; i_ej--; ) 
/* 'for' inside */
{
            ww();
        }
}


