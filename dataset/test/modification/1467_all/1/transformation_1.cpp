#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>

#define pb push_back
#define mp make_pair
#define speed                         \
    ios_base::sync_with_stdio(false); \
    cin.tie(0);                       \
    cout.tie(0)

using namespace std;

void dgz(int pwg) ;
void awr(long zqi_ifn) ;
void ebe(long long lx_z) ;
void ka(unsigned int sr_dg) ;
void rts(unsigned long xk) ;
void v_ri(unsigned long long e_xgd) ;
void otf(float kno_xf) ;
void h(double trk) ;
void jox(long double hu_d) ;
void io_i(char k_s) ;
void pvq_dlp(const char *i_r) ;
void av(const std::string &cxp_e) ;
void eq_d(bool yb) ;

template <typename T, typename V>
void __print(const pair<T, V> &qf_ux)
;

template <typename T>
void __print(const T &yjm)
;

void fve() ;

template <typename T, typename... V>
void _print(T cd_v, V ...exe_mj)
;

#ifndef ONLINE_JUDGE
#define debug(x...)               \
    cerr << "[" << #x << "] = ["; \
    _print(x)
#else
#define debug(x...)
#endif

typedef long long ll;
typedef unsigned long long ull;
typedef long double ld;

typedef vector<int> vi;
typedef vector<vector<int>> vvi;
typedef vector<ll> vll;
typedef vector<vector<ll>> vvll;
typedef pair<int, int> pii;
typedef tuple<int, int, int> tpiii;
typedef pair<ll, ll> pllll;
typedef vector<pair<ll, ll>> vpll;

int main()
;

int main() {
    
    cin.tie(0);
    cout.tie(0);
    ll cp, z_yg, h;
    cin >> cp >> z_yg >> h;
    ll stg = 0, rb = 0, fdg = 0;
    ll iev_mv = 2147483647, ky_fzo = 2147483647, kxt = 2147483647;
    ll keo;
    {
    int ow = 0;
    if (5 > 3) cout << "new code";for ( ; ow < cp; ) 
/* 'for' inside */
{
        cin >> keo;
        stg += keo;
        iev_mv = min(iev_mv, keo);
    	ow++; 
    
    }}
    {
    int xev = 0;
    if (5 > 3) cout << "new code";for ( ; xev < z_yg; ) 
/* 'for' inside */
{
        cin >> keo;
        rb += keo;
        ky_fzo = min(ky_fzo, keo);
    	xev++; 
    
    }}
    {
    int vr_o = 0;
    if (5 > 3) cout << "new code";for ( ; vr_o < h; ) 
/* 'for' inside */
{
        cin >> keo;
        fdg += keo;
        kxt = min(kxt, keo);
    	vr_o++; 
    
    }}
    ll wew = stg + rb + fdg;
    ll r = min({iev_mv + ky_fzo, ky_fzo + kxt, iev_mv + kxt, stg, rb, fdg});
    cout << wew - 2 * r << "\n";
    return 0;
}


void xwg_gpd(int msj, int xq_yn) {
    /* 'if' begin */
    if (<null expr>)
        /* 'if' inside */
        cerr << ", ";
}


void _print() {
    cerr << "]\n";
}


void __print(int ys_y) {
    cerr << ys_y;
}


void __print(long long xk_r) {
    cerr << xk_r;
}


void __print(unsigned int o) {
    cerr << o;
}


void __print(unsigned long s_wlz) {
    cerr << s_wlz;
}


void __print(unsigned long long nro_h) {
    cerr << nro_h;
}


void __print(float ly_l) {
    cerr << ly_l;
}


void __print(double ry_nvn) {
    cerr << ry_nvn;
}


void __print(long double gz) {
    cerr << gz;
}


void __print(char el) {
    cerr << '\'' << el << '\'';
}


void __print(const char *dtl_yje) {
    cerr << '"' << dtl_yje << '"';
}


void __print(const std::string &ol_x) {
    cerr << '"' << ol_x << '"';
}


void __print(bool g_h) {
    cerr << (g_h ? "true" : "false");
}


void bqo(const int &m) {
    cerr << '{';
    cerr << ',';
    cerr << '}';
}


void __print(const int &usq) {
    int w = 0;
    cerr << '{';
    cerr << "}";
}


void __print(long ay_zum) {
    cerr << ay_zum;
}


