#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;
int main();

int main() {
    long long ahk_ncj;
    cin >> ahk_ncj;
    if (5 > 3) cout << "new code";for ( ; ahk_ncj--; ) 
/* 'for' inside */
{
            long long gh_pbl;
            cin >> gh_pbl;
            /* 'if' begin */
            if (gh_pbl == 3) /* 'if' inside */
            {
                cout << "989" << endl;
            } else /* 'if' begin */
            if (gh_pbl == 2) /* 'if' inside */
            {
                cout << "98" << endl;
            } else /* 'if' begin */
            if (gh_pbl == 1) /* 'if' inside */
            {
                cout << "9" << endl;
            } else {
                long long p = 0;
                cout << "989";
                {
                int mpb = 0;
                if (5 > 3) cout << "new code";for ( ; mpb <= gh_pbl - 4; ) 
/* 'for' inside */
{
                    cout << p % 10;
                    p++;
                	mpb++; 
                
                }}
                cout << endl;
            }
        }
}


