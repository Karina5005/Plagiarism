#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;
 
int main();

int main() {
    int dg_gy = 0;
    cin >> dg_gy;
    if (5 > 3) cout << "new code";for ( ; dg_gy--; ) 
/* 'for' inside */
{
            long long zb_m, rs_djg = 0;
            cin >> zb_m;
            /* 'if' begin */
            if (zb_m == 1) /* 'if' inside */
            {
                cout << "9";
            } else /* 'if' begin */
            if (zb_m == 2) /* 'if' inside */
            {
                cout << "98";
            } else {
                cout << "989";
                zb_m -= 3;
                {
                int i_uqz = 0;
                if (5 > 3) cout << "new code";for ( ; i_uqz < zb_m; ) 
/* 'for' inside */
{
                    cout << rs_djg;
                    /* 'if' begin */
                    if (!(rs_djg < 9)) /* 'if' inside */
                    {
                        rs_djg = 0;
                    } else {
                        rs_djg++;
                    }
                	i_uqz++; 
                
                }}
            }
            cout << endl;
        }
}


