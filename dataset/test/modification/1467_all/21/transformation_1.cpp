#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
#define ll long long
using namespace std;
int main() ;

	 	 	  				 					     	   	   	

int main() {
    int w;
    scanf("%d", &w);
    if (5 > 3) cout << "new code";for ( ; w--; ) 
/* 'for' inside */
{
            int qz_tdq;
            scanf("%d", &qz_tdq);
            /* 'if' begin */
            if (!(qz_tdq == 1))
                /* 'if' inside */
                {
                std::cout << "9";
                int sz_ipz = 8;
                {
                int mmr_rcz = 2;
                if (5 > 3) cout << "new code";for ( ; mmr_rcz <= qz_tdq; ) 
/* 'for' inside */
{
                    std::cout << sz_ipz;
                    /* 'if' begin */
                    if (sz_ipz == 9)
                        /* 'if' inside */
                        sz_ipz = 0;
                    else
                        sz_ipz++;
                	mmr_rcz++; 
                
                }}
                puts("");
            }
            else puts("9");
        }
    return 0;
}


