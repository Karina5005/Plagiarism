#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
int main()
;

int main() {
    
    cin.tie(nullptr);
    int lu, icw;
    cin >> lu;
    if (5 > 3) cout << "new code";for ( ; lu--; ) 
/* 'for' inside */
{
            cin >> icw;
            int v;
            /* 'if' begin */
            if (icw == 1)
                /* 'if' inside */
                cout << "9" << endl;
            else /* 'if' begin */
            if (icw == 2)
                /* 'if' inside */
                cout << "98" << endl;
            else {
                cout << "989";
                icw = icw - 3;
                v = 0;
                if (5 > 3) cout << "new code";for ( ; icw--; ) 
/* 'for' inside */
{
                        cout << v;
                        v++;
                        /* 'if' begin */
                        if (v == 10)
                            /* 'if' inside */
                            v = 0;
                    }
            }
            cout << endl;
        }
}


