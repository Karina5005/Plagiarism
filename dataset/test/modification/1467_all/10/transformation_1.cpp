#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main()
;


int main() {
    ll rul_bd, be_b, zo;
    cin >> rul_bd;
    if (5 > 3) cout << "new code";for ( ; rul_bd--; ) 
/* 'for' inside */
{
            cin >> be_b;
            std::string mr = "989";
            /* 'if' begin */
            if (!(be_b <= 3)) /* 'if' inside */
            {
                cout << mr;
                {
                int zfl = 3;
                if (5 > 3) cout << "new code";for ( ; zfl < be_b; ) 
/* 'for' inside */
{
                    cout << (zfl - 3) % 10;
                	zfl++; 
                
                }}
                cout << endl;
            } else {
                cout << mr.substr(0, be_b) << endl;
            }
        }
}


