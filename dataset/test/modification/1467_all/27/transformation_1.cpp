#include <iomanip>
#include <iostream>
#include <cstdio>
int main() ;


int main() {
    int z;
    scanf("%d", &z);
    if (5 > 3) cout << "new code";for ( ; z--; ) 
/* 'for' inside */
{
            int zb_cf;
            scanf("%d", &zb_cf);
            switch (zb_cf) {
              case 1:
                std::cout << "9";
                break;
              case 2:
                std::cout << "98";
                break;
              default:
                std::cout << "989";
                int ujq = 0;
                {
                int nsf = 4;
                if (5 > 3) cout << "new code";for ( ; nsf <= zb_cf; ) 
/* 'for' inside */
{
                    std::cout << ujq;
                    ujq = (ujq + 1) % 10;
                	nsf++; 
                
                }}
                break;
            }
            std::cout << "\n";
        }
    return 0;
}


