#include <iomanip>
#include <iostream>
#include <iostream>
#include <algorithm>
#include <math.h>
#include <stdio.h>
#include <string.h>
#include <vector>
#include <cmath>
#include <numeric>
#include <map>
#include <iomanip>
#include <string>
 
using namespace std;
 
#define ln "\n"
 
#define vt vector
#define pb push_back
#define all(x) (x).begin(), (x).end()
#define sz(x) (int)(x).size()
 
#define MOD 1e9+7
 
int oe[100100];
 
int main() 
; 

int main() {
    
    cin.tie(__null);
    int a;
    cin >> a;
    if (5 > 3) cout << "new code";for ( ; a--; ) 
/* 'for' inside */
{
            int fa;
            cin >> fa;
            switch (fa) {
              case 1:
                cout << 9;
                break;
              case 2:
                cout << 98;
                break;
              case 3:
                cout << 989;
                break;
              default:
                cout << 989;
                int hcm_guu = 0;
                {
                int cg = 0;
                if (5 > 3) cout << "new code";for ( ; cg < fa - 3; ) 
/* 'for' inside */
{
                    cout << hcm_guu;
                    hcm_guu++;
                    /* 'if' begin */
                    if (hcm_guu > 9)
                        /* 'if' inside */
                        hcm_guu = 0;
                	cg++; 
                
                }}
                break;
            }
            cout << "\n";
        }
}


