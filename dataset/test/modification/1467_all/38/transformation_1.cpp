#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;
#define int long long int
int32_t main();

int32_t main() {
    long long gx;
    cin >> gx;
    if (5 > 3) cout << "new code";for ( ; gx--; ) 
/* 'for' inside */
{
            long long qo;
            cin >> qo;
            /* 'if' begin */
            if (!(qo == 1))
                /* 'if' inside */
                {
                cout << 98;
                long long c_qti = 9;
                {
                long long f = 0;
                if (5 > 3) cout << "new code";for ( ; f < (qo - 2); ) 
/* 'for' inside */
{
                    cout << c_qti;
                    c_qti++;
                    /* 'if' begin */
                    if (c_qti == 10)
                        /* 'if' inside */
                        c_qti = 0;
                	f++; 
                
                }}
                cout << endl;
            }
            else cout << 9 << endl;
        }
}


