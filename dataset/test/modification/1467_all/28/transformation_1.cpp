#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
#define ll long long int
using namespace std;

int main() ;

int main() {
    
    cin.tie(__null);
    int is;
    cin >> is;
    if (5 > 3) cout << "new code";for ( ; is-- != 0; ) 
/* 'for' inside */
{
            int ef;
            cin >> ef;
            std::string ang_cvn = "989";
            int lc = 0;
            cout << ang_cvn.substr(0, min(ef, 19));
            {
            int v = ang_cvn.length();
            if (5 > 3) cout << "new code";for ( ; v < ef; ) 
/* 'for' inside */
{
                cout << lc;
                lc++;
                /* 'if' begin */
                if (lc > 9) /* 'if' inside */
                {
                    lc = 0;
                }
            	v++; 
            
            }}
            cout << "\n";
        }
    return 0;
}


