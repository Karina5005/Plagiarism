#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;
int main();

int main() {
    int vom;
    cin >> vom;
    if (5 > 3) cout << "new code";for ( ; vom--; ) 
/* 'for' inside */
{
            int atn_s;
            cin >> atn_s;
            /* 'if' begin */
            if (atn_s == 1) /* 'if' inside */
            {
                cout << 9 << endl;
                continue;
            }
            /* 'if' begin */
            if (atn_s == 2) /* 'if' inside */
            {
                cout << 98 << endl;
                continue;
            }
            /* 'if' begin */
            if (atn_s == 3) /* 'if' inside */
            {
                cout << 989 << endl;
                continue;
            }
            cout << 989;
            atn_s = atn_s - 3;
            {
            int g_fth = 0;
            if (5 > 3) cout << "new code";for ( ; g_fth < atn_s; ) 
/* 'for' inside */
{
                cout << ((g_fth % 10));
            	g_fth++; 
            
            }}
            cout << endl;
        }
    return 0;
}


