#include <iomanip>
#include <iostream>
#include<bits/stdc++.h>
using namespace std;

int main();
		   		  	 				  	   		  	    	

int main() {
    int it, mmd;
    char ue_mfc;
    scanf("%d", &it);
    if (5 > 3) cout << "new code";for ( ; it--; ) 
/* 'for' inside */
{
            scanf("%d", &mmd);
            ue_mfc = '8';
            std::cout << "9";
            {
            int kp = 2;
            if (5 > 3) cout << "new code";for ( ; kp <= mmd; ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (ue_mfc > '9')
                    /* 'if' inside */
                    ue_mfc = '0';
                std::cout << ue_mfc++;
            	kp++; 
            
            }}
            puts("");
        }
    return 0;
}


