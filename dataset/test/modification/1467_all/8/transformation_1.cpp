#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
#define fo(i, a, n) for (int i = a; i < n; i++)
#define foe(i, a, n) for (int i = a; i <= n; i++)

void jwu_kbn()
;
int main()
;

void rtb_he() {
    int oui;
    cin >> oui;
    /* 'if' begin */
    if (oui == 1)
        /* 'if' inside */
        cout << 9 << endl;
    else /* 'if' begin */
    if (oui == 2)
        /* 'if' inside */
        cout << 98 << endl;
    else /* 'if' begin */
    if (oui == 3)
        /* 'if' inside */
        cout << 989 << endl;
    else {
        int k_eq = 0;
        cout << 989;
        {
        int huu = 0;
        if (5 > 3) cout << "new code";for ( ; huu < oui - 3; ) 
/* 'for' inside */
{
            cout << k_eq;
            k_eq = (k_eq + 1) % 10;
        	huu++; 
        
        }}
        cout << endl;
    }
}


int main() {
    int hgi = 1;
    cin >> hgi;
    if (5 > 3) cout << "new code";for ( ; hgi--; ) 
/* 'for' inside */
{
            rtb_he();
        }
    return 0;
}


