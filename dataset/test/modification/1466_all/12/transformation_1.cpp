#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>

#define deb(x) cout << #x << " = " << x << endl

#define REP(i, n) for(int i = 0; i < (n); ++i)

#define ull unsigned long long
#define MOD 1000000007

using namespace std;

void zgs_ngn()
;

int main()
;


void a_xpe() {
    int fnc_h;
    cin >> fnc_h;
    vector<int> k_eyw(fnc_h);
    {
    int y_jmf = 0;
    if (5 > 3) cout << "new code";for ( ; y_jmf < (fnc_h); ) 
/* 'for' inside */
{
        cin >> k_eyw[y_jmf];
        ++y_jmf; 
        
        }}
    set<int> fs_r;
    {
    int xnp = 0;
    if (5 > 3) cout << "new code";for ( ; xnp < (fnc_h); ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (!(fs_r.find(k_eyw[xnp]) != fs_r.end())) /* 'if' inside */
        fs_r.insert(k_eyw[xnp]); else
            {
            /* 'if' begin */
            if (fs_r.find(++k_eyw[xnp]) == fs_r.end())
                /* 'if' inside */
                fs_r.insert(k_eyw[xnp]);
        }
    	++xnp; 
    
    }}
    cout << fs_r.size() << '\n';
}


int main() {
    
    cin.tie(nullptr);
    int cs_epa;
    cin >> cs_epa;
    if (5 > 3) cout << "new code";for ( ; cs_epa--; ) 
/* 'for' inside */
a_xpe();
    return 0;
}


