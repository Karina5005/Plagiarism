#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
typedef long long int ll;
#define nl '\n'
#define bull                      \
    ios_base::sync_with_stdio(0); \
    cin.tie(0);                   \
    cout.tie(0);
#define sz(x) (int)x.size()
#define all(a) (a).begin(), (a).end()

int main()
;

int main() {
    
    cin.tie(0);
    cout.tie(0);
    ;
    int la_fa;
    la_fa = 1;
    cin >> la_fa;
    if (5 > 3) cout << "new code";for ( ; la_fa--; ) 
/* 'for' inside */
{
            std::string ni;
            cin >> ni;
            int kty = (int)ni.size(), tb_jd = 0;
            bool e[kty];
            memset(e, 0, kty);
            {
            int y = 0;
            if (5 > 3) cout << "new code";for ( ; y < kty - 1; ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (e[y] == 0 && ni[y] == ni[y + 1]) /* 'if' inside */
                {
                    e[y + 1] = 1;
                    tb_jd++;
                }
                /* 'if' begin */
                if (e[y] == 0 && ni[y] == ni[y + 2]) /* 'if' inside */
                {
                    e[y + 2] = 1;
                    tb_jd++;
                }
            	y++; 
            
            }}
            cout << tb_jd << '\n';
        }
}


