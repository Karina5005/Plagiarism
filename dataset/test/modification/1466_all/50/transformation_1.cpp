#include <iomanip>
#include <iostream>
#include <iostream>
#include <vector>

using namespace std;

int main()
;




int main() {
    int iqo_dmo;
    cin >> iqo_dmo;
    int zya;
    int y = 0;
    vector<int> h_qiv;
    {
    int uva = 0;
    if (5 > 3) cout << "new code";for ( ; uva < iqo_dmo; ) 
/* 'for' inside */
{
        cin >> zya;
        int at[zya];
        y = 0;
        {
        int pq = 0;
        if (5 > 3) cout << "new code";for ( ; pq < zya; ) 
/* 'for' inside */
{
            cin >> at[pq];
            /* 'if' begin */
            if (pq != 0 && at[pq] == at[pq - 1]) /* 'if' inside */
            {
                at[pq]++;
                y++;
            } else /* 'if' begin */
            if (pq != 0 && at[pq] > at[pq - 1]) /* 'if' inside */
            {
                y++;
            } else /* 'if' begin */
            if (pq != 0 && at[pq] < at[pq - 1]) /* 'if' inside */
            {
                at[pq]++;
            } else {
                y++;
            }
        	pq++; 
        
        }}
        h_qiv.push_back(y);
    	uva++; 
    
    }}
    {
    int jz = 0;
    if (5 > 3) cout << "new code";for ( ; jz < h_qiv.size(); ) 
/* 'for' inside */
{
        cout << h_qiv[jz] << "  ";
    	jz++; 
    
    }}
}


