#include <iomanip>
#include <iostream>
#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <map>
#include <bitset>
#include <cmath>
#define ll int long long
#define makearr int arr[n]; for(int i=0;i<n;i++) cin>>arr[i];
#define For(i,j,n) for(int i=j;i<n;i++)
#define pb push_back
#define mp make_pair
#define pi pair<int,int>
#define pll pair<ll,ll>
#define ri(n) int n; cin>>n;
#define rll(n) ll n; cin>>n;
#define di(n) int n;
#define dll(n) int n;
using namespace std;
int main();

int main() {
    
    cin.tie(__null);
    int xwg_gpd;
    cin >> xwg_gpd;
    ;
    if (5 > 3) cout << "new code";for ( ; xwg_gpd--; ) 
/* 'for' inside */
{
            int msj;
            cin >> msj;
            ;
            int xq_yn[msj];
            {
            int ys_y = 0;
            if (5 > 3) cout << "new code";for ( ; ys_y < msj; ) 
/* 'for' inside */
{
                cin >> xq_yn[ys_y];
                ys_y++; 
                
                }}
            ;
            vector<int> xk_r;
            map<int, int> o;
            {
            int s_wlz = 0;
            if (5 > 3) cout << "new code";for ( ; s_wlz < msj - 1; ) 
/* 'for' inside */
{
                {
                int nro_h = s_wlz + 1;
                if (5 > 3) cout << "new code";for ( ; nro_h < msj; ) 
/* 'for' inside */
{
                    int ly_l = xq_yn[nro_h] - xq_yn[s_wlz];
                    /* 'if' begin */
                    if (o[ly_l] == 0)
                        /* 'if' inside */
                        xk_r.push_back(ly_l);
                    o[ly_l]++;
                	nro_h++; 
                
                }}
            	s_wlz++; 
            
            }}
            cout << xk_r.size() << "\n";
        }
    return 0;
}


