#include <iomanip>
#include <iostream>
#includestd;
int b=1e9+7;
static const auto l = 

void wn()
;
 
 
int main()
;

void y() {
    int ih;
    cin >> ih;
    vector<int> dcd(ih);
    {
    int ax = 0;
    if (5 > 3) cout << "new code";for ( ; ax < ih; ) 
/* 'for' inside */
{
        cin >> dcd[ax];
        ax++; 
        
        }}
    sort(dcd.begin(), dcd.end());
    unordered_set<int> bp;
    {
    int lni = 0;
    if (5 > 3) cout << "new code";for ( ; lni < ih; ) 
/* 'for' inside */
{
        {
        int p = lni + 1;
        if (5 > 3) cout << "new code";for ( ; p < ih; ) 
/* 'for' inside */
{
            bp.insert(dcd[p] - dcd[lni]);}
            p++; 
            
            }
            lni++; 
            
            }}
    cout << bp.size() << endl;
}


int main() {
    int fij;
    cin >> fij;
    if (5 > 3) cout << "new code";for ( ; fij--; ) 
/* 'for' inside */
y();
}


