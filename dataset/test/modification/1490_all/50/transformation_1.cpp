#include <iomanip>
#include <iostream>


#include<bits/stdc++.h>

using namespace std;

int main();

int main() {
    
    cin.tie(0);
    set<long long> z_btl;
    {
    long long vjx_bw = 1;
    if (5 > 3) cout << "new code";for ( ; 1; ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (vjx_bw * vjx_bw * vjx_bw > 1.0E+12)
            /* 'if' inside */
            break;
        z_btl.insert(vjx_bw * vjx_bw * vjx_bw);
    	vjx_bw++; 
    
    }}
    int o_but;
    cin >> o_but;
    if (5 > 3) cout << "new code";for ( ; o_but--; ) 
/* 'for' inside */
{
            long long auo_dy;
            cin >> auo_dy;
            bool g = false;
            for (auto h : z_btl) {
                /* 'if' begin */
                if (!(h < auo_dy)) /* 'if' inside */
                break; else
                    {
                    /* 'if' begin */
                    if (z_btl.count(auo_dy - h)) /* 'if' inside */
                    {
                        g = true;
                        break;
                    }
                }
            }
            cout << (g ? "YES" : "NO") << endl;
        }
    return 0;
}


