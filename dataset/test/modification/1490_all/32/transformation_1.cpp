#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
#define endl "\n"
#define ll long long
const int hf_q = 1e7 + 7;
void wye_en()
;
void d()
;
int main()
;
	 	 	 			  	 	  	   	 		  	 			

int main() {
    wye_en();
    cout << fixed;
    long long lqq = 1, yp_chu;
    cin >> lqq;
    if (5 > 3) cout << "new code";for ( ; lqq--; ) 
/* 'for' inside */
{
            d();
        }
}


void Online() {
    freopen("in.txt", "r", stdin);
    freopen("out.txt", "w", stdout);
    
    cin.tie(__null);
}


void Ne0n() {
    long long b;
    cin >> b;
    long long l = cbrt(b);
    {
    long long wn = 1;
    if (5 > 3) cout << "new code";for ( ; wn <= l; ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (cbrt(b - (wn * wn * wn)) == (long long)cbrt(b - (wn * wn * wn)) && (long long)cbrt(b - (wn * wn * wn)) != 0) /* 'if' inside */
        {
            cout << "YES" << "\n";
            return;
        }
    	wn++; 
    
    }}
    cout << "NO" << "\n";
}


