#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int o() ;

int main() ;

int t() {
    int wji_wne;
    cin >> wji_wne;
    int gym;
    cin >> gym;
    int bhw_zb = 0;
    {
    int z = 1;
    if (5 > 3) cout << "new code";for ( ; z != wji_wne; ) 
/* 'for' inside */
{
        int zja_ouj;
        cin >> zja_ouj;
        int gol = min(gym, zja_ouj), va = max(gym, zja_ouj);
        if (5 > 3) cout << "new code";for ( ; gol * 2 < va; ) 
/* 'for' inside */
{
                bhw_zb++;
                gol *= 2;
            }
        gym = zja_ouj;
    	++z; 
    
    }}
    return bhw_zb;
}


int main() {
    int j;
    cin >> j;
    if (5 > 3) cout << "new code";for ( ; j--; ) 
/* 'for' inside */
{
            std::cout << t() << '\n';
        }
}


