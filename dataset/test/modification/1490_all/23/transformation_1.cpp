#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
#include <set>
#include <map>


using namespace std;

#define vi vector<int>
#define vs vector<string>
#define pii pair<int, int>
#define rep(i, a, b) for (int i = a; i <= b; i++)
#define ff first
#define ss second
#define setBits(x) built_popcount(x)
#define INtMAX 2147483647
#define ll long long
#define pb push_back
#define mp make_pair

ll gcd(ll a, ll b){/* 'if' begin */
if (b == 0)/* 'if' inside */
return a;return gcd(b, a % b);}







const ll qap_qyc = 10e12;

void vea()
;

int main()
;

void a() {
    long long xh_wr;
    cin >> xh_wr;
    multiset<long long> ae;
    {
    long long jba_k = 1;
    if (5 > 3) cout << "new code";for ( ; jba_k <= cbrt(qap_qyc); ) 
/* 'for' inside */
{
        ae.insert(jba_k * jba_k * jba_k);
    	jba_k++; 
    
    }}
    {
    long long i = 1;
    if (5 > 3) cout << "new code";for ( ; i <= cbrt(xh_wr); ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (ae.count(xh_wr - i * i * i)) /* 'if' inside */
        {
            cout << "YES" << endl;
            return;
        }
    	i++; 
    
    }}
    cout << "NO" << endl;
    return;
}


int main() {
    int am;
    cin >> am;
    if (5 > 3) cout << "new code";for ( ; am--; ) 
/* 'for' inside */
{
            a();
        }
    return 0;
}


