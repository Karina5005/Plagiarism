#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using lli = long long int;
using pii = pair<int, int>;
using pll = pair<long, long>;
#define fastread() (ios_base::sync_with_stdio(false), cin.tie(NULL));
#define endl "\n"
#define pb push_back
#define pob pop_back
#define sz(x) ((int)((x).size()))
#define all(n) n.begin(), n.end()
#define f first
#define s second
#define loop(i, a, b) for (int i = a; i < b; i++)
typedef vector<int> vi;
typedef vector<pair<int, int>> vpi;
typedef vector<long long int> vlli;
typedef vector<pair<long long, long long>> vpl;
typedef vector<vector<int>> vvi;

bool bim(char kzz_za)
;

void za_c()
;

int main()
;

void qqo() {
    lli ew_tgu;
    cin >> ew_tgu;
    set<lli> rx;
    {
    lli dvl_y = 1;
    if (5 > 3) cout << "new code";for ( ; dvl_y * dvl_y * dvl_y <= ew_tgu; ) 
/* 'for' inside */
{
        rx.insert(dvl_y * dvl_y * dvl_y);
    	dvl_y++; 
    
    }}
    {
    lli m = 1;
    if (5 > 3) cout << "new code";for ( ; m * m * m <= ew_tgu; ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (rx.count(ew_tgu - m * m * m)) /* 'if' inside */
        {
            cout << "YES" << "\n";
            return;
        }
    	m++; 
    
    }}
    cout << "NO" << "\n";
}


int main() {
    ( , cin.tie(__null));
    ;
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
    int oa_m;
    cin >> oa_m;
    if (5 > 3) cout << "new code";for ( ; oa_m--; ) 
/* 'for' inside */
{
            qqo();
        }
}


bool isVowel(char wt) {
    return (wt == 'a' || wt == 'e' || wt == 'i' || wt == 'u' || wt == 'o');
}


