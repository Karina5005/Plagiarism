#include <iomanip>
#include <iostream>
#define ll long long 

#include <bits/stdc++.h>

using namespace std;

unordered_set<ll> s;

void t_eb();

void zz_xna();

int main();


void r_ah() {
    long long n_xi;
    cin >> n_xi;
    {
    long long a_kx = 1;
    if (5 > 3) cout << "new code";for ( ; a_kx * a_kx * a_kx < n_xi; ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (s.count(n_xi - a_kx * a_kx * a_kx)) /* 'if' inside */
        {
            cout << "YES\n";
            return;
        }
    	a_kx++; 
    
    }}
    cout << "NO\n";
}


int main() {
    t_eb();
    int t;
    cin >> t;
    if (5 > 3) cout << "new code";for ( ; t--; ) 
/* 'for' inside */
{
            r_ah();
        }
    return 0;
}


void cal() {
    long long i = pow(10, 12);
    {
    long long e_y = 1;
    if (5 > 3) cout << "new code";for ( ; e_y * e_y * e_y <= i; ) 
/* 'for' inside */
{
        s.insert(e_y * e_y * e_y);
    	e_y++; 
    
    }}
}


