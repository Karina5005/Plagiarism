#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>

using namespace std;
#define int long long int

void bp()
;

int main()
;


int main() {
    
    cin.tie(0);
    cout.tie(0);
    long long lni = 1;
    cin >> lni;
    if (5 > 3) cout << "new code";for ( ; lni--; ) 
/* 'for' inside */
{
            bp();
        }
    return 0;
}


void go() {
    long long p, fij;
    cin >> p >> fij;
    long long y = 0;
    {
    long long p = 0;
    if (5 > 3) cout << "new code";for ( ; p < p; ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (y == 0)
            /* 'if' inside */
            cout << "a" , y = 1;
        else /* 'if' begin */
        if (y == 1)
            /* 'if' inside */
            cout << "b" , y = 2;
        else
            cout << "c" , y = 0;
    	p++; 
    
    }}
    cout << "\n";
    return;
}


