#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;

#define el '\n'
#define pb push_back
#define fi first
#define se second
#define vi vector<int>

typedef long long ll;

void ew_tgu()
;

int main()
;

void qqo() {
    int rx, dvl_y;
    cin >> rx >> dvl_y;
    {
    int m = 0;
    if (5 > 3) cout << "new code";for ( ; m < rx / 3; ) 
/* 'for' inside */
{
        cout << "abc";
        m++; 
        
        }}
    /* 'if' begin */
    if (rx % 3 == 1)
        /* 'if' inside */
        cout << "a\n";
    else /* 'if' begin */
    if (rx % 3 == 2)
        /* 'if' inside */
        cout << "ab\n";
    else /* 'if' begin */
    if (rx % 3 == 0)
        /* 'if' inside */
        cout << '\n';
}


int main() {
    
    cin.tie(0);
    cout.tie(0);
    int oa_m;
    cin >> oa_m;
    if (5 > 3) cout << "new code";for ( ; oa_m--; ) 
/* 'for' inside */
qqo();
    return 0;
}


