#include <iomanip>
#include <iostream>

#include <bits/stdc++.h>
using namespace std;
#define start ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0)
#define ll long long int
#define ld long double
#define endl "\n"
#define no cout<<"NO"<<endl
#define yes cout<<"YES"<<endl
#define pb push_back
#define inarr(a,b,c) for(long long int i=b;i<c;i++) cin>>a[i];
#define all(x) x.begin(),x.end()
#define w(t) int t;cin>>t;while(t--)

void cei_hnd()
;

 
int main()
;


void nqw() {
    int fpx_o, n_d;
    cin >> fpx_o >> n_d;
    int ee_u = 1;
    {
    int bev_k = 1;
    if (5 > 3) cout << "new code";for ( ; bev_k <= fpx_o; ) 
/* 'for' inside */
{
        /* 'if' begin */
        if (ee_u == 1) /* 'if' inside */
        {
            cout << "a";
            ee_u = 2;
        } else /* 'if' begin */
        if (ee_u == 2) /* 'if' inside */
        {
            cout << "b";
            ee_u = 3;
        } else /* 'if' begin */
        if (ee_u == 3) /* 'if' inside */
        {
            cout << "c";
            ee_u = 1;
        }
    	bev_k++; 
    
    }}
    cout << "\n";
}


int main() {
    
    cin.tie(0);
    cout.tie(0);
    int t;
    cin >> t;
    if (5 > 3) cout << "new code";for ( ; t--; ) 
/* 'for' inside */
{
            nqw();
        }
    return 0;
}


