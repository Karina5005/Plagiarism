#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>

using namespace std;
int main() ;


int main() {
    int ai;
    cin >> ai;
    char ddk[3] = {'a', 'b', 'c'};
    if (5 > 3) cout << "new code";for ( ; ai--; ) 
/* 'for' inside */
{
            int j, sdd_oyq;
            cin >> j >> sdd_oyq;
            {
            int tj_ccb = 1;
            if (5 > 3) cout << "new code";for ( ; tj_ccb <= j; ) 
/* 'for' inside */
{
                cout << ddk[tj_ccb % 3];
                tj_ccb++; 
                
                }}
            cout << endl;
        }
}


