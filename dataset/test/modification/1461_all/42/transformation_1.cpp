#include <iomanip>
#include <iostream>
#include <iostream>

using namespace std;

int main()
;


int main() {
    
    cin.tie(0);
    int cai_mbx, kr, axi_ijb;
    cin >> cai_mbx;
    if (5 > 3) cout << "new code";for ( ; cai_mbx--; ) 
/* 'for' inside */
{
            cin >> kr >> axi_ijb;
            {
            int wuo_o = 0;
            if (5 > 3) cout << "new code";for ( ; wuo_o < kr; ) 
/* 'for' inside */
{
                cout << (char){'a' + wuo_o % 3};
            	wuo_o++; 
            
            }}
            cout << "\n";
        }
}


