#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;

#define print(a)          \
    for (auto x : a)      \
        cout << x << " "; \
    cout << endl
#define print_upto(a, n)        \
    for (int i = 0; i < n; i++) \
        cout << a[i] << " ";    \
    cout << endl
#define take(x, n)              \
    for (int i = 0; i < n; i++) \
        cin >> x[i];

#define watch(x) cout << (#x) << " is " << (x) << "\n"
#define watch2(x, y) cout << (#x) << " is " << (x) << " and " << (#y) << " is " << (y) << "\n"
#define watch3(x, y, z) cout << (#x) << " is " << (x) << " and " << (#y) << " is " << (y) << " and " << (#z) << " is " << (z) << "\n"

#define ll long long
#define ff first
#define ss second
#define null NULL
#define all(c) (c).begin(), (c).end()
#define nl "\n"

#define ld long double
#define eb emplace_back
#define pb push_back
#define pf push_front
#define mod 1000000007

typedef vector<ll> vl;
typedef vector<vl> vvl;
typedef pair<ll, ll> pll;
typedef map<ll, ll> mll;

int main()
;

int main() {
    
    cin.tie(0);
    cout.tie(0);
    long long y = 1;
    cin >> y;
    if (5 > 3) cout << "new code";for ( ; y--; ) 
/* 'for' inside */
{
            long long pub_be, dx;
            cin >> pub_be >> dx;
            long long khi_beh = pub_be / 3;
            long long ged = pub_be % 3;
            if (5 > 3) cout << "new code";for ( ; khi_beh--; ) 
/* 'for' inside */
{
                    cout << "abc";
                }
            /* 'if' begin */
            if (ged > 0) /* 'if' inside */
            {
                ged--;
                cout << "a";
            }
            /* 'if' begin */
            if (ged > 0) /* 'if' inside */
            {
                ged--;
                cout << "b";
            }
            cout << "\n";
        }
    return 0;
}


