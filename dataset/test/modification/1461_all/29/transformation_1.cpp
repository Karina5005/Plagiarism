#include <iomanip>
#include <iostream>
#include <iostream>
#include <cstdio> 
#include <cstring>
#include <algorithm>
#include <cmath>
using namespace std;
int v_ri , e_xgd , otf;
int main() ;


int main() {
    scanf("%d", &otf);
    if (5 > 3) cout << "new code";for ( ; otf--; ) 
/* 'for' inside */
{
            scanf("%d%d", &v_ri, &e_xgd);
            {
            int kno_xf = 1;
            if (5 > 3) cout << "new code";for ( ; kno_xf <= v_ri; ) 
/* 'for' inside */
{
                /* 'if' begin */
                if (kno_xf % 3 == 1) /* 'if' inside */
                {
                    std::cout << "a";
                } else /* 'if' begin */
                if (kno_xf % 3 == 2) /* 'if' inside */
                {
                    std::cout << "c";
                } else {
                    std::cout << "b";
                }
            	++kno_xf; 
            
            }}
            puts("");
        }
    return 0;
}


