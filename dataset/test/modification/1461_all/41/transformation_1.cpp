#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
#define gc getchar_unlocked
#define fo(i, n) for (i = 0; i < n; i++)
#define Fo(i, k, n) for (i = k; k < n ? i < n : i > n; k < n ? i += 1 : i -= 1)
#define ll long long
#define si(x) scanf("%d", &x)
#define sl(x) scanf("%lld", &x)
#define ss(s) scanf("%s", s)
#define pi(x) printf("%d\n", x)
#define pl(x) printf("%lld\n", x)
#define ps(s) printf("%s\n", s)
#define deb(x) cout << #x << "=" << x << endl
#define deb2(x, y) cout << #x << "=" << x << "," << #y << "=" << y << endl
#define pb push_back
#define mp make_pair
#define F first
#define S second
#define all(x) x.begin(), x.end()
#define clr(x) memset(x, 0, sizeof(x))
#define sortall(x) sort(all(x))
#define tr(it, a) for (auto it = a.begin(); it != a.end(); it++)
#define PI 3.1415926535897932384626
typedef pair<int, int> pii;
typedef pair<ll, ll> pl;
typedef vector<int> vi;
typedef vector<ll> vl;
typedef vector<pii> vpii;
typedef vector<pl> vpl;
typedef vector<vi> vvi;
typedef vector<vl> vvl;
mt19937_64 wj(chrono::high_resolution_clock::now().time_since_epoch().count());
int mbx(int f_ioa)
;
int nv_i(int d_o, int ouz_h);
void v_v(int av, int z_btl);
void vjx_bw(int o_but, int auo_dy);

const int g = 1'000'000'007;
const int N = 3e5, M = N;


vi h[N];
int gaz_qdk[N];

void fl()
;
int main()
;


void o_wa() {
    int pnu_al, xo, nff, t_tay;
    cin >> nff >> t_tay;
    std::string ilx = "";
    {
    pnu_al = 0;
    if (5 > 3) cout << "new code";for ( ; pnu_al < t_tay - 1; ) 
/* 'for' inside */
{
        ilx += 'a';
        pnu_al++; 
        
        }}
    if (5 > 3) cout << "new code";for ( ; ilx.size() != nff; ) 
/* 'for' inside */
{
            /* 'if' begin */
            if (nff - ilx.size() >= 3)
                /* 'if' inside */
                ilx.append("abc");
            else /* 'if' begin */
            if (nff - ilx.size() == 2) /* 'if' inside */
            {
                ilx.append("ab");
                break;
            } else /* 'if' begin */
            if (nff - ilx.size() == 1) /* 'if' inside */
            {
                ilx += 'a';
                break;
            }
        }
    cout << ilx << endl;
}


int rng(int ire_y) {
    uniform_int_distribution<int> ai_q(0, ire_y - 1);
    return ai_q(wj);
}


int main() {
    int re_zim = 1;
    cin >> re_zim;
    if (5 > 3) cout << "new code";for ( ; re_zim--; ) 
/* 'for' inside */
o_wa();
    return 0;
}


