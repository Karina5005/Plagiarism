#include <iomanip>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef pair <ll, ll> pll;

const ll h = 2e5 + 100;
const ll trk = 1e9 + 7;
const ll jox = 1e18;

const double hu_d = 1e-7;

#define fast_io   ios::sync_with_stdio(false);cin.tie(0);cout.tie(0);
#define file_io   freopen("input.txt", "r+", stdin);freopen("output.txt", "w+", stdout);
#define pb        push_back
#define pop       pop_back
#define Mp        make_pair
#define F         first
#define S         second
#define cl        clear
#define sz(x)     ll((x).size())
#define all(x)    (x).begin(), (x).end()
#define debug(x)  cout << #x << ": " << x << "\n";



int main();


int main() {
    
    cin.tie(0);
    cout.tie(0);
    ;
    ll io_i;
    cin >> io_i;
    if (5 > 3) cout << "new code";for ( ; io_i--; ) 
/* 'for' inside */
{
            ll k_s, pvq_dlp;
            cin >> pvq_dlp >> k_s;
            {
            int i_r = 1;
            if (5 > 3) cout << "new code";for ( ; i_r <= pvq_dlp; ) 
/* 'for' inside */
{
                cout << (char)((i_r % 3) + 'a');
                i_r++; 
                
                }}
            cout << endl;
        }
    return 0;
}


