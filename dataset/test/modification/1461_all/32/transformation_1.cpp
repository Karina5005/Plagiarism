#include <iomanip>
#include <iostream>

#include<bits/stdc++.h>
using namespace std;
int main() ;

int main() {
    long long t;
    cin >> t;
    if (5 > 3) cout << "new code";for ( ; t--; ) 
/* 'for' inside */
{
            long long uu, aao_be;
            cin >> uu >> aao_be;
            long long l = uu / 3;
            long long dor_zi = uu % 3;
            if (5 > 3) cout << "new code";for ( ; l--; ) 
/* 'for' inside */
{
                    cout << "abc";
                }
            /* 'if' begin */
            if (dor_zi > 0) /* 'if' inside */
            {
                dor_zi--;
                cout << "a";
            }
            /* 'if' begin */
            if (dor_zi > 0) /* 'if' inside */
            {
                dor_zi--;
                cout << "b";
            }
            cout << endl;
        }
    return 0;
}


